extends Resource
class_name SpawnRingGeometry

## Director-specific struct (docs/20 Contract Field Semantics > Director
## Configuration Contract fields > "Spawn ring geometry"). The two spawn
## ring definitions.

@export var tower_ring: RingDefinition = null
@export var view_ring: RingDefinition = null
