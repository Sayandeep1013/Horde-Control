extends Resource
class_name DropTable

## Shared struct (docs/20 Contract Field Semantics > Shared fields and struct
## types > "Drop table"). The fixed reward a Logical Death places on the
## field; values match the entity's tier in Resource & Economy System >
## Drop Table.

@export var xp_shards: int = 0
@export var scrap: int = 0
@export var cores: int = 0
