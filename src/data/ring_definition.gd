extends Resource
class_name RingDefinition

## Nested struct inside Director Configuration's Spawn ring geometry struct
## (docs/20 Contract Field Semantics > Director Configuration Contract
## fields > "Spawn ring geometry"). One of the two spawn ring definitions
## (Tower ring or view ring).

@export var inner_radius_px: int = 0
@export var width_px: int = 0
