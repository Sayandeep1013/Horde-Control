extends Resource
class_name FinisherSpawnRate

## Nested struct inside the shared Overtime condition struct (docs/20
## Contract Field Semantics > "Overtime condition" > finisher spawn rate).

@export var count: int = 0
@export var interval_seconds: float = 0.0
