# Phase 03 - Execution Log

Dated record of every action, command, file change, test run, and result for this phase.

| Date | Task | Action | Command or file | Result |
| --- | --- | --- | --- | --- |
| 2026-09-19 | Entry | Phase entered under decisions D97-D100, the author's prototype-first direction, rather than in the plan's own sequence. Phase 02 is six of seven tasks complete with P1.7 deferred by D97 and no review gate convened | - | Recorded so a later reader does not mistake this for the documented order. The engine spine it builds on is complete: SimClock, PauseAuthority, SimLoop, keyed RNG, EventBus, EntityRegistry, CombatStats, pools with all six caps, the debug overlay and Run Recorder, the audio layout, and the hitbox/hurtbox/death framework |
| 2026-09-19 | Entry | Confirmed the inherited state before delegating | `git status`; `tests/run_tests.ps1` | Working tree clean, 172 tests passing with zero engine errors under the guard added in Phase 02 |
| 2026-09-19 | Entry | Recorded the one debt this phase inherits and must not compound | `LEDGER.md` F03-01 | The placeholder enemy's HP, damage and Visual-Death duration have no Register rows. P2.5 takes its numbers from the Register and escalates anything missing rather than copying a fixture value forward |
| 2026-09-19 | P2.1, P2.2, P2.4 | Delegated the player controller, the arena and camera, and the Tower to three Sonnet implementers in parallel, scoped to separate directories so they cannot collide | - | In progress. Each brief carries the no-delete/no-export constraints, requires values to be read from an authored `.tres` rather than from script constants, requires code-driven animation using only the permitted tween APIs, and requires `run_tests.ps1` to be run at the end because a green gdUnit4 summary is no longer sufficient evidence (Phase 02 F02-14) |
| 2026-09-19 | P2.2 | Arena and camera delivered | `scenes/arena.tscn`; `src/camera/game_camera.gd`, `vignette.gdshader`; `src/environment/arena_bounds.gd`; 10 new tests | Arena at the Register's dimensions with a tiled floor and four flush `ArenaBounds` walls, no interior obstacles. The camera does velocity-based lead plus exponential smoothing, with trauma-based shake and a zoom hook, and applies the arena clamp **last**, after lead and shake, never through `Camera2D.offset` |
| 2026-09-19 | P2.2 | Camera bounds test, the named acceptance test | `tests/unit/camera_bounds_test.gd` | Sweeps four corners and four edge midpoints at view scales 0.9, 0.95, 1.0, 1.05, 1.1 and 1.15 - the full range the exit criterion names. 182 tests at exit 0 (172 pre-existing plus 10), and `run_tests.ps1` clean with no engine errors under the F02-14 guard |
| 2026-09-19 | P2.2 | Falsification, and a blind spot the implementer closed before claiming anything | - | Clamp removed entirely gives exit 100; Y-axis clamp removed gives exit 100 with X still passing, so the breakage is axis-specific; clamp ignoring the arena centre gives exit 100 - **but only after the implementer noticed every case used a zero centre and added a non-zero-centre test first**. Without that, the third mutation would have been reported as caught when it was not. Recorded as F03-03 |
| 2026-09-19 | P2.2 | Three findings recorded | `LEDGER.md` | F03-02, the first GodotPrompter skill conflict in this project that would have produced a visible bug rather than a stylistic difference; F03-04, an uncatalogued gdUnit4 exit code 105 after editing a `class_name` script; F03-05, an open cross-task seam where the camera assumes the Tower sits at the world origin |
| 2026-09-19 | P2.1 | Player controller delivered | `data/player/prototype.tres`; `src/player/player.gd`, `collector.gd`, `player_animator.gd`; `scenes/player.tscn`; 46 new tests | Every value read from the authored `.tres` rather than from script constants, each cited to the Register's Player & Weapons rows. Code-driven cosmetic animation - idle bob, squash-and-stretch, directional lean, hit flash - using only the permitted tween API |
| 2026-09-19 | P2.1 | Both named acceptance tests, each falsified twice | `tests/unit/player_movement_test.gd`, `player_silhouette_test.gd` | Movement check: instant-snap with no ramp caught; a hardcoded wrong timing constant caught. Silhouette test: a wrong `z_index` caught in two files; a distinctness check comparing the silhouette against itself rather than against an enemy caught. All restored and re-run green |
| 2026-09-19 | P2.1 | A real bug the task's own tests caught before review | `src/player/player_animator.gd` | Squash-and-stretch mis-signed every full stop as a stretch, because acceleration was projected onto the wrong reference direction at the instant velocity hit exactly zero. Fixed and covered by a test. Recorded as F03-10 |
| 2026-09-19 | P2.1 | Suite total and the engine-error guard | `run_tests.ps1 -TestPath res://tests/unit` | 245 tests at exit 0, guard clean with no engine errors. The total includes P2.2's and P2.4's concurrent additions, not P2.1's alone; P2.1's own contribution is 46 |
| 2026-09-19 | P2.1 | **The banned-API check is currently red**, and the cause is a defect in the check rather than in the code | `tools/checks/banned_api_check.sh` | It matches the banned call inside comments, so a line documenting the rule trips it. Recorded as F03-07 and held until the tree settles, because the offending file belongs to a concurrently running task |
| 2026-09-19 | P2.4 | Tower delivered | `data/tower/base.tres`, `base_weapon.tres`; `scenes/tower.tscn`; eight scripts under `src/tower/`; 17 new tests | Every value authored into the `.tres` from the Register - health, shield fraction, regeneration rate and delay, footprint and interaction radii, targeting range, evolution thresholds, repair price. Health, weapon, interaction radius, evolution stage and visuals are separate components rather than one class |
| 2026-09-19 | P2.4 | All three named acceptance tests, each falsified by real mutation rather than scenario variation | `tests/unit/tower_weapon_test.gd`, `tower_same_frame_death_test.gd`, `tower_health_recovery_test.gd` | Weapon check: disabling Seeker priority caught; changing damage in the `.tres` caught. **Same-frame death**: reversing the resolution order caught, and breaking the already-ended latch so one-tick-apart bleeds into same-tick credit caught - built on the real `player.tscn` and the real Tower, both driven to zero inside one synchronous call with no frame boundary between them. Health recovery: disabling delay-restart-on-hit caught; removing the shield clamp caught. Every mutation restored and verified byte-identical by `diff` |
| 2026-09-19 | Orchestrator | Settled verification pass across all three parallel tasks, run only after the last landed rather than against a moving tree | `run_tests.ps1 -TestPath res://tests/unit` | **245 of 245 at exit 0, engine-error guard clean.** F03-05's seam resolves favourably: `scenes/tower.tscn`'s root carries no position override, so the Tower sits at the world origin and the camera's `arena_center` assumption holds - verified rather than assumed |
| 2026-09-19 | Orchestrator | Fixed F03-07, the red P1.1 exit-criterion check, at the cause rather than by rewording the comment that tripped it | `tools/checks/banned_api_check.sh` | The check now strips comments before matching. **Falsified four ways**: a real banned call goes red; the same call in a comment stays green; a real call with a trailing comment on the same line still goes red, which was the actual regression risk of stripping; and a real call under `src/ui/` stays green, so the pure-UI exemption survives. The strip's one failure direction is documented in the script rather than left to be found |
| 2026-09-19 | Orchestrator | Repeated an error of my own from Phase 01 and caught it | - | Read the banned-API check's exit status through a pipe into `head`, which reports the pager's status rather than the script's, and briefly recorded exit 0 for a check that was failing. Re-run without the pipe gave exit 1. The same mistake produced a false all-clear once before; recorded again because it keeps being available |
| 2026-09-19 | Entry (2nd session) | Re-ran the phase-entry checks before delegating the second wave | `git status`; `git -C tools/mcp/coding-solo rev-parse HEAD`; `git -C tools/mcp/comprehensive rev-parse HEAD`; `ls tools/.gdignore sandbox/.gdignore` | Working tree clean at `e31e991`. Both MCP pins match docs/28 (`1209744fad78f3998f98c7394fd0f6ef50da5281`, `fcbc29e03297900b9a1fcc6c7cbae116fe9d532a`). Both `.gdignore` files present, so the next export does not pack the MCP servers |
| 2026-09-19 | P2.3, P2.5, P2.6 | Delegated the handgun and auto-targeting, the three enemies, and the HUD and threat feedback to three Sonnet implementers in parallel, scoped to disjoint directories - `src/combat/auto_weapon*` + `data/weapons/`, `src/enemy/` + `data/enemies/`, and `src/ui/` + `scenes/ui/` | - | In progress. Each brief carries the no-delete, no-export, sandbox-only constraints explicitly, because the ask gate does not intercept subagents (F-06); requires every number to come from the Register with the row cited and anything missing escalated rather than copied forward from a fixture (F03-01); requires `physics_step(delta)` plus a `driven_externally` export on every self-stepping entity, so the SimLoop wiring has one uniform seam; forbids editing `src/core/event_bus.gd` and instead requires each task to list the signals it needed; and requires each named acceptance test to be falsified by real mutation of the source or the `.tres`, restored, and verified byte-identical by `git diff` |
| 2026-09-19 | Orchestrator | Reserved four integration items to a single owner rather than letting three parallel tasks each reach for them | `LEDGER.md` F03-06, F03-09, F03-15 | The EventBus signal list; the `data/tower/base_weapon.tres` move to `data/weapons/`; the SimLoop driver that ends entities stepping themselves; and the `player_died` signal that stops `DeathState` broadcasting the player's death as an enemy kill. All four are shared-file edits, and F02-13 and F03-05 are both cases of two parallel implementers making defensible choices that broke where they met |
| 2026-09-19 | P2.3 | Handgun and auto-targeting delivered | `data/weapons/handgun.tres`; `src/combat/auto_weapon.gd`, `player_projectile.gd`; `scenes/player.tscn`; 13 new tests | `unique_id = "handgun_prototype"` matches the `starting_weapon_reference_id` `data/player/prototype.tres` was carrying provisionally, closing half of F03-09. The weapon re-picks the nearest target every shot rather than holding it - deliberately not the Tower's C-TOWERTARGET rule - and reads no Input API at all, which is the task's "no targeting input exists" exit criterion. The projectile carries the `intersect_ray` sweep, draws at z_index 30 at 70% opacity, and resolves its damage source by value at fire time per the Projectile Orphans rule |
| 2026-09-19 | P2.3 | Weapon check, the named acceptance test, with all 13 tests falsified individually | `tests/unit/weapon_check_test.gd`, `player_projectile_test.gd` | Each mutation broke the real `.tres` or `.gd`, not the test, and each was restored and verified byte-identical by `diff` - the files are untracked, so `git diff` does not apply to them |
| 2026-09-19 | P2.3 | A falsification that caught its own test rather than the code | - | The first sweep test did not prove the sweep mattered: the projectile's own 6 px collision radius gave Godot's discrete check enough combined radius to catch the geometry with no sweep at all. Caught during falsification, before anything was claimed. Same shape as F03-03, and the second time in this phase that setting up a mutation exposed the test rather than the code |
| 2026-09-19 | P2.3 | A real defect found in another task's committed code, and verified rather than taken on report | `LEDGER.md` F03-17 | `src/tower/tower_projectile.gd` has no `intersect_ray` sweep despite 900 px/s exceeding the 12 px/tick threshold docs/20 sets - and docs/20 names the Tower's projectile as its own example. Confirmed by the orchestrator against the source before recording: `grep -rn intersect_ray src/` finds it only in `player_projectile.gd`. P2.4's three named acceptance tests all passed without exercising it |
| 2026-09-19 | P2.6 | HUD and threat feedback delivered | `src/ui/hud.gd`, `hud_bar.gd`, `hud_truncatable_label.gd`, `hud_economy_state.gd`, `threat_feedback.gd`; `scenes/ui/hud.tscn`, `threat_feedback.tscn`; 35 new tests across five suites | All four HUD fields with the Register's 40% tick and border-shape rule, so the state is never signalled by colour alone; the 8-segment vignette and the off-screen Tower indicator; and the Tower cue wired through P1.6's existing `TowerCuePlayer` and `AudioDucking` rather than duplicating either. Scrap, XP, level, rerolls and the wave counter have no owning system until Phases 04 and 05, so they read through a named `HudEconomyState` seam rather than an invented economy |
| 2026-09-19 | P2.6 | Tower cue audibility check, the named acceptance test, falsified four ways | `tests/unit/tower_cue_audibility_test.gd` and four further suites | Swapping `tower_x` and `player_x` in the cue-play call caught; removing the ducking hookup caught; disabling the Scrap label's ellipsis trimming caught; oversizing a HUD field so it overflows the viewport caught. Every mutation was confined to this task's own files and each was restored and `diff`-confirmed byte-identical. The suite asserts `SFX_Priority` is itself *unchanged* while SFX, Ambience and Music duck, which is the property that makes the cue audible rather than merely loud |
| 2026-09-19 | P2.6 | A real defect the task's own test caught before review | `LEDGER.md` F03-20 | The XP bar was pinned off-screen at y=1080: a raw-anchor `MarginContainer` under a plain `Control` never grows to fit content added after the anchor preset is set. Invisible to every assertion about the HUD's contents; only a test asserting screen position could see it |
| 2026-09-19 | P2.6 | Three escalations and two more skill conflicts recorded rather than resolved | `LEDGER.md` F03-18, F03-19 | The Register specifies the vignette's trigger, window and fade but not what damage amount corresponds to full intensity - the one number that decides how the effect reads on screen. No audio asset exists in the repository at all, so the Tower cue is a placeholder tone |
| 2026-09-19 | Assets | Author redirected prototype art to free online assets, CC0 only, with the generated sprites kept as a fallback rather than deleted | Author instruction this session | This selects the alternative D99 itself named and rejected ("use a CC0 asset pack such as Kenney's"), so it needs its own Review Decision Log row rather than an edit to D99. Proposed as D101; the author writes or approves the row. The generator and its sprites stay, so the `--silhouette` regeneration that proves entity distinguishability survives the swap |
| 2026-09-19 | Assets | Proved the download path before relying on it, per the project's own rule about tooling changes | `curl` against kenney.nl, opengameart.org, freesound.org | Network reachable. Kenney's zip URL is not on the asset page as a plain link - it sits inside the page's download modal as `id='donate-text' href='...zip'`, so it is extracted rather than guessed; five guessed filenames all returned 404 before the real pattern was found. Eleven packs resolved and downloaded, 31 MB, every one carrying its own CC0 licence file stating commercial use is permitted and credit is optional |
| 2026-09-19 | Assets | Packs downloaded to the scratchpad rather than the repository | `scratchpad/kenney/` | Only the files actually used will be committed, so provenance stays exact and the tree stays small. Nothing has been copied into `assets/` yet |
| 2026-09-19 | Assets | **A candidate set was rejected on measured evidence before anything was wired in** | `LEDGER.md` F03-21 | Kenney's top-down characters were rendered flat black from their own alpha channels and compared. Zombie and robot are near-identical ovals; survivor and soldier differ only by a gun stub. The one readability property the master constrains is the one a stock human pack cannot supply |
| 2026-09-19 | P2.5 | Three enemies delivered, one per target intent | `data/enemies/tower_seeker.tres`, `player_hunter.tres`, `opportunist.tres`; `src/enemy/enemy_controller.gd`, `attack_slot_manager.gd`, `enemy_ai_tuning.gd` and `.tres`; three scenes under `scenes/entities/`; 5 new suites | One controller drives all three intents - steering, attack cycle, body-block, the leash timer and its live conversion, the Opportunist event rule, and the stuck ladder. The C-SLOTS attack-slot geometry is its own component. The tags are read from `TowerWeapon`'s own constants rather than retyped, and a converting Hunter re-registers, which closes F03-15's tag half |
| 2026-09-19 | P2.5 | Three named acceptance tests, each falsified against real source | `tests/unit/leash_test.gd`, `opportunist_test.gd`, `stuck_exemption_test.gd` | Leash: reset-on-hit disabled caught; timeout-to-conversion disabled caught. Opportunist: the 25%-closer hysteresis gate removed - and the first attempt did **not** fail, because the test's hardcoded distance did not account for the enemy's own movement during setup; fixed to compute distance dynamically, then it caught the mutation |
| 2026-09-19 | P2.5 | **A mutation with zero effect exposed dead code rather than a weak test** | `LEDGER.md` F03-23 | Deleting the stuck-rule body-block exemption changed nothing, because a body-blocked Seeker is already short-circuited by the `in_attack_range` early return before the exemption is reached. The clause was removed, the surrounding documentation corrected, and the real mechanism falsified instead. Every other mutation this phase proved a test could catch a defect; this one proved a clause of the implementation was doing nothing |
| 2026-09-19 | P2.5 | Six real defects found and fixed during the task's own work, before any review | `evidence/p25_report.md` | A teardown crash from passing freed nodes to statically typed parameters; a stuck-tracking bug that would have despawned an enemy the instant an exemption ended; three suites leaking a `SimClock` each; a test reading `SimClock.now - 1.0`, which can go negative early in a process and collide with the leash sentinel; `round()` overshooting a just-short-of-threshold in the shared tick helper; and an Opportunist test whose Tower was close enough that the enemy's own chase defeated the out-of-range premise |
| 2026-09-19 | Orchestrator | Settled verification pass, run by the orchestrator against the whole tree after the last of the three tasks landed | `.\tests\run_tests.ps1 -TestPath res://tests/unit`; `bash tools/checks/banned_api_check.sh` | **323 test cases, exit 0, 0 errors, 0 failures, 0 flaky, 0 skipped, 0 orphans**, no engine-error line under the F02-14 guard. Banned-API check exit 0. Note for later sessions: `pwsh` is **not installed** on this machine - the runner is Windows PowerShell 5.1, so the script is invoked directly rather than through `pwsh -File` |
| 2026-09-19 | Orchestrator | Spot-checked delegated output against the Register rather than trusting the reports, per LESSONS rule 5 | `data/enemies/tower_seeker.tres`; `data/weapons/handgun.tres`; `src/enemy/enemy_controller.gd` | Tower Seeker matches every Register value - speed 0.55x, 60 HP, body radius 14, melee 15 damage per 1.5 s cycle, 0.4 s wind-up, reach 20 px, Contact None. Handgun matches its row - 10 damage, 2 shots/s, range 260, projectile 1000 px/s - and its derived lifetime uses the same 1.1 safety margin the Tower established under F03-16, so the two projectiles are consistent. The seeker tag is read from `TowerWeapon.SEEKER_TAG` rather than retyped |
| 2026-09-19 | Orchestrator | Re-classified a seam the implementer reported as routine | `LEDGER.md` F03-22 | P2.5 named, correctly and per its brief, that melee and contact damage resolve inside the overlap callback rather than through `SimLoop.enqueue_hit()`, following precedent set by P1.5 and P2.4. Recorded as a **Major** rather than a note: three tasks have now repeated it, `_step_07_hit_queue_resolution()` sorts a queue that gameplay never fills, and the deterministic same-tick ordering that step exists to provide is the guarantee Phase 02 built its whole spine around |
| 2026-09-19 | Integration | Delegated the asset wiring and the playable prototype scene to a single implementer, sequentially rather than in parallel | - | One owner, because the three seams this phase has already hit (F02-13, F03-05, F03-15) were all two parallel implementers making defensible choices that broke where they met. The architectural pass - F03-06, F03-09, F03-17, F03-22 - is explicitly excluded from this task's scope and follows as its own task, so the shared core files have exactly one writer |
| 2026-09-19 | Integration | Playable prototype scene assembled and the CC0 assets wired | `scenes/prototype.tscn`; `src/integration/prototype_integration.gd`; `src/enemy/telegraph_visual.gd`; `src/ui/ui_sfx.gd`; `tests/unit/prototype_scene_test.gd`; edits across the arena, Tower, player, enemy and UI scenes | Arena, Tower at the world origin, player, camera, HUD and threat feedback, with one hand-placed enemy per intent and no Wave Director - P2.7's own composition. All 23 assets wired, including `tower_damage.ogg` replacing P2.6's placeholder tone, which closes F03-18's audio half. The `AudioDucking` process-mode contradiction was **avoided rather than resolved**: a single shared node sits as a sibling of `Main` and is referenced rather than parented, and the open author decision stands |
| 2026-09-19 | Integration | A real Godot behaviour caught by the integration task | `scenes/player.tscn`, `scenes/tower.tscn` | `z_as_relative` accumulates through every relative `CanvasItem` ancestor, and 4.7.1's actual default is `true` - contradicting the `2d-essentials` skill's stated default of `false`. Nesting the player and Tower under `Main` for the first time pushed their projectiles to an effective z of 80 and 55 instead of the Register's 30. Fixed with one property per `Projectiles` container and falsified by removing the fix and confirming the predicted wrong value |
| 2026-09-19 | Orchestrator | Verified the integration independently rather than accepting the report | `git diff` per protected file; a resolve check over every `res://assets/` path; `.\tests\run_tests.ps1` | All six do-not-touch files confirmed unmodified. Every one of the 20 asset paths referenced in `scenes/`, `data/` and `src/` resolves to a file on disk. 353 of 353 at exit 0, engine-error guard clean |
| 2026-09-19 | Orchestrator | **The spot-check found a Major the whole 353-test suite did not** | `LEDGER.md` F03-27 | The three enemy scenes had no sprite node at all - every enemy invisible in the assembled prototype. The generated art was referenced only by the silhouette test, which compares PNG files on disk and so passed while nothing rendered them. Found by asking which files referenced the enemy sprites, rather than by asking whether the referenced textures loaded |
| 2026-09-19 | Orchestrator | Fixed at the cause, and the fix's own test was falsified before it was trusted | `scenes/entities/*.tscn`; `tests/unit/entity_visual_test.gd`; `LEDGER.md` F03-28 | A `Visuals` + `Sprite2D` pair per enemy, matching the player's convention. The new suite's **first version passed against the very defect it was written to catch**, because `TelegraphVisual` builds a `Sprite2D` child at runtime and satisfied the check. Strengthened to skip telegraph subtrees, re-falsified, and the exclusion's reason written into the helper |
| 2026-09-19 | Orchestrator | Settled verification after the fix | `.\tests\run_tests.ps1 -TestPath res://tests/unit`; `bash tools/checks/banned_api_check.sh` | **356 test cases, exit 0, 0 errors, 0 failures, 0 flaky, 0 skipped, 0 orphans**, engine-error guard clean. Banned-API check exit 0 |
| 2026-09-19 | Orchestrator | Ran the assembled prototype for the first time, headless, rather than assuming a scene that instantiates in a test also runs | `Godot ... --headless --path D:/Gamedev res://scenes/prototype.tscn --quit-after 180` | 180 frames with no gameplay errors. Two shutdown warnings recorded as F03-30 and left undiagnosed rather than guessed at |
| 2026-09-19 | Orchestrator | Fixed the two things that stopped the author playing it | `LEDGER.md` F03-29; `project.godot`; `tests/settings_check.gd`; `scenes/prototype.tscn` | `run/main_scene` pointed at the bare container scene, so pressing play gave an empty screen; and F1 did nothing because the P1.4 debug overlay was never instanced here. Both fixed. The Settings check's `MAIN_SCENE_PATH` was updated in the same change rather than the assertion being loosened, and re-run: PASS on 16 layers, 24 actions, 8 settings, 6 autoloads. Full suite still 356 at exit 0 |
| 2026-09-19 | P2.7 | **Feel check run by the author. Verdict: ADJUST, not go.** | Author, playing `scenes/prototype.tscn` | The author's words: "even as a prototype its bad ... where is the wave ... use proper assets ... use proper sceneery". Three named adjustments: no wave pacing, entity art not good enough, and an empty arena. Recorded verbatim because P2.7's exit criterion is a recorded go-or-adjust verdict and the adjustment has to be named before it can be made. This is the phase's actual deliverable and it is the author's alone |
| 2026-09-19 | P2.7 | Orchestrator's own error, recorded because it shaped the verdict | - | The scene was handed over as "the prototype is playable" when what was playable was a **feel-check fixture** - three hand-placed enemies in an empty box, by P2.7's own definition - not a game. The absence of waves is the documented Phase 04 boundary, but the author met it as a surprise rather than as a stated limit. Setting that expectation was the orchestrator's job and it was not done. The lesson is not about scope: it is that handing someone a fixture and calling it a prototype guarantees the verdict is about the missing frame rather than about the thing being tested |
