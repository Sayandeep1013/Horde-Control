# Phase 03 - Ledger

Every finding raised against this phase, carried across review iterations.

Severities: Blocker, Major, Minor. Statuses: open, fixed, closed, deferred with owner, recorded, withdrawn.

Structural rule, carried from Phases 01 and 02 where this file's shape broke four times between them: every finding row sits **below** the header, carries exactly seven columns, and holds a status word in the Status column. A column count alone catches none of the three ways it actually broke, so the check asserts all three. Escaped pipes inside a cell also break the count - write "or" instead.

| ID | Finding | Severity | Raised by | Status | Evidence | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| F03-01 | Entering this phase, the enemy fixture constants P1.5 used - HP, contact damage, Visual Death duration - have **no Provisional Values Register rows**. P1.5 correctly treated them as framework test fixtures rather than gameplay content and flagged the debt (Phase 02 F02-18). P2.5 authors the first real enemies and is where that debt comes due | Minor | Orchestrator, at phase entry | open | `phases/PHASE_02_Technical_Foundations/LEDGER.md` F02-18; the placeholder enemy's constants | Open until P2.5 lands. The rule the phase must not break: a real enemy takes its numbers from the Register, and any number the Register does not carry is escalated rather than copied forward from a fixture. Fixture values becoming de facto gameplay values by being inherited is exactly how the one-place rule gets lost |
| F03-02 | **GodotPrompter skill conflict, the first one that actually changes code in this project.** The `camera-system` skill's reference screen-shake implementation writes to `Camera2D.offset`. The Provisional Values Register's "Lead/shake application" row forbids exactly that, because `offset` is applied after the camera's own transform and therefore bypasses the arena-bounds clamp - shake would push the view outside the walls | Minor | P2.2 implementer | recorded | The skill's reference implementation against the Register row; the implementer followed the project and applied lead and shake before the clamp instead | CLAUDE.md requires a skill-versus-project conflict to be recorded here, with the project winning. Worth noting this is the first conflict in three phases where following the skill would have produced a visible bug rather than merely a stylistic difference - the earlier ones (gdUnit4 CLI guidance, GUT-over-gdUnit4) were about tooling advice. The skills advise on Godot idiom and this project has a rule the idiom does not know about |
| F03-03 | P2.2's own acceptance test could not catch one of the three defects it was asked to falsify against. Every case in the suite used `arena_center = Vector2.ZERO`, so a clamp that ignores the arena's centre offset produced identical results to a correct one | Minor | P2.2 implementer, self-caught **before** running the mutation | fixed | The implementer added `test_visible_rect_respects_a_non_zero_arena_center` first, then ran the mutation, which then failed with 24 failures. Without that test the mutation would have been reported as caught when it was not | Fixed inside the task, and the sequence is what matters: the implementer noticed the gap while setting up the falsification rather than after, and closed it before making any claim. That is the fourth named-test blind spot found in three phases (F01-15, F02-02, F02-16, this) and the first caught by the implementer **pre-emptively** rather than by a reviewer afterwards |
| F03-04 | Engine behaviour worth carrying: the first gdUnit4 run immediately after editing a `class_name`-registered script twice failed test discovery with `Class GameCamera hides a global script class` and exit **105**, a code this project had not previously catalogued. A second `--import` pass always resolved it | Minor | P2.2 implementer | recorded | Observed twice, resolved twice by re-importing; all reported results were taken from post-settle runs | Recorded rather than diagnosed, per the project rule about untested causes. Two consequences: exit 105 joins the exit codes a later phase may meet, and `tests/run_tests.ps1` currently maps any uncatalogued code to "not a usable run" and exit 1, which is the correct fail-closed behaviour but names the wrong cause. Worth revisiting if 105 recurs often enough to deserve its own branch |
| F03-05 | Open cross-task dependency: P2.2 read "Tower at centre" as the world origin and set `arena_center = Vector2.ZERO`, because P2.4 had not yet placed `tower.tscn` when the arena was built. If the Tower lands anywhere else, the camera clamps around the wrong point | Minor | P2.2 implementer | open | The implementer flagged it as a one-line fix and named it rather than assuming | Open until P2.4 lands and the two are wired together. This is the same class of seam that produced F02-13, where two parallel implementers each made a defensible choice that broke where they met - so it is checked explicitly at integration rather than assumed to agree |
| F03-06 | `DeathState` emits `EventBus.emit_enemy_died()` unconditionally, so **the player's own death is broadcast as an enemy death**. There is no `player_died` signal to emit instead, and `src/core/event_bus.gd` was outside P2.1's write scope | Minor | P2.1 implementer | open | `src/combat/death_state.gd` emits the same signal for every entity it kills; `event_bus.gd` declares only the three signals docs/20 names as examples | Open. A real mislabelling with a real consequence once anything listens - a wave-completion or kill-count listener would count the player's death as a kill. The fix belongs wherever the EventBus signal list is extended, which F02-11 already records as deliberately non-exhaustive: docs/20 names three signals as examples and later phases add their own as the owning systems are built. P2.6 (HUD) or the integration task is the natural place |
| F03-07 | **A P1.1 exit-criterion check is currently red, on a false positive.** `tools/checks/banned_api_check.sh` matches the banned call inside **comments**, so a line documenting the rule - literally quoting `get_tree().create_tween()` to explain why it must not be used - trips the check. Found on `src/tower/tower_visuals.gd`; the P2.1 implementer hit the same class of match in its own file and reworded around it | Minor | P2.1 implementer | open | The check fails while a scoped grep confirms `src/player/**` and `scenes/player.tscn` carry no real banned call | **Fixed.** The check now strips comments before matching. Falsified four ways: a real banned call in gameplay code goes red; the same call in a comment stays green; **a real call with a trailing comment on the same line still goes red**, which was the actual regression risk of stripping; and a real call under `src/ui/` stays green, so the master's pure-UI exemption survives. The strip's one failure direction - a hash inside a string literal preceding a real call on the same line - is stated in the script rather than left to be discovered. Originally: The check is right to be blunt but wrong to read comments: the rule is about calls, and a codebase that explains its own rules in comments will keep tripping it. Rewording each comment is the workaround the P2.1 implementer took under scope pressure; stripping comments before matching is the fix |
| F03-08 | Skill-versus-project convention difference, recorded because CLAUDE.md requires it: the `godot-testing` skill prescribes a `test_*.gd` prefix for suites, while this project has used a `*_test.gd` suffix since Phase 01. The P2.1 implementer followed the project and flagged it, but `LEDGER.md` was outside its write scope so it could only record it in its own evidence file | Minor | P2.1 implementer | recorded | `evidence/p21_report.md`; the existing suites under `tests/unit/` | Project wins, as CLAUDE.md states. Copied here by the orchestrator because a finding that only exists in an implementer's evidence file is not on the record - the same gap F02-13 was raised about, where a disclosure inside prose never became a row. Also noted: the implementer read CLAUDE.md's rule broadly and invoked two skills beyond the two named in its brief, which is the right reading |
| F03-09 | Two integration debts P2.1 named rather than crossing scope to fix: the player drives itself from `_physics_process` with a `driven_externally` export for a later task to flip, because `sim_loop.gd`'s own header bans entities running their own `_physics_process` but `src/core/` was out of scope; and `data/player/prototype.tres` carries a provisional `starting_weapon_reference_id` of `handgun_prototype` pending P2.3 authoring the real weapon resource | Minor | P2.1 implementer | open | `src/player/player.gd`; `data/player/prototype.tres` | Both are correct calls under a scoped brief and both are real debts for the integration task that wires the slice together. The SimLoop one matters more than it looks: the fixed tick order is a determinism guarantee, and an entity stepping itself outside that order is exactly what the order exists to prevent |
| F03-10 | A real bug the task's own tests caught before review: squash-and-stretch mis-signed every full stop as a **stretch** rather than a squash, because acceleration was projected onto the wrong reference direction at the instant velocity reached exactly zero | Minor | P2.1 implementer, self-caught | fixed | Fixed by projecting onto the previous tick's travel direction; covered by `player_animation_test.gd::test_squashes_when_stopping` | Recorded because it is the second time in two phases that an implementer's own animation or bookkeeping test caught a real defect before any reviewer saw it (the first was P1.2's DPS formula inverting a rate). Writing tests for cosmetic code is easy to skip; both times it paid |
| F03-11 | Second GodotPrompter skill conflict this phase: `component-system` prescribes signals-only communication and forbids direct sibling access, while this project's docs/20 defines a three-way split where **typed commands** are a first-class channel alongside events and queries - already used by `death_state.gd` and `player.gd` before this task | Minor | P2.4 implementer | recorded | docs/20's "Communication, commands" bullet against the skill's rule | Project wins, per CLAUDE.md. Worth noting the pattern: three skill conflicts now, and the skills are consistently right about generic Godot idiom and wrong about this project's own architecture. That is what the rule anticipates - skills advise on idiom and never override the project - but it means a skill's advice is a default to depart from, not a standard to meet |
| F03-12 | `src/core/sim_clock.gd`'s header documents a `reset_for_test()` method **that does not exist anywhere in the codebase** | Minor | P2.4 implementer | open | A search of the whole repository finds the name only in that header comment | Open. A documented API that does not exist is worse than an undocumented one, because a later implementer writes a test against it and only then discovers it is fiction. Either add the method or strike the sentence; the choice belongs with whoever owns SimClock's test surface |
| F03-13 | `DeathState`'s `hurtbox_paths` auto-wiring assumes **one health pool**, which the Tower does not have - it has shield-then-health. The Tower's hurtbox is therefore deliberately left out of `hurtbox_paths`, and the two Logical-Death cleanup calls are replicated **by hand** in the Tower's own code | Minor | P2.4 implementer | open | `scenes/tower.tscn` omits the Tower hurtbox from `hurtbox_paths`; the cleanup duplicates `death_state.gd`'s `logical_death` | Open, and the most fragile thing in this phase. Hand-replicated cleanup is the exact shape of the ghost-hit class P1.5 exists to prevent: the pool restores four flags, Logical Death clears them, and a second copy of that logic drifts from the first the moment either changes. It is correct today, and for the reason the implementer gives. It should become a supported two-pool path in `DeathState` rather than a duplicate |
| F03-14 | The first `await get_tree().physics_frame` inside a gdUnit4 test function does not reliably correspond to a fresh SimClock tick, reproduced with a throwaway diagnostic. Related to F03-04 but distinct - that one is a discovery failure after editing a `class_name` script, this is a timing misalignment inside a running test | Minor | P2.4 implementer | recorded | The implementer's own diagnostic; reported as observed rather than diagnosed | Recorded per the project rule about untested causes. It matters because every timing-sensitive test here - pause clock, shield regeneration, retrigger limits, telegraph lead times - is written against SimClock and stepped with physics frames. A test that believes it advanced one tick and did not will pass or fail for the wrong reason |
| F03-15 | Two cross-task seams P2.4 introduced and named: the EntityRegistry tag `tower_seeker` is a convention this task invented, and **P2.5 must register real Tower Seekers under exactly that tag** or the Tower's targeting rule silently finds nothing; and `data/tower/base_weapon.tres` sits under `data/tower/` rather than `data/weapons/`, which is P2.3's path, because P2.3 had not landed | Minor | P2.4 implementer | open | `src/tower/tower_weapon.gd`; `data/tower/base_weapon.tres`; the weapon reference is cross-checked by a `push_warning()` on mismatch since no weapon-ID registry exists | Open until P2.5 and P2.3 land. Third instance of the F02-13 seam class in two phases - two parallel implementers each making a defensible choice that only breaks where they meet - so it gets checked explicitly at integration. The `push_warning()` cross-check is the right instinct: it makes a mismatch loud rather than silent |
| F03-16 | The Tower projectile's lifetime is **derived** rather than taken from a Register row - range divided by speed, times a 1.1 safety margin - and the implementer named it as a derivation instead of presenting it as a Register value | Minor | P2.4 implementer | recorded | `src/tower/tower_projectile.gd`; `evidence/p24_report.md` | Correct handling. A derived value is not a Provisional Default and does not belong in the Register; what would have been wrong is quietly writing the resulting number as though it were one. Recorded so a later reader knows where it comes from, and so the 1.1 margin is visible as a choice rather than buried |
| F03-17 | `src/tower/tower_projectile.gd` does not implement the fast-projectile sweep. It advances by `global_position += _velocity * delta` and relies on Godot's discrete `area_entered`, while docs/20 requires a `PhysicsDirectSpaceState2D.intersect_ray` sweep from the previous position whenever a projectile's per-tick travel exceeds 12 px - and names the Tower's own projectile as an instance: "even the Tower's slowest, at 900 px/s, covers 15 px per tick" | Major | P2.3 implementer; verified independently by the orchestrator | open | `grep -rn intersect_ray src/` finds it only in `src/combat/player_projectile.gd` and in comments; `tower_projectile.gd` lines 108-113 are the bare position step. The combined radius of a 6 px projectile and a 14 px Seeker hurtbox is 20 px against a 15 px step, so it is unlikely to tunnel on a Seeker today | Open. Raised by a parallel task reading P2.4's code rather than by P2.4's own three acceptance tests, none of which exercise the sweep - the fifth instance in this project of a named test unable to catch a defect class in its own task (F01-15, F02-02, F02-16, F03-03). It is not currently reachable as a bug at prototype radii, which is exactly why it would survive to the slice, where a smaller hurtbox or a faster projectile makes it reachable. The rule docs/20 states is unconditional and the fix is the one already written in `player_projectile.gd` |
| F03-18 | Three P2.6 numbers have no Provisional Values Register row and were escalated rather than invented: the damage-to-full-vignette-intensity normalisation (provisionally 0.15 times Tower max health per second), the off-screen indicator's hit-arc display duration, and the Tower-cue audio itself - no audio asset exists anywhere in the repository, so the cue is a procedurally generated placeholder tone | Minor | P2.6 implementer | open | Each marked `# NO REGISTER ROW - escalated` in `src/ui/threat_feedback.gd`; `evidence/p26_report.md` | Open, owner the author. The Register's "Threat feedback" row specifies the vignette's trigger, its 1 s window and its 0.6 s fade but never what damage amount corresponds to full intensity, so the one number that decides how the effect actually reads on screen is the one the Register does not carry. The arc duration was reused from the vignette's own fade rather than invented fresh, which is the right instinct but is still a choice with no row. The missing audio asset compounds F02-03's five audio under-specifications |
| F03-19 | Two further GodotPrompter skill conflicts, both resolved in the project's favour per CLAUDE.md: `hud-system` prescribes signal-binding over polling, while docs/20's "Communication, queries" sanctions exactly this read-query pattern and two of the four HUD fields have no signal source at all because no owning system exists yet; and `responsive-ui` prescribes `aspect=expand`, while `project.godot` pins `aspect=keep` under docs/20 | Minor | P2.6 implementer | recorded | `evidence/p26_report.md`; docs/20 "Communication, queries"; the pinned `display/window/stretch/aspect` | Fifth and sixth skill conflicts in this phase, after F03-02, F03-08, F03-11 and P2.3's repeat of F03-02's class. The pattern holds: the skills are right about generic Godot idiom and wrong about this project's own architecture and its pinned settings. Copied here by the orchestrator because `LEDGER.md` is outside an implementer's write scope and a finding that exists only in an evidence file is not on the record |
| F03-20 | A real defect P2.6's own work caught before review: the bottom-anchored HUD region carrying the XP bar was pinned off-screen at y=1080, because a raw-anchor `MarginContainer` parented under a plain `Control` never grows to fit content added after the anchor preset is set | Minor | P2.6 implementer, self-caught | fixed | Fixed by rebuilding the HUD root as a `VBoxContainer` with an expand-fill spacer, so the layout comes from genuine Container arrangement rather than anchor and grow arithmetic; covered by `tests/unit/hud_layout_test.gd` | Third time in two phases that an implementer's own test caught a real defect before any reviewer saw it, after F03-10's squash-and-stretch sign error and P1.2's inverted DPS formula. Worth recording for the same reason as F03-10: the defect was invisible to every assertion about the HUD's *contents* and only a test that asserted screen position could see it |
| F03-21 | Measured, not assumed: Kenney's `top-down-shooter` characters - the obvious CC0 fit for a top-down game - **fail this project's silhouette requirement**. Rendered flat black, `zoimbie1_stand` and `robot1_stand` are near-identical ovals, and `survivor1_gun` and `soldier1_gun` differ only by a protruding gun stub. Every character in the pack is the same human-from-above oval | Major | Orchestrator, during CC0 asset acquisition | open | Silhouette contact sheet rendered from the packs' own alpha channels at `scratchpad/sheet_silhouette.png`; the four source files under `top-down-shooter/PNG/` | Open, and it decides which sprites the three enemies use. The master constrains exactly one readability property - that a Tower Seeker, a Player Hunter and an Opportunist stay distinguishable at a glance under load - and D99 chose generated art partly because it satisfies that by construction and ships a `--silhouette` mode so the claim can be checked. A stock human pack cannot satisfy it without reshaping, which removes the reason to use stock art. `pixel-shmup`'s 24 craft are genuinely shape-distinct and would pass, at the cost of a 16-bit style and reading as drones rather than monsters. Recorded before any sprite was wired in, so the choice is made on evidence rather than discovered at the feel check |
| F03-22 | **Damage from melee and contact attacks resolves immediately inside the `Area2D` overlap callback rather than through `SimLoop.enqueue_hit()`.** docs/20 states that an overlap callback must only enqueue a hit record for step 7, which sorts by (target serial, attacker serial) - the ordering that makes same-tick damage deterministic. Three tasks now bypass it: `hitbox.gd` (P1.5), `tower_projectile.gd` (P2.4) and `enemy_controller.gd` (P2.5) | Major | P2.5 implementer, named as a seam; re-classified by the orchestrator | open | `src/core/sim_loop.gd`'s own header states the rule; `_step_07_hit_queue_resolution()` sorts a queue that gameplay code never fills; docs/20 > SimLoop order steps 6 and 7 | Open. The implementer named this correctly and followed the precedent its brief pointed at, so this is not a task defect - it is an architectural drift that three tasks have now repeated, which is what makes it worth a Major rather than a note. The determinism guarantee Phase 02 built the whole spine around is the reason step 7 exists, and a hit queue that nothing enqueues into is a guarantee in name only. Fixing it is one change at the framework level, not three at the entity level |
| F03-23 | Dead code found by a falsification that had **zero effect**: the stuck-rule body-block exemption clause could be deleted without any test noticing, because a body-blocked Seeker is already short-circuited by the separate `in_attack_range` early return before the exemption is ever reached | Minor | P2.5 implementer, self-caught | fixed | The mutation produced no failure; the implementer removed the dead clause, corrected the surrounding documentation, and re-falsified against `_is_in_attack_range`'s real body-block branch, which then failed correctly | Fixed. This is the most valuable single result of the phase's falsification discipline so far: every other mutation proved a test could catch a defect, and this one proved a *clause of the implementation* was doing nothing. A test suite passing against dead code is indistinguishable from a test suite passing against working code, which is exactly why the rule is mutate-the-source rather than vary-the-scenario |
| F03-24 | Four P2.5 numbers have no Provisional Values Register row and were escalated rather than invented: each enemy's Visual-Death duration (set provisionally to 0.5 s, deliberately **not** `death_state.gd`'s 0.6 s fixture default), `entity_cap_weight` (set to 1), the telegraph shape, colour and audio cue ID for all three enemies, and the readability profile fields for all three | Minor | P2.5 implementer | open | Each marked `NO REGISTER ROW - escalated` in its own `.tres` or `.tscn` | Open, owner the author. This is F03-01 coming due exactly as predicted at phase entry, and it was handled the right way round: the fixture value was refused rather than inherited. The Visual-Death duration matters most, because it governs how long a corpse is visible and therefore how readable a swarm is at density |
| F03-25 | The Enemy Definition Contract carries no fields for the numbers docs/09's Intent Behaviour Defaults actually require - attack-slot geometry, leash timers, the Opportunist event-rule thresholds, and the stuck-rule seconds and pixels. P2.5 worked around it with `src/enemy/enemy_ai_tuning.gd` and `.tres`, placed under `src/enemy/` because both `data/enemies/` and `src/data/` were outside its scope | Minor | P2.5 implementer | open | `src/enemy/enemy_ai_tuning.tres`; `docs/20` > Enemy Definition Contract fields | Open. A genuine contract gap rather than an implementation shortcut: the numbers are all Register-cited, they simply have nowhere contractual to live. The workaround keeps them in one authored resource rather than scattering them through script constants, which preserves the one-place rule. A future contract revision should decide whether these belong on the Enemy Definition or on a separate AI tuning contract |
| F03-26 | The Tower is never registered with the `EntityRegistry`, so enemies cannot find it through the same query interface they use for everything else. P2.5 worked around it with `set_tower_reference()` and a `tower_path` export, mirroring `tower_weapon.gd`'s own `origin_path` seam | Minor | P2.5 implementer | open | `src/enemy/enemy_controller.gd`; `src/tower/tower.gd` carries no `register_entity()` call | Open. Every Tower Seeker's entire purpose is to reach the Tower, and it currently finds it by node path rather than by query. It works, and the workaround follows existing precedent, but it means the Tower is invisible to any future system that asks the registry what is in a radius - spawn validation and the Pressure Metric both will |
| F03-27 | **All three enemies were invisible in the assembled prototype, with 353 tests green.** `scenes/entities/*.tscn` carried a body, hurtbox, hitbox, death state and telegraph, but **no sprite node at all**. The generated enemy art existed on disk throughout and was referenced by exactly one file - `tests/unit/player_silhouette_test.gd`, which loads the PNGs directly off disk to compare shapes and therefore passed while nothing in any scene rendered them | Major | Orchestrator, spot-checking the integration task's output | fixed | `grep` for the three sprite names across `src/`, `scenes/`, `data/` and `tests/` returned only the silhouette test; `scenes/entities/tower_seeker.tscn` had no `Sprite2D` from any source, and `enemy_controller.gd` creates none in code | **Fixed**: a `Visuals` + `Sprite2D` pair added to each of the three scenes, matching `scenes/player.tscn`'s convention, at native size with no `z_index` override because the `Entities` container supplies z_index 20 and the Y-sort group. Covered by the new `tests/unit/entity_visual_test.gd`. The integration suite could not have caught this and it is worth stating why: it asserts every texture a scene **references** resolves to a non-null resource, and a texture that is never referenced at all is invisible to that check. "No broken paths" and "something is drawn" are different claims and only the first was being made. Sixth instance of a named test unable to catch its own defect class (F01-15, F02-02, F02-16, F03-03, F03-23) |
| F03-28 | The first version of `entity_visual_test.gd` - written specifically to catch F03-27 - **passed against F03-27's own defect**. `TelegraphVisual` builds a `Sprite2D` child at runtime, so an enemy with no body sprite still satisfied "has a node that draws a texture" | Minor | Orchestrator, self-caught during falsification | fixed | The mutation removing the sprite from `tower_seeker.tscn` left `test_every_entity_scene_renders_at_least_one_texture` green; only the third, art-source-specific test failed | Fixed by skipping telegraph subtrees entirely, with the reason recorded in the helper rather than left as an unexplained exclusion. Re-falsified: the same mutation now fails the first test with the intended message. Recorded because it is the cleanest demonstration in the project so far of why the rule is falsify-before-reporting - the test was written by someone who had just diagnosed the exact defect, against a scene they had just read, and was still wrong. A telegraph draws only during an attack wind-up, so an entity whose only visual is its telegraph is invisible for almost all of its life |
| F03-29 | The prototype could not be launched the normal way. `run/main_scene` still pointed at `scenes/main.tscn`, the bare container scene, so pressing play in the editor or double-clicking the binary produced an **empty screen**; and the debug overlay, built in P1.4 and bound to F1 by author decision D78, was not in the prototype scene at all, so F1 did nothing | Minor | Author, on trying to play it | fixed | Reported directly by the author; confirmed by reading `project.godot` and `scenes/prototype.tscn` | **Fixed**: `run/main_scene` now points at `scenes/prototype.tscn`, and `tests/settings_check.gd`'s `MAIN_SCENE_PATH` constant was updated in the same change so the Phase 00 regression guard still asserts the real entry scene rather than being loosened around it. The Settings check re-run passes - 16 layers, 24 actions, 8 settings, 6 autoloads. The overlay is now instanced in the prototype scene, because a feel verdict backed by FPS, entity counts, both health pools and SimClock is worth more than one backed by an impression. `main.tscn` is unchanged and is still what `main_scene_structure_test.gd` asserts |
| F03-30 | The prototype exits with `4 ObjectDB instances were leaked at exit` and `2 resources still in use at exit`, reproduced on every headless run of 180 frames, both before and after the debug overlay was added, so the overlay is not the cause | Minor | Orchestrator, running the scene headless | open | `Godot_v4.7.1-stable_win64_console.exe --headless --path D:/Gamedev res://scenes/prototype.tscn --quit-after 180` | Open, and recorded rather than diagnosed, per the project rule about untested causes. These are shutdown-order symptoms, not gameplay failures - the 180-frame run is otherwise clean and the 356-case suite reports zero orphans. Candidates worth testing rather than assuming: the shared `AudioDucking` node the integration task deliberately parented outside `Main`, and the pools, which hold instances that outlive the tree. Worth resolving before the swarm test at P1.7, where leaked instances per run would compound |
