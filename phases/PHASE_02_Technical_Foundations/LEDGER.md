# Phase 02 - Ledger

Every finding raised against this phase, carried across review iterations.

Severities: Blocker, Major, Minor. Statuses: open, fixed, closed, deferred with owner, recorded, withdrawn. "Recorded" is used for observations that are neither defects to fix nor claims to close.

Structural rule for this file, carried from Phase 01 where it broke three times: every finding row sits **below** the header, carries exactly seven columns, and holds a status word in the Status column. A column count alone does not catch a row inserted above the header or a row whose Status and Evidence cells are swapped, so the check asserts all three.

| ID | Finding | Severity | Raised by | Status | Evidence | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| F02-01 | This plan's entry conditions contradict its own task table. An entry condition requires the gameplay-root container layout - `Entities`, `Projectiles`, `Pickups`, `Effects`, `Environment`, `Audio` - to exist before the phase starts, but building that scene is **P1.3's own deliverable**, and `scenes/main.tscn` is a bare `Node2D` at phase entry | Minor | Orchestrator, at phase entry | recorded | `scenes/main.tscn` contains a single `Node2D` with no children; the task table lists "gameplay root scene" as P1.3's deliverable | Read as applying from P1.3's completion onward rather than at phase entry, and the entry-conditions bullet is corrected in PLAN.md to say so. Recorded rather than treated as a blocker because the phase can only satisfy it by doing the phase. Worth noting for later phase plans drafted ahead of their dependencies: an entry condition that names a deliverable of the same phase is a drafting error, not a real precondition |
| F02-02 | The named acceptance test for P1.6 - the Audio priority check, 5 of 5 trials - **could not catch the defect it exists to catch**. Its pre-fill voices are all tied at priority 0, so the lowest-versus-highest steal comparison is never exercised; inverting the steal rule left that test green. Only the implementer's own dedicated bookkeeping tests caught it | Minor | P1.6 implementer, self-caught during falsification | fixed | Mutation 1 of the implementer's falsification log: flipping the steal comparison produced exit 100 from `test_general_steal_picks_lowest_priority_voice_first`, not from the named acceptance test | Fixed inside the task by adding the dedicated tests, and recorded rather than quietly absorbed. This is Phase 01's defining failure mode - a check that cannot fail - caught by an implementer applying the carried lesson to its own work rather than by a reviewer. Worth noting that the *named* test in the Acceptance Test Matrix remains the weaker instrument; what protects the behaviour is the suite around it |
| F02-03 | Five under-specifications in docs/20's Audio Mixing & Dynamic Ducking section, named rather than silently resolved: how "a sound plays on SFX_Priority" is to be detected, since `AudioServer` exposes no such query; the attack and release curve shape; how the Music floor at -18 dB combines with the 6 dB duck; whether the general 32-voice steal rule may target an active priority voice; and whether the Provisional Values Register's "six buses" counts TowerCue | Minor | P1.6 implementer | open, needs an owner | `evidence/p16_report.md` section 8 | Each was implemented with a stated choice - an explicit notify API for detection, linear ramps, an absolute clamp on the ducked result, uniform stealing across all 32 voices, and PLAN.md's explicit six-name enumeration for the bus count, which gives seven buses including Master and matches what was built. All five are recorded so the author can overturn any of them; none blocks the phase |
| F02-04 | Observation worth carrying: the ducking node ramps on its own real per-frame `_process(delta)` rather than on SimClock, and the reasoning was stated rather than assumed. SimClock is `PROCESS_MODE_PAUSABLE` and stops advancing exactly when the ducking node most needs to keep ramping - mid-pause with a release still in progress - so using it would be self-defeating; and the master groups the ducking node with the UI set that it exempts from SimClock | Minor | P1.6 implementer | recorded | `src/audio/audio_ducking.gd` header comment and `evidence/p16_report.md` | Correct as reasoned, and the implementer flagged the one case that would change it: if ducking should someday respect `SimClock.time_scale` during slow motion, that needs a recorded decision rather than a code change. Carried here so a later phase does not rediscover the question |
| F02-05 | `.gitignore` excluded the whole of `tools/`, where its own comment says it means the locally built MCP servers. The broad rule swallowed two things that should travel with a clone: `tools/checks/`, where P1.1's banned-API check lives - so a **required exit-criterion artifact could never be committed** - and `tools/.gdignore`, the file that keeps the servers out of an exported build, which docs/28 consequently had to describe as something recreated by hand after every rebuild | Minor | P1.1 implementer, flagged rather than worked around | fixed | `git check-ignore -v` resolved both paths to `.gitignore:32:tools/`; the rule's own comment reads "Locally built, commit-pinned MCP servers" | Narrowed to `tools/mcp/`, which is what the comment always intended. Both paths are now committable and `tools/mcp/` is still ignored, all three verified with `git check-ignore`. docs/28 updated: a rebuild no longer needs `tools/.gdignore` recreated by hand, so the export protection can no longer be lost by forgetting it - `sandbox/` stays fully ignored, so its `.gdignore` is still manual. Export re-verified after the change: 88 KB pack, zero matches for `tools`, `mcp`, `gdUnit4`, `settings_check` or `banned_api`, with the new P1.1 and P1.6 code present |
| F02-06 | Three interpretations the P1.1 implementer made explicit rather than burying: SimLoop is **not** registered as an autoload, because the master names SimClock and PauseAuthority as Autoloads and never uses that word for SimLoop, so it belongs under the gameplay root P1.3 builds; the banned-API check is scoped to source directories excluding `ui/` rather than to "the gameplay root", because no gameplay-root scene exists to walk until P1.3; and hit stop was left out of SimClock, since the master keeps `time_scale`'s rules textually separate from hit stop, which it says explicitly does not touch `time_scale` | Minor | P1.1 implementer | recorded | `evidence/p11_report.md`, contradictions 2, 3 and 4 | All three are sound readings of the master and are recorded so a reviewer can overturn any of them without having to re-derive the reasoning. The SimLoop call in particular is load-bearing for P1.3, which now owns where it lives |
| F02-07 | The P1.1 implementer wrote a temporary fixture to `src/ui/_tmp_ui_fixture.gd`, outside its permitted write scope, while verifying the banned-API check's UI exemption | Minor | P1.1 implementer, self-reported | recorded | The implementer caught it immediately, moved the file out of the repository with `mv` rather than deleting it, confirmed `src/ui/` held only its original `.gitkeep`, and re-verified the exemption using synthetic path strings with no repository file involved | Recorded rather than treated as a breach. The constraint worked as intended: the scope was narrow enough that the implementer noticed the violation itself, and it reported the slip unprompted instead of quietly cleaning up. This is the behaviour the delegation constraints exist to produce, and the orchestrator independently confirmed `src/ui/` clean |
| F02-08 | Observed engine behaviour worth carrying: after a test fails, gdUnit4's CLI runner stops executing the **remaining tests in that same suite file**, while still running every other suite file fully. Reported as observed, with no cause guessed | Minor | P1.1 implementer | recorded | Seen consistently across the falsification runs; no project-level configuration found that would explain it | Recorded rather than diagnosed, which is the right call under this project's rule about untested causes. The consequence matters for falsification work: a single mutation may mask later tests in its own suite, so a green run after restoration is the check that the suite is whole again - and every falsification in this phase does re-run to green |
| F02-09 | **The Registry query check's 0.05 ms bound is met on uniformly distributed entities and not on clustered ones**, and clustering is the case this game actually produces. Measured at 300 entities, radius 480 px: uniform mean 15.4 us (p99 21 us), clustered mean 72.4 us (p99 109 us). The implementer also measured a from-scratch brute-force baseline at roughly 66-69 us on the same clustered data, so the gap is not a grid inefficiency - it is inherent to materialising roughly 300 results through an interpreted loop. The acceptance test as written in the Matrix names no distribution | Major | P1.2 implementer, raised rather than worked around | open, needs the author | Orchestrator re-ran the suite independently: `uniform: mean=15.41us p99=21us` and `clustered: mean=72.41us p99=109us`, n=5000 with 300 warmup iterations each. The clustered test is named `test_clustered_distribution_does_not_meet_the_bound` and asserts the shortfall rather than hiding it | Open. The suite currently asserts the 50 us bound on the uniform case and only a loose 1000 us sanity ceiling plus correctness on the clustered case, which is honest but means the named acceptance test does not cover the realistic worst case. For scale: 72 us is about 0.43% of a 16.67 ms frame at 60 Hz, so the absolute cost is small and the question is really how many such queries a tick will make. **Three options for the author are drafted in `evidence/p12_report.md`.** An orchestrator probe intended to separate search cost from result-array construction errored before producing a figure and is NOT evidence either way |
| F02-10 | The uniform timing assertion is a weak regression detector. The implementer's first falsification - forcing the query to ignore the spatial grid and scan all 300 slots, a genuine O(n) regression - **stayed green at 45.7 us**, just under the 50 us bound. Only escalating to an O(n-squared) busy loop produced exit 100 | Minor | P1.2 implementer, self-reported as a near-miss rather than omitted | open | The mutation log in `evidence/p12_report.md`: 45.70 us mean against a 50 us bound, then 1608 us once escalated | Open, and worth pairing with F02-09 since both concern the same test. A bound the correct implementation clears by 3.2x and a broken one clears by 1.09x is measuring the machine as much as the code. The wrong-set guard is the stronger half of this suite - breaking the radius filter to return everything was caught by an explicit set-membership assertion, not by timing, which is the right structure |
| F02-11 | Three interpretations and one engine pitfall from P1.2, recorded rather than buried: docs/20 names exactly three EventBus signals as examples and the implementer treated that as non-exhaustive, implementing those three only; docs/20's `CombatStats.sheet_dps()` example is zero-argument, which cannot serve the Pressure Metric's need for separate player, Tower and per-enemy figures, so it was implemented as a pure computation plus a per-subject report/query pair; `EntityRegistry.set_entity_alive()` is the implementer's own design for how Logical Death reaches the registry without a per-tick broadcast, and P1.5 owns Logical Death; and in Godot 4.7.1 a strictly-typed `Array[StringName]` parameter rejects an inline array literal at runtime when called through a weakly-typed Node reference, which is exactly this project's own test-file style for autoloads | Minor | P1.2 implementer | recorded | `evidence/p12_report.md`, contradictions 1, 2, 4 and 5 | All four recorded so a reviewer can hold them against docs/20 directly. The `set_entity_alive` interpretation is the one that matters most, because P1.5 will either adopt it or contradict it |
| F02-12 | The P1.2 implementer invoked the required GodotPrompter skills **after** most of the work was done, where CLAUDE.md requires them to be invoked first, and said so plainly instead of reordering the narrative | Minor | P1.2 implementer, self-reported | recorded | The implementer's own report states the ordering and what changed as a result: one idiom retrofitted (typed `Dictionary[K,V]`), two skill-versus-project tensions resolved in the project's favour | Recorded rather than treated as a breach. The rule's purpose - that skill guidance actually reaches the code - was served, since the design was checked against the skills and amended. But the ordering exists so the guidance shapes the work rather than being audited against it afterwards, and a later implementer doing the same on a task with more design latitude would get less value from it |
| F02-14 | **gdUnit4's reported error count does not include Godot engine errors, so "exit 0, 0 errors" never meant the engine did not error.** Measured directly: a probe suite issuing five `push_error` calls reported `1 test case, 0 errors, 0 failures` and exited 0 while all five errors appeared in the output. This is not a gdUnit4 defect - it reports on assertions - but nothing in this project was reading the engine's own channel, so every green run recorded across Phases 01 and 02 was weaker evidence than it appeared | Major | P1.5 implementer, observed during falsification; reproduced independently by the orchestrator | fixed | The probe: `EXIT=0`, `Overall Summary: 1 test cases, 0 errors, 0 failures`, with 5 `ORCHESTRATOR_PROBE` error lines present in the same output | `tests/run_tests.ps1` gains a fourth guard that scans captured output for engine `ERROR`/`SCRIPT ERROR`/`USER ERROR` lines, reports them whatever the exit code, and refuses to call a green suite a pass when any are present. Falsified: with the F02-15 bug reverted, gdUnit4 still reported 172 tests and 0 errors while the runner caught 60 engine error lines and exited **1**; after restoring, exit **0**. The guard is deliberately reported even on a failing run, since an engine error is just as real in a suite that failed for another reason |
| F02-15 | **A real latent bug that every green run in this phase had hidden**: `EntityRegistry.get_entities_in_radius()` appended freed object references into its typed `Array[Node2D]` return, producing `Attempted to push_back an invalid (previously freed?) object instance` - thirty occurrences, sixty error lines, inside a suite reporting 172 of 172 passing. An entity freed without `deregister_entity()` leaves a dangling reference in the slot table, and the query neither skipped nor cleaned it | Major | Orchestrator, found by acting on F02-14 | fixed | The engine backtrace named it exactly: `get_entities_in_radius (res://src/core/entity_registry.gd:248)` reached from `run_recorder.gd` during `test_idleness_does_not_flag_before_the_threshold` | The registry now skips invalid references in both the radius and tag queries and **reaps** the dangling slot afterwards, so the stale entry cannot persist and re-error on every later query. Grid mutation is deferred until after iteration. Freeing without deregistering remains a caller bug, but the registry is queried every tick by Tower targeting, weapon auto-targeting, the magnet and the Pressure Metric, so it self-heals rather than propagating one caller's mistake into a query result. Engine error lines in a full run: **60 to 0**, with 172 of 172 still passing |
| F02-16 | **The Ghost hit test cannot catch the defects it exists for.** The P1.5 implementer ran all three requested mutations and reported plainly that all three left it green: removing the hurtbox's engine-level disable; disabling the hitbox directly instead of deferred; and skipping one of the four flags `Pool.acquire()` restores. The third is the sharpest - the named acceptance test cannot prove that the pool-restored engine flag is ever correctly cleared, because a synchronous `_window_active` guard alone satisfies every scenario the test constructs | Major | P1.5 implementer, self-reported rather than resolved by widening the test | open, needs an owner | The falsification log in `evidence/p15_report.md`, three mutations, all green. Mutation 2 additionally produced ten real Godot errors (`Can't change this state while flushing queries`) that gdUnit4's statistics did not surface - the same blind spot as F02-14, found independently in the same task | Open. The implementation is protected by direct unit tests, which did catch all three; what is not protected is the **named** acceptance test in the Acceptance Test Matrix, which a later phase will cite as evidence. This is the third named acceptance test in two phases found unable to catch its own defect class (F01-15, F02-02, now this), which is a pattern about how these tests are specified rather than three coincidences. Deferred under the author's prototype-first direction; the implementation itself is sound |
| F02-17 | The P1.5 implementer ruled on the `set_entity_alive()` seam that F02-11 left open: **use it.** Logical Death calls `EntityRegistry.set_entity_alive(entity, false)`, leaving the entity registered but excluded from live counts, with deregistration happening at despawn as P1.3's spawner already does | Minor | P1.5 implementer | recorded | Verified by a direct test that the entity remains registered while dropping out of live counts | The seam is now agreed across all three tasks that touch it - P1.2 proposed it, P1.3 stayed clear of it, P1.5 owns and uses it. Recorded so the agreement is findable rather than having to be re-derived from three separate evidence reports |
| F02-18 | Two scope boundaries P1.5 named rather than silently crossing: SimLoop's `enqueue_hit()` pipeline at steps 5-8 is **not** wired, because it needs attacker and target serials no system yet assigns, so hitbox and hurtbox resolve damage directly for now; and the placeholder enemy's HP, damage and Visual-Death duration have no Provisional Values Register rows, so they are treated as test-fixture constants rather than gameplay content | Minor | P1.5 implementer | recorded | `evidence/p15_report.md`, contradictions 1 and 2 | Both are correct calls for a framework task and both are real debts for P2.x, which builds the actual combat system and the actual enemies. The fixture constants in particular must not be allowed to become de facto gameplay values by being copied forward - whoever authors a real enemy takes its numbers from the Register |
