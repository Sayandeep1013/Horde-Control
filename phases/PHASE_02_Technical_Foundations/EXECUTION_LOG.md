# Phase 02 - Execution Log

Dated record of every action, command, file change, test run, and result for this phase.

| Date | Task | Action | Command or file | Result |
| --- | --- | --- | --- | --- |
| 2026-09-18 | Entry | Loop rule (a): read phases/LESSONS.md and the full records of Phases 00 and 01 before any implementation, then filled this phase's placeholder "Carried lessons" section | `PLAN.md` | Eight patterns written in, each naming the task it changes, plus two engine-specific carries from Phase 00. Phase 01 produced 56 findings across three review iterations; the eight selected are the ones bearing on an engine-spine phase |
| 2026-09-18 | Entry | Phase-entry verification, per the sequence Phase 01 established | `git -C tools/mcp/... rev-parse HEAD`; `addons/gdUnit4/plugin.cfg`; tree hash | Both MCP pins match their recorded shas; `tools/.gdignore` and `sandbox/.gdignore` both present; gdUnit4 still 6.2.1 with tree hash `5ae43377...8b9a` unchanged |
| 2026-09-18 | Entry | Phase 01 regression re-run, since Phase 02 builds directly on its deliverables | `settings_check.gd`, `schema_check.gd`, gdUnit4 pass suite | Settings check exit 0, Schema check exit 0, harness exit 0. All three Phase 01 acceptance artifacts still hold at Phase 02 entry |
| 2026-09-18 | Entry | Found a contradiction between this plan's entry conditions and its own task table | `scenes/main.tscn` | An entry condition required the gameplay-root containers to exist before the phase starts, but that scene is P1.3's deliverable and the file is a bare `Node2D`. Recorded as LEDGER F02-01 and the bullet corrected; not treated as a blocker |
| 2026-09-18 | Entry | Author consulted on three items before any implementation, so that no decision lands mid-flight against an already-written delegation scope (Phase 01 carried lesson 7) | - | Three decisions taken and recorded as D94, D95 and D96 in MASTER_SDLC.md > Review Decision Log; master raised to 0.8.6 with a Change Log row |
| 2026-09-18 | Entry | Applied decision D94, which narrows D83's review pace for this phase and records the narrowing in advance as D83 itself requires | `PLAN.md` > Agent assignment | Seven tasks group into four critical agents by coupled subsystem - P1.1 with P1.2, P1.3 with P1.5, P1.4 alone, P1.6 alone - plus the phase reviewer, who also covers P1.7 as the phase's load test. Five reviewers per iteration rather than eight |
| 2026-09-18 | P1.1 | Delegated the simulation clock, pause authority, tick loop, keyed RNG and banned-API check to a Sonnet implementer, carrying the no-delete, no-export, no-MCP constraints explicitly | - | Delivered. The brief flagged the Settings check coupling: `tests/settings_check.gd` asserts an exact autoload set, so registering new autoloads breaks Phase 00's regression guard unless the check is updated **and re-falsified** |
| 2026-09-18 | P1.6 | Delegated the audio bus layout and 32-voice AudioPool to a Sonnet implementer, scoped away from `src/core/` and `project.godot` so it cannot collide with P1.1 | - | Delivered. Brief carries D95 (docs/20 binding, document 26 not to be read) and requires the implementer to state and justify which clock drives the ducking ramps rather than choosing one silently |
| 2026-09-18 | P1.6 | Audio layout and pool built | `default_bus_layout.tres`; `src/audio/audio_pool.gd`, `audio_ducking.gd`, `cue_retrigger_limiter.gd`, `tower_cue_player.gd`; `tests/unit/audio/` (5 suites, 44 tests) | Seven buses including Master, with `TowerCue` sending to `SFX_Priority` and carrying the graph's only `AudioEffectPanner` - verified against the live `AudioServer` after boot rather than by reading the `.tres` alone. The layout sits at the project root because Godot's `audio/buses/default_bus_layout` setting defaults to `res://default_bus_layout.tres`, so it loads at boot with no `project.godot` edit, which the concurrent P1.1 implementer owns |
| 2026-09-18 | P1.6 | Audio priority check (Acceptance Test Matrix > Technical Tests), re-run by the orchestrator rather than taken from the report | `--headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit/audio --ignoreHeadlessMode` | Exit 0, 44 of 44 tests across 5 suites |
| 2026-09-18 | P1.6 | Falsification. The implementer broke the pool twice; the orchestrator then broke the **ducking node**, which the implementer had not falsified at all | `src/audio/audio_ducking.gd` | Removing the Music -18 dB floor clamp: exit 100. Reducing the duck depth from 9 dB to 3 dB: exit 100. Both restored and confirmed identical by `diff`, final run exit 0. The implementer's own two mutations - inverting the steal comparison, and raising the priority cap from 8 to 9999 - also produced exit 100 each |
| 2026-09-18 | P1.6 | A defect in the orchestrator's own falsification command, caught and corrected | - | The first attempt chained the mutation through `grep -c`, which exits non-zero when it counts zero matches, so the `&&` chain silently skipped the test run entirely and reported nothing for that mutation. Re-run without the chain. Worth recording because it is the same shape as the findings this phase is guarding against: a verification step that quietly did not happen |
| 2026-09-18 | P1.6 | Three findings recorded from this task | `LEDGER.md` | F02-02, the named acceptance test being unable to catch the defect it exists for, self-caught by the implementer; F02-03, five under-specifications in docs/20's audio section, each implemented with a stated choice and routed to the author; F02-04, the ducking node's clock decision and the one case that would change it |
| 2026-09-18 | P1.1 | Simulation spine built | `src/core/sim_clock.gd`, `pause_authority.gd`, `sim_loop.gd`, `keyed_rng.gd`; `tools/checks/banned_api_check.sh`; `project.godot` (autoload lines only) | SimClock is `PROCESS_MODE_PAUSABLE` with a clamped `time_scale`; PauseAuthority is `PROCESS_MODE_ALWAYS` and the sole writer of `get_tree().paused`, with `push_reason`/`pop_reason` queuing and `flush()` applying at end of tick; SimLoop implements the fifteen-step order as named extension points, with the hit-queue sort and the flush step filled and the rest traced to the later P-tasks. Keyed RNG uses a hand-rolled FNV-1a rather than Godot's `hash()`, so the sequence does not depend on an engine implementation detail |
| 2026-09-18 | P1.1 | Settings check widened for the two new autoloads and re-falsified, since it is Phase 00's regression guard and a version of it that could not fail was Phase 00's worst defect | `tests/settings_check.gd` | Expected set widened to BootCheck, SimClock, PauseAuthority, and a new per-autoload script-path manifest added. Falsified both ways by the implementer: a rogue extra autoload gives exit 1 naming it, a removed expected autoload gives exit 1 naming it. Orchestrator re-ran the check: PASS at 3 autoloads |
| 2026-09-18 | P1.1 | Both named acceptance tests re-run and independently falsified by the orchestrator on assertions the implementer had not targeted | `--headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode` | Baseline 57 of 57, exit 0. **End-of-tick pause rule**: making `push_reason` flush immediately - breaking the determinism guarantee that requests apply at tick end, never mid-resolution - gives exit 100 with four failures. Restored identical, final exit 0. The implementer's own two mutations (SimClock set to `PROCESS_MODE_ALWAYS`, the exact leak the Risk Register names; and keyed RNG reseeding per call) each gave exit 100 |
| 2026-09-18 | P1.1 | **Falsified the banned-API check itself**, which is a P1.1 exit criterion and which the implementer's report did not record falsifying | `tools/checks/banned_api_check.sh` | Planting `get_tree().create_timer()` in `src/core/sim_loop.gd` gives exit 1, naming the file and line. Planting the identical call in `src/ui/` leaves exit 0, so the master's pure-UI exemption is real rather than asserted. Both probes removed with `mv`, check back to exit 0, `src/ui/` confirmed clean |
| 2026-09-18 | P1.1 | Fixed the `.gitignore` contradiction the implementer flagged, then re-verified export packaging because the change touches the mechanism behind Phase 00's F-05 | `.gitignore`, `docs/28_AI_Development_Workflow.md` | Rule narrowed from `tools/` to `tools/mcp/`. Export re-run: exit 0, 88 KB pack, zero matches for `tools`, `mcp`, `gdUnit4`, `settings_check`, `banned_api` or `PLACEHOLDER`, with `sim_clock`, `pause_authority`, `audio_pool` and `default_bus_layout` all present. Recorded as F02-05 |
| 2026-09-18 | P1.2 | Delegated the EventBus and query interfaces to a Sonnet implementer. P1.2 is the only task unblocked by P1.1's completion - P1.4 needs P1.2's EventBus as well, so it cannot start in parallel | - | Delivered |
| 2026-09-18 | P1.2 | Communication spine built | `src/core/event_bus.gd`, `entity_registry.gd`, `combat_stats.gd`; `tests/unit/` (4 suites); `project.godot` (autoload lines only) | EventBus is `PROCESS_MODE_ALWAYS` with three typed signals stamped from `SimClock.now`; EntityRegistry and CombatStats are `PROCESS_MODE_PAUSABLE`. The registry uses structure-of-arrays storage plus an incrementally maintained spatial hash grid, with pull-only queries and no per-tick broadcast anywhere, which is the rule docs/20 states explicitly |
| 2026-09-18 | P1.2 | Settings check widened again, to six autoloads, and re-falsified both ways | `tests/settings_check.gd` | Rogue extra autoload gives exit 1 naming it; a removed expected autoload gives exit 1 naming it. Orchestrator confirmed PASS at 6 autoloads |
| 2026-09-18 | P1.2 | Registry query check re-run independently by the orchestrator rather than taken from the report | `--headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/unit --ignoreHeadlessMode` | Exit 0, 90 of 90 tests. Timing reproduced exactly as reported: `uniform: mean=15.41us worst=103us p99=21us` against the 50 us bound, and `clustered: mean=72.41us worst=340us p99=109us`, which does not meet it. Recorded as F02-09 and routed to the author |
| 2026-09-18 | P1.2 | A real bug the task's own tests caught before review | `src/core/combat_stats.gd` | `sheet_dps_from_weapon()` divided by the fire rate instead of multiplying, confusing a rate with `AttackProfile`'s interval semantics - 10 damage at 2 shots per second computed as 5.0 DPS rather than 20.0. Caught, fixed and documented by the implementer |
| 2026-09-18 | P1.2 | An orchestrator probe intended to separate search cost from result-array construction, to test whether a count-only or callback query would clear the bound on clustered data | scratchpad probe script | **Errored at line 14 and produced no measurement.** Recorded because the option it was meant to evidence is one of the three offered to the author under F02-09, and that option is therefore currently unevidenced rather than supported. It must not be cited as if it had run |
| 2026-09-18 | Pause | Session paused at the author's request with P1.1, P1.2 and P1.6 delivered and committed, and P1.3, P1.4, P1.5 and P1.7 not started | - | No implementer or reviewer was running at the pause point; the only background process was the orchestrator's own probe, which was stopped and its Godot process killed. Working tree committed clean. Resume state written into NEXT_SESSION.md |
