# Phase 04 - Ledger

Every finding raised against this phase, carried across review iterations until closed.

Severities: Blocker, Major, Minor.
Statuses: open; fixed; deferred (with owner); withdrawn.

| ID | Finding | Severity | Raised by | Status | Evidence | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| F04-01 | Two Pressure formula constants have **no schema field to hold them** - the threat DPS reference (10) and the Pressure time window (20 s) - so they are cited script constants on `PressureMetric` rather than authored data. Both are Register values and both are cited; the gap is that the Pressure Metric Constants schema cannot carry them | Minor | P2.9 implementer | open | `src/director/pressure_metric.gd`; `src/data/pressure_metric_constants.gd` | Open for the author. Same family as F05-04 and F03-25: three contracts now under-specify what implementation needs. Either the schema grows the two fields or the constants stay in code by a recorded decision |
| F04-02 | **"Teaching wave" has no field in the Wave Definition Contract**, and the Pressure Metric must know which waves are teaching waves because it is only evaluated while a combat wave is open. P2.9 worked around it with a `teaching_wave_unique_ids` export listing T1-T4 by id, because `src/data/**` was outside its write scope | Minor | P2.9 implementer | open | `src/director/wave_director.gd` export; `src/data/wave_definition.gd` | Open, and named as a required schema seam rather than resolved silently. A list of ids duplicates knowledge the wave resources already carry; a `is_teaching_wave` field would put it in one place |
| F04-03 | The post-draft grace period is a seam, not a behaviour: `notify_draft_closed()` exists on the Wave Director and nothing calls it, because the Level-Up Draft (P2.12) does not exist yet. Until it does, Pressure is not suppressed after a draft closes | Minor | P2.9 implementer | open | `src/director/wave_director.gd` | Open, owner: P2.12, and carried into its brief. The Encounter deferral test is the named test that would catch it being left unwired |
| F04-04 | The debug overlay is not wired to the Pressure Metric: `set_debug_overlay_reference()` exists and nothing calls it in the assembled scene, so Pressure, its escalation state and the health quadrant do not appear on screen yet. The overlay's own `set_pressure()` and `set_health_quadrant()` seams were sufficient and were not edited | Minor | P2.9 implementer | open | `src/director/wave_director.gd`; `src/debug/overlay.gd` | Open, owner: the integration task. Exactly the shape of F03-27 and F05-07 - correct components, absent wiring - so the integration pass asserts against the assembled scene |
| F04-05 | **Overtime is not implemented** (P2.8 deferred it), so P2.9's "never de-escalates in Overtime" rule could not be exercised end to end through a real Wave Director run. It is tested directly against `PressureMetric` with a hand-set flag instead, which its `RefCounted` design allows. The Siege-escalation-suppression rule has the same limitation | Major | P2.9 implementer | open | `tests/unit/pressure_test.gd`; the P2.9 report | Open. Two Register rules are asserted only against a unit-level flag, never against the state the game actually produces. Overtime is part of P2.8's own Wave Runtime Model scope and its Overtime test is a named Phase 04 exit test, so this is a gap in the phase, not only in P2.9 |
| F04-06 | Edge case resolved by interpretation and named: Capacity at or below zero with Threat above zero returns a Pressure of 0 rather than dividing by zero. The Register's formula does not say what happens when both the player and the Tower are dead or have no weapon | Minor | P2.9 implementer | open | `src/director/pressure_metric.gd` | Open for the author. Returning 0 makes a dead-everything state read as no pressure, which is harmless for the prototype because the run is over; recorded so it is not mistaken for a measured value |
| F04-07 | Five debug-output `.txt` files were left in the repository root by the P2.9 implementer instead of in the scratchpad directory, and correctly left in place rather than deleted under the no-delete constraint | Minor | P2.9 implementer | fixed | `git status` before the move | Fixed by the orchestrator: moved to the session scratchpad, repository root clean. The constraint worked as intended - the implementer flagged rather than deleted |
| F04-08 | **The magnet motion constants have no contract field anywhere.** 40 px/s initial, 900 px/s^2 acceleration and 700 px/s maximum are real Register values, but neither the Pickup Definition Contract nor the Economy Configuration Contract has a field to hold them, so they stay as cited script constants. The implementer named this as a schema gap distinct from an escalation, and it is why one falsification had to mutate a script constant rather than a `.tres` | Minor | P2.10 implementer | open | `src/pickup/pickup.gd`; `src/data/pickup_definition.gd` | Open for the author. This is the **fourth** contract found to under-specify what implementation needs (F03-25 enemy AI defaults, F04-01 Pressure constants, F05-04 upgrade effect routing, and now pickup motion). Four instances is a pattern about the contracts, not four separate oversights |
| F04-09 | Four P2.10 numbers have no Register row and were escalated rather than invented: the pickup's own collision radius (6 px), the blink rate inside the 5-second expiry window (0.2 s), the search-pattern shape used to place a drop that has nowhere reachable to land, and the collect audio cue ids (`PLACEHOLDER_` prefixed, following the convention `data/enemies/*.tres` already uses) | Minor | P2.10 implementer | open | `data/pickups/xp_shard.tres`, `scrap.tres`; `src/pickup/pickup.gd` | Open for the author, alongside F03-18, F03-24 and F03-36. The escalation habit is holding across six tasks now |
| F04-10 | `SimLoop`'s `_step_09`, `_step_10` and `_step_11` stubs do not call the generic per-step dispatch the way step 13 does, so `PickupSystem`'s three registered step adapters are registered and never called | Major | P2.10 implementer | open | `src/core/sim_loop.gd`; `src/pickup/pickup_system_step_adapter.gd` | Open, owner: the SimLoop debt task, which owns that file today and was still running when P2.10 finished. Verified by the orchestrator before the settled test pass rather than taken on report |
| F04-11 | An enemy despawned by the stuck rule emits `removed_while_stuck(position, drop_table)` and nothing connects it, so a stuck-despawned enemy's drops - which the Register explicitly requires to be placed - do not appear. The handler exists and is tested against the real signal shape; only the live connection is missing, and enemy spawning lives in a file P2.10 did not own | Minor | P2.10 implementer | open | `src/enemy/enemy_controller.gd`; `src/pickup/pickup_system.gd` | Open, owner: the integration task |
| F04-12 | `PickupSystem`'s `entity_spawner_path`, `player_path` and `player_collector_path` are unassigned in the real scene, and the HUD is not driven by the run inventory: `RunInventory.apply_to_hud_state()` exists and nothing calls it | Minor | P2.10 implementer | open | `src/pickup/pickup_system.gd`; `src/economy/run_inventory.gd` | Open, owner: the integration task. This is now the **fourth** finding of the same shape in two phases - correct components, absent wiring (F03-27, F04-04, F05-07, F04-12) - so the integration pass is a single owned task that asserts against the assembled scene a human launches, not against components |
| F04-13 | Interpretation named rather than assumed: an Overtime finisher's Scrap suppression reads `EnemyController.is_finisher` and hard-zeroes Scrap, instead of resolving the `OvertimeCondition.finisher_drop_override` the wave data authors, which is unreachable from an `enemy_died` signal. Moot when written, because Overtime was deferred - **but P2.8b is building Overtime now**, so it stops being moot inside the same session | Minor | P2.10 implementer | open | `src/pickup/pickup_system.gd` | Open, and deliberately re-raised: the orchestrator verifies this specific path once P2.8b's finishers exist, rather than letting a correct-when-written interpretation quietly go stale |
| F04-14 | A genuine test-design bug the implementer found and fixed in its own work: an early merge test asserted that a Node reference disappeared from tracking, which is unsound, because `EntitySpawner`/`Pool` can hand the just-freed instance straight back for the very next spawn in the same cap cascade. Rewritten to assert on data - position and value - rather than on object identity | Minor | P2.10 implementer | fixed | `tests/unit/pickup_merge_cap_test.gd` | Fixed by the implementer before reporting. Carried to `phases/LESSONS.md`: in a pooled system, object identity is not a stable test subject |
| F04-15 | **A pre-existing suite now fails because the rule it predates was finally built.** `wave_sequence_test.gd`'s dummy enemies never die, so its combat waves genuinely have zero kills - which the real stall check, built by P2.8b, correctly reads as stalled, so the sequence never reaches `wave_combat_2`. The implementer named it as a caused, understood consequence and refused the two bad fixes: weakening the stall check into a rule that cannot fire, or editing a suite outside its write scope | Major | P2.8b implementer | fixed | `tests/unit/wave_sequence_test.gd`; `evidence/p28b_report.md` | Fixed by the orchestrator, at the fixture rather than the rule: the suite's dummy enemies now die as a real wave's enemies do, so the sequence it asserts is a sequence the stall check agrees is progressing. Weakening the stall check would have produced exactly the defect this project has recorded five times - a rule that cannot fail |
| F04-16 | **A numeric inconsistency inside the master itself**, found by implementing the Siege volume formula rather than transcribing its results: the Register's formula, applied at multiplier 1.5, yields 6 accompanying Hunters where the same row states "+7 Hunters", and the Encounter Budgets row authors 7. Unreachable today, because Siege scaling is gated to activate only strictly above base Tower DPS | Minor | P2.8b implementer | open | MASTER_SDLC.md > Provisional Values Register > Encounter Budgets > "Siege volume formula" and "Combat wave 2 Siege x1.5" | **Open, and the author's** - the Provisional Values Register wins on any numeric conflict, and here it conflicts with itself. Either the Hunter count is authored data that the formula does not derive, or the formula's rounding is stated. Not resolved by an agent |
| F04-17 | Interpretation named rather than escalated: an Overtime finisher's health is 25% of a Hunter's 30 HP = 7.5, which rounds to 8 because `BandedValue.value` is int-typed and the Register states the percentage without a rounding direction | Minor | P2.8b implementer | open | `data/encounters/player_hunter_finisher.tres` | Open for the author, at the same weight as the other rounding interpretations in this phase |
| F04-18 | Two real defects P2.8b found and fixed in existing code while building the encounter queue, both no-ops for the current eight-wave sequence and both real: `start_offset_seconds` is "from encounter open" per the schema and was being measured from wave open (fixed with a separate encounter-open anchor, leaving the wave-open anchor for max duration); and a final wave reaching zero live enemies could end the whole wave before a queued lower-priority encounter had opened | Minor | P2.8b implementer | fixed | `src/director/wave_director.gd` | Fixed. Both are invisible in the prototype because every prototype wave carries exactly one encounter, and both would have surfaced the moment a wave carried two |
| F04-19 | An engine error caught while writing tests rather than in the game: connecting to `EventBus.enemy_died` through dot-property access fails; the string-based `connect()` form is required. Worth carrying because every later system that listens to an EventBus signal will write this line | Minor | P2.8b implementer | recorded | `src/director/wave_director.gd` `_on_enemy_died()` | Recorded |
| F04-20 | **Off-screen spawn markers were not built, and the reason is a gap in the owning document rather than a choice**: docs/11 specifies a marker's timing, validation and aggregation but never what it renders or where it appears. The implementer quoted the document rather than inventing a visual | Minor | P2.8b implementer | open | docs/11_Wave_Director.md > "Spawn Rings & Placement" | Open for the author. Consistent with P2.8's own earlier reading that ring placement plus camera exclusion already guarantee off-screen arrival structurally; what is missing is the telegraph, and nothing says what it looks like |
