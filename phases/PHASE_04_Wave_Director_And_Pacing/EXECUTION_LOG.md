# Phase 04 - Execution Log

Purpose: a dated record of every action, command, file change, test run, and result for this phase.

| Date | Task | Action | Command or file | Result |
| --- | --- | --- | --- | --- |
| 2026-09-20 | Entry | Phase entered out of the plan's sequence, continuing the author's prototype-first direction (D97, D100) | `git status` clean at `b82aec1`; `phases/PHASE_03_Core_Entities_And_Feel_Check/EXECUTION_LOG.md` | **P2.8, this phase's largest task, was already built inside Phase 03** and is recorded there (F03-34), because the author's first feel-check verdict named the missing wave pacing. What remains here is P2.9 and P2.10 |
| 2026-09-20 | Entry | Filled this plan's "Carried lessons" section from the four phases that have now run, per loop rule (a), before delegating anything | `PLAN.md` | The placeholder written when phases 00-04 had not run is replaced by what they actually recorded |
| 2026-09-20 | P2.9, P2.10 | Delegated the Pressure Metric and the pickup/economy layer to two Sonnet implementers in parallel, scoped to disjoint directories - `src/director/**` and `src/pickup/**` + `data/pickups/**` + `data/economy/**` | - | In progress. Each brief carries the no-delete, no-export, sandbox-only constraints explicitly (F-06), requires every number to come from the Provisional Values Register with the row cited and anything missing escalated rather than invented (F03-01, F03-24), requires `physics_step(delta)` plus `driven_externally` with no self-registration against SimLoop (that file has a single owner this session), and requires each named acceptance test to be falsified by real mutation of the source or the `.tres`, restored, and proved byte-identical |

No entries yet.
| 2026-09-20 | P2.9 | Pressure Metric delivered | `src/director/pressure_metric.gd` (a pure `RefCounted`, no scene tree); `src/director/wave_director.gd` (+406/-13); `tests/unit/pressure_test.gd` (30 cases) | Threat, Capacity and Pressure as static math, with the escalation and de-escalation lockouts as instance state, following the precedent that keeps the spawn geometry testable without a running game. Every constant is read from the already-authored `data/encounters/director_configuration.tres`, which until now nothing consumed. The implementer verified that with nothing ever escalating or de-escalating the spawn-timing formula reduces byte-for-byte to its pre-P2.9 form, and the whole pre-existing suite still passed, which is the property that keeps this from silently re-timing P2.8's waves |
| 2026-09-20 | P2.9 | Named acceptance test falsified four ways, each on a real file, each restored and proved byte-identical | `tests/unit/pressure_test.gd` | Seeker intent weight 1.25 to 1.0; the 4-second minimum between escalations to 0; the 6-second re-arm lockout to 0; and the Siege clause removed from de-escalation's gate. All four gave exit 100. None produced a false green |
| 2026-09-20 | P2.9 | Capacity seam recorded rather than assumed | `WaveDirector.set_player_capacity_provider()`, `set_tower_capacity_provider()` | A provider Callable falling back to `CombatStats.get_sheet_dps()` and then to the base weapon `.tres`. The middle step is already live, because the weapons report their sheet DPS on every configure - so the seam may need no wiring at all, which the integration pass verifies rather than assumes. No PauseAuthority or Console branch anywhere, by design: the Register says the player's term is not zeroed while the Console is open, and a dedicated test asserts it |
| 2026-09-20 | P2.9 | Seven findings recorded | `LEDGER.md` F04-01 to F04-07 | Two schema gaps (the Pressure constants and the missing teaching-wave field), three unwired seams the integration pass owns (grace period, debug overlay, capacity), one interpreted edge case, and five stray debug files moved out of the repository root. **F04-05 is a Major**: Overtime does not exist yet, so two Register rules are asserted only against a hand-set flag rather than against state the game produces |
| 2026-09-20 | P2.8b | Delegated the remainder of P2.8's own Wave Runtime Model, which P2.8 deferred and named | - | Three of this phase's named exit tests - Wave runtime, Overtime, Encounter recovery - sit inside what P2.8 deferred, so the phase cannot reach them without this work. Scope: the stall check and Overtime with its finishers (subject to the same entity cap as any other spawn, which is this phase's own predetermined risk), encounter priority and deferred recovery gaps, the encounter-level alive cap of 120 that is authored and unenforced, the Siege volume formula computed from live Tower DPS now that upgrades exist, and the Split Assault and Hunt suites that were never built as their own acceptance tests. Off-screen spawn markers only if docs/11 actually specifies what a marker renders - otherwise left, with the document's own sentence quoted as the reason |
| 2026-09-20 | P2.8b | Held the implementer to the regression standard P2.9 set | - | With no stall and no Overtime, wave timing must be unchanged, and the existing P2.8 and P2.9 suites are the guard. P2.9 proved its own change reduced byte-for-byte to the earlier formula; the same bar applies here rather than a green summary |
| 2026-09-20 | P2.10 | Pickups, the Drop Table and the run economy delivered | `src/pickup/pickup.gd`, `pickup_system.gd`, `pickup_system_step_adapter.gd`; `src/economy/run_inventory.gd`; `data/pickups/xp_shard.tres`, `scrap.tres`; `data/economy/prototype.tres`; `scenes/pickups/pickup.tscn`; four suites, 32 cases | Magnet, acceleration, the per-tick blocking raycast, lifetime and blink, the C-MERGE cascade at the 150 pickup cap, the Drop Table applied through the existing `enemy_died` signal, and a run inventory carrying the Scrap cap, the FULL state, overflow discard and the XP curve. `src/player/collector.gd` was not edited - the `pickup_entered` seam P2.1 built for this was sufficient, which is the first time a seam reserved for a future task has been used as intended rather than worked around |
| 2026-09-20 | P2.10 | Named acceptance test falsified four ways on real source, each restored and `diff`-confirmed byte-identical | `tests/unit/pickup_physics_test.gd`, `pickup_merge_cap_test.gd`, `run_inventory_test.gd` | Removing the blocked-raycast early return made a blocked pickup slide (red); changing acceleration 900 to 1 gave the exact predicted wrong speed (red); making the cap cascade a no-op drifted bookkeeping from the pool's real count (red); making the player-death handler a no-op kept 120 Scrap through death (red). No mutation left a test green, so no dead-test finding |
| 2026-09-20 | P2.10 | The cross-task prediction in the brief held, and was verified rather than assumed | - | The brief told the implementer to write its Scrap-loss listener against `EventBus.player_died` even though the signal did not exist when it started, and to guard the connection. The signal landed mid-task from the debt implementer and the guarded connection bound to it with no code change. This is the seam-reservation rule working: the orchestrator named the signature in advance and two parallel tasks met at it |
| 2026-09-20 | P2.10 | Seven findings recorded, one of them a Major | `LEDGER.md` F04-08 to F04-14 | **F04-10 is a Major**: the pickup system registers three step adapters against SimLoop and steps 9, 10 and 11 do not dispatch them, so they are registered and never called. Also a fourth contract found to under-specify implementation (F04-08), a fourth 'correct components, absent wiring' finding (F04-12), and F04-13, an interpretation that was moot when written and stops being moot the moment P2.8b's Overtime finishers exist |
| 2026-09-20 | Orchestrator | Spot-checked the authored economy and pickup data against the Register rather than trusting the report | `data/economy/prototype.tres`; `data/pickups/xp_shard.tres`; Register > Economy & Pickups, Technical Caps & Performance | Console price 30 per rank, dominance threshold 0.65 over 10 runs, hopper 10:1 on entering the Interaction Radius, pickup cap 150, merge radius 64 px, C-MERGE's trigger and behaviour text - all match their Register rows. The XP shard's merge rule is authored identically on both currencies because docs/20 names those sub-fields as fixed text rather than a per-pickup choice, which the file says in a comment rather than leaving a reader to wonder |
| 2026-09-20 | P2.8b | The Wave Runtime Model's deferred remainder delivered | `src/director/wave_director.gd`; `data/encounters/player_hunter_finisher.tres`; all eight wave `.tres` repointed at it; seven new suites, 19 cases | The stall check and Overtime with finisher bursts on the view ring, **spawned through the same encounter cap and global cap throttle as an ordinary spawn** - which is this phase's own predetermined risk, closed by construction rather than by assertion; the final wave's repeated re-check per C-FINAL; encounter priority ordering with deferred recovery gaps, tested against ten scripted double-schedules because the real sequence never schedules two at once; the authored encounter alive cap now enforced; and the Siege volume formula computed from live Tower DPS, gated so a no-upgrade build stays byte-identical to the authored counts. P2.9's `is_overtime` flag, hardcoded false when it was written, is now driven by the real Overtime state |
| 2026-09-20 | P2.8b | The two acceptance suites P2.8 named but never built now exist | `tests/unit/split_assault_test.gd`, `hunt_test.gd` | Both are named Phase 04 exit tests in the master's Acceptance Test Matrix and both had been counted as covered by a determinism suite that exercised them incidentally |
| 2026-09-20 | P2.8b | Four falsifications, all red, all restored byte-identical by md5 | `wave_runtime_test.gd`, `overtime_test.gd`, `encounter_recovery_test.gd`, `split_assault_test.gd` | Kill-rate check removed; finisher cap throttle removed; recovery-gap deferral removed (20 failures); Split Assault lane separation removed (42 failures) |
| 2026-09-20 | P2.8b | Six findings recorded, two of them the author's | `LEDGER.md` F04-15 to F04-20 | **F04-16 is a numeric inconsistency inside the master itself** - the Siege volume formula yields six accompanying Hunters where the same Register row states seven - found only because the formula was implemented rather than its results transcribed. **F04-20**: off-screen spawn markers stay unbuilt because docs/11 never says what a marker renders. Plus two real defects fixed in existing code, both invisible until a wave carries two encounters |
| 2026-09-20 | Orchestrator | Fixed F04-15 **at the fixture rather than at the rule** | `tests/unit/wave_sequence_test.gd` | The suite predates the stall check: its dummy enemies never died, so its combat waves genuinely had zero kills and the new rule correctly read them as stalled. A wave in which nothing ever dies **is** stalled, so the suite was wrong, not the rule. The fixture now kills each spawned dummy two seconds after it spawns - despawning it through the real spawner so the director's registry query sees it gone, and emitting the real `enemy_died` so the trailing kill window is non-empty - and waves complete naturally instead of running to their maximum duration. The reasoning is written into the suite's own header, including what was rejected: weakening the stall check to make an old fixture pass would have produced a rule that cannot fire, which is this project's most-repeated defect |
| 2026-09-20 | Orchestrator | Verified the repair did not defeat the rule it was accommodating | `overtime_test.gd`, `wave_runtime_test.gd`, `encounter_recovery_test.gd`, `encounter_alive_cap_test.gd`, `siege_volume_formula_test.gd`, `split_assault_test.gd`, `hunt_test.gd`, `pressure_test.gd`, `spawn_ring_test.gd`, `wave_determinism_test.gd`, `wave_sequence_test.gd` | All eleven Wave Director suites at exit 0 - 62 cases. The stall check still fires where it should (overtime_test), which is the property that would have been quietly destroyed by the easy fix |
