# UI Pass - Ledger

Every finding raised against the UI pass, carried across review iterations until closed. The full text of each reviewer finding is in `REVIEW.md`; this table is the tracking copy.

Severities: Blocker, Major, Minor, Nit.
Statuses: open; fixed; deferred (with owner); withdrawn.
"Raised by": R1 is the iteration 1 blind reviewer; "orchestrator" rows were found from the frames after that review and are not the reviewer's.

| ID | Finding | Severity | Raised by | Status | Evidence | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| UR-01 | OpenType ligatures were on and Pixelify Sans's `fi` ligature reads as a capital A: the Rapid Fire card said "+20% player Are rate per rank" in both resolutions | Major | R1 | fixed | `screenshots/after_1920/02_draft.png` (third card); `data/upgrades/rapid_fire.tres` | `UiTheme._make_font()` sets `liga`, `clig`, `dlig`, `hlig` to 0 on the `FontVariation`. Read on the re-captured card at 1920, 1280 and pseudo: "+20% player fire rate per rank". The font was later replaced (UR-15); ligatures stay off for any font |
| UR-02 | Threat feedback, one of the seven named surfaces, had no screenshot in any set, although its `_draw()` was rewritten; the claims said "six states each" and "all seven surfaces" without saying a surface went unphotographed | Major | R1 | fixed | `screenshots/*/` directory listings; `package_reports/A_hud.md` | `ui_capture.gd` gained three staged states - `07_threat_vignette`, `08_threat_offscreen`, `09_threat_low_health` - driven through the Tower Hurtbox's own `damage_received` signal. All five sets now hold nine frames. What the frames showed is UR-16 |
| UR-03 | `●`, `■` and `▲` are not in the shipped font (package D measured this with `Font.has_char()`), yet were used for the Console row glyphs, the Draft card glyphs and the Draft hold ring; they rendered only through the OS fallback font. The Console also used a circle for the player pool where docs/19 asks for the Draft's glyph rules | Major | R1 | fixed | `src/ui/theme/ui_strings.gd` (`CONSOLE_GLYPH_*`), `src/ui/draft_card_view.gd`, `src/ui/draft_controller.gd`; `package_reports/D_menus.md` | New `src/ui/shape_glyph.gd` (`UiShapeGlyph`): the pool glyph is a drawn triangle (player) or square (Tower) on Draft cards, Console rows, the Draft hold ring and the three menu rings. It stays a `Label` and keeps its `text` so the `get_glyph_label()` test seam is untouched. The Console's circle became the Draft's triangle, as docs/19 asks. `CONSOLE_GLYPH_*` keys removed. The two Draft glyph constants the seam reads remain as text and are never rendered |
| UR-04 | `LEDGER.md`, a named deliverable, did not exist | Major | R1 | fixed | `phases/UI_PASS/` listing at `2f1c8ad` | This file |
| UR-05 | The Console compaction moved the price into a 20 px tag and the MAX badge to 16 px; the Register names price and MAX as entry content and sets a 24 px text floor. The brief's rule was to stop and ask the author, and nobody asked | Minor | R1 | fixed | `src/ui/console.gd` `_make_pill_label()`; `MASTER_SDLC.md` Register > Interfaces > Tower Console | Author decision 2026-09-20: the floor covers the row label and the price / MAX badge. Price tag and MAX badge now scale with `ENTRY_FONT_SIZE_PX` each frame like the row label. Review Decision Log row proposed in HANDOFF H-06 |
| UR-06 | Deliverable 1's mechanism was not met: `add_theme_*_override` calls went from 25 to 42, 33 of them `separation` constants that a container type variation carries. Every one does read a palette token | Minor | R1 | fixed | `grep -c add_theme_ src/ui/*.gd` at `3ef4bc7` vs `2f1c8ad` | `UiTheme.hbox(step)` / `vbox(step)` container variations added; every `separation` override in `src/ui` converted or deleted. `add_theme_*_override(` calls: 25 at `3ef4bc7`, 42 at review, 22 now, none of them a separation. The 22 are per-node colours, runtime-shaped styleboxes, the Register-cited Console font size and one margin set |
| UR-07 | No `before_1280` set exists, and every "1920" frame is 1875x1055 while the claims say 1920x1080 | Minor | R1 | fixed | `screenshots/` listing; FAILURE_POINTS UP-05 | `ui_capture.gd --size=WxH` makes the window borderless at exactly that client size. All five sets re-taken; every frame checked by script: 1920x1080 or 1280x720 exactly. `before_1280` added; both before sets are from an exported copy of `1321f52` and now include the threat frames |
| UR-08 | `UiStrings.ensure_registered()` runs only as a side effect of `UiTheme.get_theme()`; `src/run/run_flow_controller.gd` calls `tr()` for the run-end cause and never touches the theme, so it resolves only because a menu fetched the theme earlier | Minor | R1 | fixed in `src/ui`; deferred (owner of `src/run`) for the rest | `src/ui/theme/ui_theme.gd`; `src/run/run_flow_controller.gd:487-489` | Every `src/ui` script that calls `tr()` now calls `UiStrings.ensure_registered()` first in its build function (6 of 6, by grep). The `run_flow_controller.gd` call site is HANDOFF H-07; H-02 removes the need altogether |
| UR-09 | The headline "591 tests, 2 failing" came from a run that was in flight (08:22:37-08:30:53 UTC) while `console.gd` was still being written (08:26:14 UTC), so the four Console suites in it ran against pre-final code. The separate 22-case re-run was disclosed; the headline was not qualified | Major | R1 | fixed | per-suite timestamps in `evidence/after_results_run1.xml` | Round 2's full suite was started at 12:08:45 UTC, after the last code edit; a `find -newermt` over `src`, `tests`, `assets/ui` and `scenes/ui` afterwards lists no file changed since. Through `tests/run_tests.ps1`: 598 test cases, 0 errors, 2 failures - `test_trivial_fail` and the T4 aggregate, the baseline's two by name. 5 engine error lines, all resource or RID leaks at exit; the baseline was not run through the script, so whether they predate the pass is not established. `evidence/after_results_round2.xml`, `evidence/after_run_round2_console.txt` |
| UR-10 | The Console footer reserves two lines unconditionally, so with Repair highlighted - the default on open - the panel ends in about 50 px of blank space | Minor | R1 | fixed | `screenshots/after_1920/05_console.png` | Repair now gets a one-line footer, "Restores %d HP for %d Scrap" (`CONSOLE_REPAIR_FOOTER`), built from the values the row already shows. Conditional reservation was rejected because the panel would resize as the highlight moves. The wording is new and is listed for the author with the rest of `ui_strings.gd` (H-02) |
| UR-11 | `ui_console_layout_test.gd`'s first height ceiling (900 px) could not fail on the 773 px panel it was written to guard | Minor | R1 | fixed | `package_reports/C_console.md` | `ui_console_layout_test.gd` now asserts the requirement - the author's 35% allowance, 378 px at 1080p - as its own case, separate from the tight regression bound |
| UR-12 | `choice_highlight_row.gd` maps the underline's left edge through the transforms but uses the unscaled width, so the underline is wider than its option while the card's open tween is below scale 1 | Nit | R1 | fixed | `src/ui/choice_highlight_row.gd:85-86` | Both corners of the option's rect go through one transform. Package D found the old code was exact for today's single shared ancestor, built an asymmetric-scale case, saw it fail against the old code (180 vs 225 px), then fixed it |
| UR-13 | The hold-confirm ring in the three paused menus is an unlabelled empty circle and reads as a stray dot | Nit | R1 | fixed | `screenshots/after_1920/03_pause_menu.png`, `04`, `06` | The menu rings carry the same dim centre triangle as the Draft's ring (`DraftFillRing.center_shape`, additive). No new string |
| UR-14 | Run end: "Time survived: 0:03" wraps to two lines beside a one-line "Scrap held", so the two cells do not align | Nit | R1 | fixed | `screenshots/after_1920/06_run_end.png` | `STAT_CELL_MIN_WIDTH` 220 -> 320 after measuring the worst-case string; both cells are one line at 1920, 1280 and pseudo |
| UR-15 | Pixelify Sans's "5" is close to its "8" and "S" at HUD and Console sizes: 150 Scrap reads near "180" in a downscaled frame | Minor | orchestrator | fixed | `screenshots/after_1920/05_console.png`, HUD Scrap field | Author decision 2026-09-20: Jersey 10 (SIL OFL 1.1) replaces Pixelify Sans; a comparison sheet was shown to the author. Font-size tokens raised about a quarter because Jersey sits small in the em; the Register-cited Console 24 px is untouched. Jersey 10 lacks 29 Latin Extended-A characters (Esperanto and a few others); French, German, Spanish, Polish, Czech and Turkish are covered, and the pseudo-localized set shows no missing-glyph box |
| UR-16 | Threat feedback, once photographed, does not read as a vignette: a flat, nearly opaque salmon band about 300 px thick set well inside the screen, hard black outlines round each lit wedge, faceted arcs, drawn over the XP pill and hiding enemies under it | Major | orchestrator | fixed | first `07`-`09` captures, 2026-09-20 | `_draw_vignette_segments()` redrawn: the outer edge is the screen border, it fades inward over 18% of the shorter side with per-vertex alpha to a 0.4 peak, neighbours blend, and the vignette has no outline pass. `SEGMENT_COUNT`, intensities, bleed, window, fade and every getter a test reads are unchanged. The inward band itself predates the pass (`before_1920/07_threat_vignette.png`); round 1 added the outlines |
| UR-17 | Three package reports describe code that no longer exists (`hud_strings.gd`, `draft_strings.gd`, the marker-per-option highlight), and `BRIEF.md` was edited from `reports/` to `package_reports/` with no note | Minor | R1 | fixed | `package_reports/A_hud.md`, `B_draft.md`, `D_menus.md`; `BRIEF.md:9` | `> Superseded:` notes added in `A_hud.md`, `B_draft.md`, `D_menus.md`; `BRIEF.md` carries a dated note on the `reports/` rename |
| UR-18 | "27/27 test cases pass ... all green" in `C_console.md`, and "pass on the final code" in the orchestrator's own claims, where the brief allows counts only | Minor | R1 | fixed | `package_reports/C_console.md:74,123`; the pause-point summary | `C_console.md` corrected to bare counts. The orchestrator's pause-point sentence is in a chat transcript, not a file; the rule is applied to this round's records |
| UR-19 | HANDOFF H-03 cites `before_1920/01_hud_gameplay.png` as showing the debug overlay over the HUD; the committed frame was taken with the overlay hidden and does not show it | Minor | R1 | fixed | `HANDOFF.md` H-03; `src/ui/dev/ui_capture.gd` | H-03 now says no committed frame shows the overlay and cites `src/debug/overlay.gd` (`_visible_overlay: bool = true`) instead |
| UR-20 | The suite evidence is against `3ef4bc7`. `main` has since changed two tests and added `prototype_integration_test.gd`, which instantiates the assembled scene; no run covers the merged tree | Minor | R1 | deferred (whoever merges) | `git diff 3ef4bc7..main --stat -- tests` | HANDOFF H-09 |
| UR-21 | The capture tool reaches into five private members of three classes with no test guarding them, and ships inside `src/ui/` with no export exclusion. No shipped code depends on it | Minor | R1 | fixed in scope; deferred (owner of `export_presets.cfg`) for the export filter | `src/ui/dev/ui_capture.gd` | New `tests/unit/ui_capture_seams_test.gd` asserts every node path and member the tool uses. Its first run failed on a real defect - the tool read `PauseAuthority._reasons`, which does not exist, so its log always printed `<null>` - now `get_active_reasons()`. Export exclusion: HANDOFF H-08 |
| UR-22 | 24 reference screenshots sat inside `res://` and were imported as game textures, each with a committed `.import` | Minor | R1 | fixed | `screenshots/**/*.png.import` | `screenshots/.gdignore` added; the 24 `.import` sidecars removed |
| UR-23 | `ui_theme.gd` carried bare literals (`line_spacing 2`, slider padding `5`) | Nit | R1 | fixed | `src/ui/theme/ui_theme.gd` | `UiPalette.LINE_SPACING`, `UiPalette.SLIDER_PAD` |
| UR-24 | The Draft's highlighted-card border gap narrowed from 3/7 px to 2/3 px; a 1.05 lift compensates, but the static cue is weaker than before | Nit | R1 | fixed | `src/ui/draft_card_view.gd`; `screenshots/after_1920/02_draft.png` | Highlighted card: 5 px border against 2 px, 1.09 lift, and a drop shadow only the highlighted card has |
| UR-25 | The Console panel is taller than docs/19's default 30%-of-screen-height cap (324 px at 1080p): 377 px at review time | Minor | orchestrator (RESUME item 3), confirmed R1 | fixed | `tests/unit/ui_console_layout_test.gd` output | Author decision 2026-09-20: the Console may use about 35% of screen height. docs/19 text and Review Decision Log row proposed in HANDOFF H-05 |
| UR-26 | The Console row label's composition changed (name and rank in the row, price only in the tag, effect sentence in a footer). It is the pass's one departure from "no wording change" and was an orchestrator decision (UP-08), not the author's | Minor | orchestrator (RESUME item 3) | fixed | FAILURE_POINTS UP-08; `src/ui/console.gd` | Author decision 2026-09-20: keep the compact rows. Recorded in HANDOFF H-06 |
| UR-27 | HUD: each value sits about 100 px from its label ("Level      0", "Wave      1/8"), so values read as detached | Nit | orchestrator | fixed | `screenshots/after_1920/01_hud_gameplay.png` | `WaveRow` no longer stretches across its pill, and captions right-align against their values. New `tests/unit/ui_hud_render_test.gd` asserts one line per HUD label and a pill height bound in a real tree; falsified first by zeroing the label minimum width (7 of 7 labels failed the way round 1's HUD did) |
