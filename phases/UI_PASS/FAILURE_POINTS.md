# UI Pass - FAILURE POINTS

## Predetermined

| ID | Risk | Mitigation |
| --- | --- | --- |
| UP-P1 | A restyle silently changes behaviour a test reads (node names, label text, style getters, sizes) | Implementers read the covering tests first; existing tests are never edited; full suite compared against the baseline |
| UP-P2 | Parallel implementers collide in one worktree | Disjoint file ownership per package; palette and theme are read-only to implementers; one suite at a time, never the whole folder |
| UP-P3 | Cosmetic motion re-times input, or leaves a closed surface visible | Motion animates visual-only properties; hide is immediate; tweens are bare `create_tween()` guarded by `is_inside_tree()` |
| UP-P4 | The subagent ask-gate does not intercept subagents (Phase 00 F-06) | Every delegation carries the worktree-only, no-delete, no-export, no `run_project`/`game_*` constraints in `BRIEF.md` |
| UP-P5 | A new font lacks the accented glyphs pseudo-localization renders | Pixelify Sans chosen for Latin Extended coverage; a pseudo-localized screenshot set is part of verification |
| UP-P6 | Interfering with agents working in `D:\Gamedev` | Separate worktree and branch; strict write allow-list; out-of-scope needs go to `HANDOFF.md` |

## Discovered during execution

| ID | What happened | Cause | Fix | Prevention |
| --- | --- | --- | --- | --- |
| UP-01 | With the player parked inside the Interaction Radius, standing still, holding 150 Scrap, the Console never opened in the assembled scene | `scenes/prototype.tscn` authors the Console's four NodePaths relative to the scene root; the Console resolves them relative to itself, so all four are null and `physics_step()` returns at its first guard. Every Console unit test injects the references, so none can see it | Out of scope: HANDOFF H-01, with the exact four-line fix and a proposed assembled-scene assertion. The capture tool injects the references, and says so on stdout | A screenshot of the launched scene is evidence a unit suite cannot give. This is the second defect of this class found that way (F05-30 was the first) |
| UP-02 | The launched HUD read `HUD_WAVE 1/8`, `HUD_LEVEL 0`; the run-end screen read `RUN_END_CAUSE: RUN_END_CAUSE_TOWER` | 33 `tr()` keys, no translation file tracked, none registered in `project.godot` | In scope: `ui_strings.gd` registers English strings at runtime. Durable fix: HANDOFF H-02 | Tests compare against `tr()` on both sides, so a missing translation is invisible to them; only looking at the screen shows it |
| UP-03 | The open Console rendered as a column about 450 px wide and the full screen height, its text wrapped one character per line | To be confirmed by package C: autowrap + expand-fill labels with no real minimum width, so the row settles at near-zero text width | Passed to package C with the screenshot | Same as UP-01 |
| UP-04 | The F1 debug overlay opens by default and covers the HUD's top-left Player Health field | Overlay default visibility and placement | Out of scope: HANDOFF H-03. The capture tool hides it | - |
| UP-06 | The paused menus' shape cue for the highlighted option was an accent underline spanning the whole option row; it never moved, so it carried no information | One marker per option in an HBoxContainer, toggled by `visible`. A hidden child takes no space in a box container, so the one visible marker expanded across the row | `choice_highlight_row.gd` now draws the underline from the highlighted option's real rectangle. New `tests/unit/ui_choice_highlight_test.gd` asserts the drawn geometry: option-wide, and moves with the highlight (1 case, 0 failures). The old version was shown wrong by screenshot, not by this test, which could not run against it | A cue that is only ever looked at in one state looks right. Capture a second state, or assert the geometry |
| UP-07 | The first HUD restyle left every package-A test green (73 of 73) and the launched HUD broken: bars about 160 px tall, header and caption labels wrapped one character per line, the Scrap value cut to "0/20" | Autowrap + expand-fill labels with no minimum width settle at near-zero width inside an HBox; the vertical text inflated each pill and the bars stretched to fill it. Same class as UP-03 | Returned to the implementer with the screenshot and permission to run the capture tool; bars now carry slim minimum sizes and every short label a real minimum width | The implementers were first forbidden from running anything windowed, so none could look at its own work. Three of four packages needed a second pass for defects a single screenshot shows. Give a UI implementer the capture tool from the start |
| UP-08 | The restyled Console laid out correctly but measured about 672x773 px: clipped off the top of the screen and sitting under the HUD | Each row label carried the full `effect_description` sentence plus rank plus price, three to four lines per row at the 24 px text floor. docs/19 asks for a compact panel listing price and rank, and caps a container at 30% of screen height | Orchestrator decision, recorded as an interpretation: the row label's composition may change (no test asserts its content). One-line rows of name and rank, price only in the tag, the highlighted entry's effect sentence in a footer. In progress with package C | - |
| UP-05 | A 1920x1080 window comes back as a 1875x1055 client area on the capture machine | The desktop clamps a decorated window to the work area | Recorded; the 1280x720 set is unclamped. Not yet resolved for an exact 1920x1080 frame | - |
