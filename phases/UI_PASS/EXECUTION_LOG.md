# UI Pass - EXECUTION LOG

All entries 2026-09-20. Paths are relative to the `ui-pass` worktree, `D:\Gamedev-ui-pass`.

| # | Action | Result |
| --- | --- | --- |
| 1 | Read the reference screenshot `G:\screenshots\Screenshot 2026-09-20 125749.png` and the current UI | Reference: near-zero chrome, in-world state, muted ground against saturated threats, one outlined font, a radial countdown ring. Project: no shared Theme; about 31 per-widget overrides across 8 files in `src/ui/`; every surface built in code |
| 2 | `git worktree add -b ui-pass D:\Gamedev-ui-pass HEAD` from `main` at `3ef4bc7` | Worktree placed beside the repository, not inside it, so it never appears in another agent's `git status` and Godot never scans it as part of the main project |
| 3 | Headless `--import`, then the full gdUnit4 suite, on the untouched worktree | 588 tests, 2 failing: `test_trivial_fail` (the harness's deliberate failure) and `teaching_siege_tuning_test > test_z_aggregate_both_halves_of_the_t4_tuning_target` (T4 is recorded as untuned in `phases/README.md`). Kept as `evidence/baseline_results.xml` |
| 4 | Foundation: `src/ui/theme/ui_palette.gd`, `src/ui/theme/ui_theme.gd`, `assets/ui/fonts/PixelifySans-Variable.ttf` + `OFL.txt` | Compile-checked headless: theme builds, 6 panel and 5 label variations present, the font resolves (not the engine fallback). Committed as `1321f52` |
| 5 | Four Sonnet implementers launched in parallel on disjoint files, each bound by `BRIEF.md` | Packages A (HUD, threat feedback), B (Draft), C (Console), D (menus, run end) |
| 6 | Capture tool `src/ui/dev/ui_capture.gd` written; "before" set taken from a throwaway detached checkout of `1321f52` | Six states at a 1875x1055 client area (a 1920x1080 window is clamped by the desktop). Three findings came out of the first run - see FAILURE_POINTS UP-01 to UP-03 |
| 7 | `src/ui/theme/ui_strings.gd` added and hooked into `UiTheme.get_theme()` | Answers UP-02 inside the write scope; durable fix requested as HANDOFF H-02 |
| 8 | UP-03's evidence sent to the package C implementer while it was still working | - |
| 9 | Packages A, B, D reported; interim capture of the launched scene | Draft and run-end sound. HUD broken on screen with 73 of 73 tests green (UP-07); returned to A with the screenshot and leave to run the capture tool. D returned for duplicated captions. Per-package string helpers folded into `ui_strings.gd` and removed |
| 10 | Package C reported a 672x773 px panel | Returned with a compaction design and one lifted constraint (UP-08); came back at 512x377 px |
| 11 | Menu highlight underline found spanning the whole row (UP-06) | Fixed by the orchestrator in `choice_highlight_row.gd`; `ui_choice_highlight_test.gd` added, 1 case, 0 failures |
| 12 | Full suite on the restyled worktree; banned-API check; Console suites and highlight test re-run on the final code | 591 tests, 2 failing, the same 2 as the baseline (`evidence/after_results_run1.xml`). 0 banned calls. 22 cases, 0 failures |
| 13 | Final captures at 1920x1080, 1280x720 and pseudo-localized; allow-list check; `reports/` renamed `package_reports/` (the repository's unanchored `reports/` ignore rule was hiding it) | Nothing outside the allow-list changed; no existing test edited. Committed as `d6bbae6` |
| 14 | `main` found at `c076fc2`; its `66e623e` applies H-01's exact fix | Read-only `git merge-tree ui-pass main`: no conflicts. HANDOFF H-01 updated. Throwaway "before" checkout removed |
| 15 | Blind Opus review launched, then stopped unfinished at the author's request | No review result exists. `RESUME.md` lists what remains |
| 16 | Resumed in a new session. Blind Opus review (`critical-reviewer`) re-run against `2f1c8ad` with only the author's request, the pause-point claims and artifact paths | Verdict "accept with fixes", 5/10, no blocker, 5 Major. Reproduced verbatim in `REVIEW.md`; tracked in `LEDGER.md` as UR-01 to UR-27 (five of those rows are the orchestrator's own, found from frames after the review) |
| 17 | Foundation fixes by the orchestrator: ligatures off in `UiTheme._make_font()`; `UiTheme.hbox()/vbox()` separation variations; `src/ui/shape_glyph.gd`; the two bare literals in `ui_theme.gd` moved to the palette | Rapid Fire card read on a native-resolution crop: "+20% player fire rate per rank" |
| 18 | Capture tool: `--size=WxH` (borderless, exact client size), three threat-feedback states, master bus muted, wall-clock wait before the Draft hover | Exact 1920x1080 frames. First threat frames showed UR-16. The mute followed the author's complaint about sound from concurrent capture runs |
| 19 | Author asked four multiple-choice questions | Font: Jersey 10. Console 24 px floor: row label plus price / MAX. Console height: about 35% of screen height. Console rows: keep the compact composition. Proposed docs/19 text and Review Decision Log rows are in HANDOFF H-05 and H-06 |
| 20 | Four Sonnet implementers in parallel on the round 1 file split, bound by `BRIEF.md` plus `BRIEF_R2.md`, each with the capture tool from the start and its own scratch frame folder | Reports appended as `## Round 2` in `package_reports/`. Per-package suite counts are in those reports; none reported a failure |
| 21 | `tests/unit/ui_capture_seams_test.gd` written by the orchestrator | First run: 2 cases, 1 failure - the tool read `PauseAuthority._reasons`, which does not exist. Tool fixed to `get_active_reasons()`; then 2 cases, 0 failures |
| 22 | "Before" sets re-taken from a `git archive` export of `1321f52` in a scratch directory (no worktree, no git state change), with the current capture tool copied in | `before_1920` at exact size with threat frames; `before_1280` new |
| 23 | Font swapped to Jersey 10 after all four packages returned; Pixelify Sans and its licence removed; font-size tokens raised about a quarter; `CONSOLE_REPAIR_FOOTER` added and `CONSOLE_GLYPH_*` removed in `ui_strings.gd` | Glyph coverage measured with `FontFile.has_char()` over ASCII, Latin-1 letters and Latin Extended-A: 29 of 287 missing in Jersey 10 (2 in Pixelify Sans). See UP-10 |
| 24 | Full suite through `tests/run_tests.ps1`, started only after the last code edit | 598 test cases, 0 errors, 3 failures: the baseline's two, plus `ui_console_layout_test` - panel 396 px against the 378 px allowance after the font change (UP-11). 5 engine error lines, all resource or RID leaks at exit; the baseline was not run through the script, so whether they predate the pass is not established |
| 25 | Console footer reservation reduced from two lines to one | Panel 520x370 px at base resolution, 583x458 px pseudo-localized; `ui_console_layout_test`: 4 cases, 0 failures |
| 26 | Full suite again through `tests/run_tests.ps1`, started 12:08:45 UTC after the last code edit; the three after sets re-taken alongside it (captures write PNGs only) | 598 test cases, 0 errors, 2 failures: `test_trivial_fail` and `teaching_siege_tuning_test > test_z_aggregate_both_halves_of_the_t4_tuning_target`, the baseline's two by name. 5 engine error lines, leaks at exit. No file under `src`, `tests`, `assets/ui` or `scenes/ui` is newer than the start time. `evidence/after_results_round2.xml`. Allow-list diff against `3ef4bc7` outside the permitted paths: empty |
| 27 | Superseded-wording sweep after the font change: six comment blocks in `src/ui` and one in `ui_console_layout_test.gd` named Pixelify Sans's file as the font their `Font.has_char()` checks ran against | Re-measured against Jersey 10 first: U+25B2, U+25A0, U+25CF and the nine dingbats are all absent there too, so the comments' claims hold for the shipped font. These edits came after run 26 and touch comment lines only (`git diff -U0`, filtered for non-comment lines: none); headless import afterwards: 0 script errors. Run 28 covers the committed tree |

