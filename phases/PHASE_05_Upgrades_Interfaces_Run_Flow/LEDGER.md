# Phase 05 - Ledger

Every finding raised against this phase, carried across review iterations until closed.

Severities: Blocker, Major, Minor.
Statuses: open; fixed; deferred (with owner); withdrawn.

| ID | Finding | Severity | Raised by | Status | Evidence | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| F05-01 | P2.11 resolved five contract and wording ambiguities by interpretation rather than by escalation, and named each one rather than assuming it silently. The fallback cards' Console price is read as a flat 90 Scrap rather than 30 x rank, because C-FALLBACK-CONSOLE states 90 while the general Console price row states 30 x rank and the fallback has no rank | Minor | P2.11 implementer | open | `data/upgrades/overdrive_fallback.tres`, `reinforce_fallback.tres`; `phases/PHASE_05_Upgrades_Interfaces_Run_Flow/evidence/p211_report.md` | Open for the author. The two Register rows can both be satisfied only by reading the fallback price as an exception to the formula, which is what was built |
| F05-02 | Shield Matrix's +10%-of-max-health-per-rank capacity is applied as already filled: current shield is topped up by exactly the delta when a rank lands. The Register states the capacity, not whether the new capacity arrives full or empty | Minor | P2.11 implementer | open | `src/tower/tower_health.gd` `set_bonus_max_shield_fraction()`; the P2.11 report | Open for the author. Arriving empty would make the upgrade do nothing until the shield regenerates, which reads as a bug at the moment of purchase; arriving full is the reading that was built |
| F05-03 | The Reinforce fallback card is authored with `draft_weight = 1` unconditionally, per docs/20's wording, even though the card is only reachable once its pool is exhausted | Minor | P2.11 implementer | open | `data/upgrades/reinforce_fallback.tres`; docs/20 Contract Field Semantics | Open. A weight that only matters when the card is offerable is harmless; recorded so a later reviewer does not read it as an authoring mistake |
| F05-04 | **The Upgrade Definition Contract has no field naming what an upgrade modifies**, so `upgrade_system.gd` routes effects by matching `unique_id`. Renaming an upgrade's id silently disconnects its effect, and nothing in the schema or a test would say so | Minor | P2.11 implementer | open | `src/upgrade/upgrade_system.gd`; `src/data/upgrade_definition.gd` | Open. Same family as F03-25, where the Enemy Definition Contract carried no fields for the numbers docs/09 required. Two contracts now under-specify what implementation needs; the author owns whether the contracts grow a field or the coupling is accepted and asserted by a test |
| F05-05 | `Player.heal()` was added as the minimal seam Patch Kit needs, because `src/combat/death_state.gd` was owned by another task this session. The implementer's own reading is that a heal command belongs beside `apply_damage()` in the damage framework rather than on the player | Minor | P2.11 implementer | open | `src/player/player.gd` (git diff: 25 insertions, 0 deletions); the P2.11 report | Open. Deliberate scope discipline, not an oversight - recorded so the seam is moved on purpose rather than discovered later |
| F05-06 | `evolution_stage_contribution` is authored correctly on the Tower upgrades, but nothing sums it or pushes it to `TowerEvolutionStage`, so Tower evolution does not respond to upgrades | Minor | P2.11 implementer | open | `data/upgrades/caliber.tres` and siblings; `src/tower/tower_evolution_stage.gd` | Open, owner: the integration task in this phase. The data is right and the wiring is absent |
| F05-07 | `UpgradeSystem`'s four NodePath wiring exports are unassigned; nothing in the assembled scene connects the system to the weapon, Tower weapon, Tower health and player it modifies | Minor | P2.11 implementer | open | `src/upgrade/upgrade_system.gd` exports | Open, owner: the integration task. This is exactly the shape of F03-27, where correct components produced nothing visible because the assembled scene did not connect them - so the integration pass asserts on the assembled scene, not on the component |
| F05-08 | `apply_rank()` carries no cost concept: it applies a rank and nothing else. P2.13 (Tower Console) must charge Scrap itself before calling it, or ranks are free at the Console | Minor | P2.11 implementer | open | `src/upgrade/upgrade_system.gd` public API | Open, owner: P2.13. Carried into P2.13's brief rather than left in a report |
| F05-09 | Testing gotcha worth carrying: this gdUnit4 invocation stops a suite at its **first failing test**, so a falsification run's partial test count (2/12, 8/12, 1/12) says where the first failure landed, not how many assertions the mutation broke | Minor | P2.11 implementer | recorded | Three falsification runs in the P2.11 report | Recorded, not a defect. A partial count read as a coverage number would be misread |
| F05-10 | GodotPrompter skill conflict, recorded per CLAUDE.md: `ability-system` prescribes an Ability shape with cost, cooldown and cast, which does not fit passive stat-modifier upgrades. The skill's data/behaviour separation was followed; its field shape was not | Minor | P2.11 implementer | recorded | `src/upgrade/upgrade_system.gd`; the P2.11 report | Recorded in the project's favour per CLAUDE.md. The implementer judged a shape mismatch not to need a ledger row; it is written here anyway, because CLAUDE.md asks for the conflict to be recorded and the phase ledger is where conflicts have been kept since Phase 03 |
| F05-11 | **A real structural contradiction, raised rather than resolved unilaterally: `SimClock` is `PROCESS_MODE_PAUSABLE`, so it freezes for exactly as long as the Draft holds the pause - which makes this project's own rule that all timing runs on SimClock unsatisfiable for the Draft's own 0.4 s lockout, 0.3 s cycle repeat and 1.0 s hold-to-confirm.** A paused menu's timers cannot run on a clock its own pause stops. P2.12 resolved it locally with a `PROCESS_MODE_ALWAYS` accumulator, using no `get_tree()` timer or tween, and named the choice in the source header | Major | P2.12 implementer | fixed | `src/ui/draft_controller.gd` header; `src/core/sim_clock.gd`'s process mode; MASTER_SDLC.md D104; docs/20 | **Closed by the author, 2026-09-20, as decision D104**: the carve-out, written into docs/20 beside the SimLoop order bullet. A paused menu may accumulate its own presentation timing in a `PROCESS_MODE_ALWAYS` node; `get_tree()` timers and tweens stay banned; everything the simulation can observe stays on SimClock; the Tower Console, which never pauses, is explicitly outside it. The alternatives the author declined are named in D104. P2.12's implementation already matches the carve-out, so no code changed. Structurally identical to the standing `AudioDucking` contradiction (a node needing `PROCESS_MODE_ALWAYS` under a gameplay root docs/20 forbids it in), and it will recur in P2.14 for the pause menu, the settings menu and the run-end screens. Either the rule gains an explicit carve-out for paused-menu UI timing, or paused menus need a second clock that the pause does not stop |
| F05-12 | `RunInventory` has **no teaching-wave awareness at all**, so the C-XPCAP rule could not be enforced at the point XP is credited. P2.12 enforces it by catching an already-committed erroneous level-up each tick and reverting it, refunding the XP the credit wrongly spent before re-clamping. The implementer found and fixed a bug in its own first version of this while writing the test, not through falsification | Minor | P2.12 implementer | open | `src/ui/draft_controller.gd`; `src/economy/run_inventory.gd` | Open. The cap is enforced and tested, but it is enforced one layer away from where it is violated, which is why the fix needed a revert path at all. The cheaper shape is for the inventory to know the cap and refuse the credit; that crosses a task boundary and is recorded rather than taken |
| F05-13 | **Three exports must independently agree for the run to be deterministic**: `DraftController.run_seed`, `WaveDirector.run_seed` and the Run Recorder's seed. Nothing enforces or checks the agreement, and a mismatch produces a run that replays wrong while every test stays green | Major | P2.12 implementer | open | `src/ui/draft_controller.gd`; `src/director/wave_director.gd`; `src/debug/run_recorder.gd` | Open, owner: the integration task, and the fix is a single seed source rather than three exports that happen to match. The Determinism test is a named prototype exit test, so a silent disagreement here defeats a gate criterion rather than merely a feature |
| F05-14 | Four P2.12 numbers or assets have no Register row and were escalated rather than invented: the card frame's corner-radius pixels, the derivation of a card's name and icon from `effect_description` (the Upgrade Definition schema has no name or icon field, so the glyph doubles as the icon), the fallback card's rank-change text format, and the Reroll audio cue - no fourth distinct CC0 audio asset exists, so the routing is built and the stream is unassigned | Minor | P2.12 implementer | open | `src/ui/draft_card_view.gd`; `src/ui/ui_sfx.gd` | Open. The missing name and icon fields are the **fifth** contract found to under-specify implementation (F03-25, F04-01, F04-08, F05-04, and now this). The audio asset belongs to the art and audio session D102 defers |
| F05-15 | Seven seams P2.12 named for the integration task, including two that are not merely wiring: `scenes/ui/draft.tscn` is never instantiated under `scenes/prototype.tscn`, so **nothing opens a Draft in the running game**; and the HUD's `rerolls_remaining` field is never populated by anything | Minor | P2.12 implementer | open | `scenes/prototype.tscn`; `src/ui/hud_economy_state.gd` | Open, owner: the integration task. This is the **sixth** finding of the shape "correct component, never wired" across two phases (F03-27, F04-04, F04-11, F04-12, F05-07, F05-15). The integration pass is therefore a single owned task whose assertions run against the assembled scene, and the pattern itself is now worth a LESSONS row rather than another per-task note |
| F05-16 | Three GodotPrompter skill conflicts recorded per CLAUDE.md: `godot-ui` forbids polling in `_process`, which the hold-to-confirm and cycle-repeat timers require and which `hud.gd` already does by precedent; Godot's native focus-neighbour system is not used for card highlighting, because the repeat and hover rules need custom state; `responsive-ui`'s DPI and touch guidance does not apply to a fixed 1920x1080 desktop prototype | Minor | P2.12 implementer | recorded | `src/ui/draft_controller.gd`; the P2.12 report | Recorded in the project's favour per CLAUDE.md |
| F05-17 | **A falsification that found the test rather than the code, and the fourth instance of this project's single most-repeated defect shape.** `console_rules_test.gd` declared its dwell, speed-gate and channel constants as `const X = Console.X`, reading them back from the class under test, so setting the dwell to zero silently moved the test's own expectation with it and all sixteen cases stayed green. Rewritten as independent literals citing the Register directly; the mutation then went red | Minor | P2.13 implementer | fixed | `tests/unit/console_rules_test.gd` | Fixed by the implementer before reporting, and named plainly rather than papered over. Identical in shape to F01-15's schema check, which asked the script under test what fields to expect. Carried to LESSONS: a test that imports its expectation from the code under test cannot detect a change to that code |
| F05-18 | **A production defect found and fixed while building, not a test-convenience fix**: `UpgradeSystem.get_console_cost()` raises an engine error on an unknown id by design, and the Console called it unconditionally, so a single mistyped or pool-restricted upgrade id would have spammed the real engine error channel - the channel the F02-14 guard exists to watch | Minor | P2.13 implementer | fixed | `src/ui/console.gd` `_entry_for_upgrade()` | Fixed by guarding on the definition lookup first |
| F05-19 | **The Console emits no EventBus signals for open, close or purchase, and the Register's C-TELEMETRY names them**, because `src/core/` was reserved to a single writer this session. This is a real gap against a Register-named requirement, not a deferral | Minor | P2.13 implementer | open | `src/ui/console.gd`; `src/core/event_bus.gd`; Register > Technical Caps & Performance > "Run Recorder" | Open, owner: the integration task, which owns the shared core files. The Run Recorder cannot record what is never emitted, and Run Recorder traces are the instrument named in several P2.15 acceptance tests |
| F05-20 | Eight seams the Console needs wired, one of which fails **silently and completely**: `set_run_inventory()` is a required code call, not a NodePath, because `RunInventory` is a `RefCounted`. Without it every entry reads zero Scrap and the Console dwell-detects, builds, and never opens - with no error anywhere | Major | P2.13 implementer | open | `src/ui/console.gd`; `src/economy/run_inventory.gd` | Open, owner: the integration task. This is the seventh 'correct component, never wired' finding and the first that fails with no symptom at all, so the integration task asserts that the Console actually opens in the assembled scene rather than that it exists |
| F05-21 | Seven interpretations named by P2.13, two worth the author's eye: sector width uses 360/7 = 51.428571 rather than the Register's rounded 51.4, which would leave a 0.2-degree gap; and auto-fire suppression follows the Register's broader "any speed while overlapping the radius" over docs/19's narrower "while the Console is open", because the Register wins on conflict | Minor | P2.13 implementer | open | `src/ui/console.gd`; Register > Interfaces > "Movement-only controls setting", C-AUTOFIRE | Open for the author. The second is a genuine document-versus-document conflict resolved by this project's own precedence rule, and recorded so the losing document can be corrected rather than left to contradict |
| F05-22 | GodotPrompter skill conflict recorded per CLAUDE.md: `godot-ui` prescribes putting UI in a `CanvasLayer` unaffected by the camera, while docs/19 and the Register's C-CONSOLE-NODE explicitly require a world-space `Node2D` scaled to the view scale. The project wins | Minor | P2.13 implementer | recorded | `src/ui/console.gd`; `scenes/ui/console.tscn` | Recorded |
| F05-23 | **Wave-Director-spawned enemies are never given a Tower reference.** `EnemyController.set_tower_reference()` is called only for the three hand-placed enemies wired by `prototype_integration.gd`, whose own header names that limit - so in the assembled game every enemy the waves spawn, which is all of them, has no Tower to seek. The Tower Seeker's entire purpose is unreachable in real play, and P2.14 could not measure T4 at all until it patched this locally in its own test scope | Blocker | P2.14 implementer | open | `src/enemy/enemy_controller.gd`; `src/integration/prototype_integration.gd`; `src/director/wave_director.gd`; `evidence/p214_report.md` | **Open, owner: the integration task, and raised to Blocker by the orchestrator.** This is not a wiring nicety: the dual-entity tension the whole prototype exists to prove - protect yourself AND the Tower - cannot occur while the enemies that attack the Tower cannot find it. It is the eighth 'correct component, never wired' finding and the first that disables a design pillar rather than a feature |
| F05-24 | **T4 is not tuned, and the Register row that says it would be ("a starting estimate; tuned in P2.14") still carries the untuned numbers.** Measured over 5 seeds with no player: the Tower was destroyed in **0 of 5** against a target of at least 3 of 5, ending every seed at exactly 500.0 of 500.0 health - never hit once. The implementer proposed levers (more Seekers, shorter interval) but did not verify them, and did not edit `data/waves/t4.tres`, which was correct: the number lives in the Register | Major | P2.14 implementer | open | `evidence/p214_report.md`; MASTER_SDLC.md > Register > Encounter Budgets > T4 row | Open. **The measurement is not yet trustworthy evidence about tuning**, because it ran on a tree carrying a local patch for F05-23 and produced an implausible result - twelve melee Seekers arriving over a 22-second window against a single-target Tower, with the Tower taking literally zero damage, is more consistent with the damage path not connecting than with perfect interception. Diagnose by experiment before changing any number: the integration task establishes whether a wave-spawned Seeker can damage the Tower at all, and only then is T4 re-measured |
| F05-25 | **The full suite is not clean after this task**: 584 cases, 1 failure, 0 gdUnit4 errors but **5 engine ERROR lines and 200 orphans**, all traced to `teaching_siege_tuning_test.gd`'s own residual pool cleanup, reduced from about 160 per seed to about 20 per seed and not eliminated. The implementer named the root cause as not fully chased | Major | P2.14 implementer | open | `tests/unit/teaching_siege_tuning_test.gd`; `tests/run_tests.ps1` engine-error guard | Open, owner: the orchestrator's settled verification pass. The engine-error guard exists precisely because a green gdUnit4 summary hid a real bug for a whole phase (F02-14); a run with five engine errors is not a clean run, and the count must reach zero or the cause must be named and understood rather than reduced |
| F05-26 | A scripted-bot defect found and fixed during measurement, worth carrying beyond this task: the first responsive bot chased the nearest enemy with no range cap and was pulled out to the Tower-Seeker spawn ring - measured 1336 px from the Tower, matching that ring's radius almost exactly - instead of defending. Fixed to engage only within 400 px of itself and otherwise hold about 200 px from the Tower | Minor | P2.14 implementer | fixed | `tests/unit/teaching_siege_tuning_test.gd` | Fixed. Recorded because P2.15 runs five more scripted bots (orbit, roam, still, far, chase) against criteria that assume the bot does what its name says, and a bot silently doing something else produces confident, wrong measurements |
| F05-27 | Five seams P2.14 needs wired, listed by the implementer most-silent-first: `RunFlowController` is instantiated nowhere, so none of this task's work runs and nothing errors; `tower_path` and `wave_director_path` have no defaults, so Tower destruction and sequence completion never end the run; `set_run_inventory()` unset makes the run-end Scrap field always read zero; `set_console_ref()` unset makes the Movement-only toggle move its own label and never reach the Console; plus the F05-23 Tower-reference gap | Major | P2.14 implementer | open | `src/run/run_flow_controller.gd`; `scenes/prototype.tscn` | Open, owner: the integration task. Note the shape: four of the five fail **silently**, and the run-end screen showing zero Scrap would have looked exactly like the correct behaviour after a player death |
| F05-28 | One number escalated: the run-end screen's 0.75 dim opacity has no Register row. The 0.3 s and 1.0 s hold-to-confirm timings are not fresh escalations - they are the Draft's own cited Register values, reused because the Register requires every paused menu to support hold-to-confirm without stating separate figures | Minor | P2.14 implementer | open | `src/ui/run_end.gd` | Open for the author, with the other interface escalations |
| F05-29 | GodotPrompter skill conflict recorded per CLAUDE.md: `godot-ui`'s pause-menu example writes `get_tree().paused` directly and stacks its buttons vertically. This project's PauseAuthority-only-writer rule and the Register's platform input floor, which requires every paused menu to lay choices out horizontally, both win | Minor | P2.14 implementer | recorded | `src/ui/pause_menu.gd` header | Recorded |
| F05-30 | **The real cause of the Tower taking zero damage, found by measurement rather than reasoning: a 14 px ghost zone between the attack's logical reach and its physical hitbox.** `_is_in_attack_range()` gates the whole attack cycle on the Register-cited `reach_or_range_px` (20 px for the Tower Seeker), so a Seeker stops and attacks anywhere out to 140 px from the Tower's centre - but the hitbox radius was hard-coded to `body_radius + 6 px` contact margin, which against a 106 px Tower hurtbox can only physically overlap within 126 px. In that 14 px band the AI believes it is attacking, its wind-up fires on schedule every 1.5 s, and the two `Area2D` shapes never touch. A real Tower Seeker parked at a distance its own code reported as in-range landed **zero hits over 240 physics ticks** | Blocker | Integration task | fixed | `src/enemy/enemy_controller.gd` (hitbox now sized `body_radius + max(reach, contact margin)`); `tests/unit/tower_damage_path_test.gd` | Fixed at the cause. The diagnosis followed this project's standing rule - build the smallest scenario that answers the question and widen by one variable - rather than accepting P2.14's plausible "the Tower intercepts them all" hypothesis, which was wrong. **This was prototype-breaking**: melee enemies could not damage the Tower at all, so the dual-entity tension the whole prototype exists to prove could not occur, and every T4 tuning measurement taken before this was measuring the wrong thing |
| F05-31 | **F05-23 fixed** (the Blocker): an enemy with no explicit Tower reference now resolves the Tower through the EntityRegistry's `&"tower"` tag - the same query interface every other entity uses, made possible by F03-39 registering the Tower there. `set_tower_reference()` and `tower_path` still take precedence and still work, so no existing system or test changed behaviour | Blocker | P2.14 implementer, fixed by the integration task | fixed | `src/enemy/enemy_controller.gd` `_resolve_sim_loop_and_register()` | Fixed. The registry route was chosen over hand-wiring each spawned instance because it removes the class of bug rather than one instance of it: any future spawner gets a Tower-aware enemy without knowing it had to |
| F05-32 | The integration pass wired the assembled scene: one `run_seed` as the single source both the Wave Director and the Draft derive from (F05-13); `PickupSystem` with its spawner, player and collector paths and the `removed_while_stuck` connection (F04-11, F04-12); `Console` instanced with `set_run_inventory()` called in code and registered at SimLoop step 12 (F05-20); `RunFlowController` instanced with its Tower and director paths, `set_run_inventory()` and `set_console_ref()` (F05-27); the Draft scene instanced with its four paths (F05-15); `UpgradeSystem` instanced and shared by the Draft, the Console and the pickup system (F05-07); the capacity providers and the debug-overlay reference on the Wave Director (F04-04); the HUD driven from the real run inventory; and wave-spawned enemies flipped to `driven_externally` and registered at SimLoop step 3 through a `enemy_spawned` handler (F03-45) | Major | Integration task | fixed | `scenes/prototype.tscn`; `src/integration/prototype_integration.gd` (+278 lines) | Fixed, and **the wiring itself still needs its own assertions**: the assembled-scene suite the task was briefed to write was not reached before the session limit ended it. Until that exists, this row rests on reading the diff, not on a test that fails when a NodePath is unset |
| F05-33 | **The integration task was terminated early by an API session limit, mid-verification.** Its last recorded action was running suites after a green 30-case result; it never wrote `evidence/integration_report.md`, and three briefed items were not reached: the assembled-scene assertion suite, the T4 re-measurement after the Blocker fix, and the headless run of the prototype scene | Major | Orchestrator | open | `git status` at the interruption; the absent `evidence/integration_report.md` | Open. The work on disk was salvaged rather than re-run, per this project's own lesson that an interrupted agent's artifacts are worth recovering. What it completed is verified by reading the diff; what it did not reach is carried forward as named work, not assumed |
