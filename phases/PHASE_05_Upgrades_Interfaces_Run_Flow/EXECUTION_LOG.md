# Phase 05 - Execution Log

Purpose: a dated record of every action, command, file change, test run, and result for this phase.

| Date | Task | Action | Command or file | Result |
| --- | --- | --- | --- | --- |
| 2026-09-20 | Entry | Phase opened early, overlapping Phase 04, under the author's prototype-first direction (D97, D100) and their instruction to build the rest of the game's functionality | - | P2.11 has no dependency on P2.9 or P2.10 - it needs the schemas (P0.6), the weapon (P2.3) and the Tower (P2.4), all of which exist - so it runs alongside them rather than after them. P2.12, P2.13 and P2.14 do depend on this wave and follow it |
| 2026-09-20 | P2.11 | Delegated the six prototype upgrades and the two fallback cards to a Sonnet implementer, scoped to `src/upgrade/**`, `data/upgrades/**` and the three effect seams (`auto_weapon.gd`, `tower_weapon.gd`, `tower_health.gd`) | - | In progress. The brief requires the base `.tres` files owned by other tasks to be left unmodified and un-mutated at runtime, so upgrade effects sit in a modifier layer above the authored base values; and it requires the additive-versus-multiplicative stacking question to be escalated rather than settled silently if the Register's wording does not decide it |

No entries yet.
| 2026-09-20 | P2.11 | Six upgrades and both fallback cards delivered | `data/upgrades/*.tres` (8 files); `src/upgrade/upgrade_system.gd`; `tests/unit/upgrade_effect_check_test.gd` (12 cases); effect seams added to `auto_weapon.gd`, `tower_weapon.gd`, `tower_health.gd` and one `heal()` method on `player.gd` | Ranks are shared between the two channels with no channel parameter anywhere in the API, which is the rule the acceptance test exists to check. **Stacking was settled from the Register rather than escalated**: C-STACK states stat = base x (1 + sum of bonuses), so rank 3 of a +20% upgrade is base x1.6, never base x1.2 cubed. The base `.tres` files owned by P2.1, P2.3 and P2.4 are neither edited nor mutated at runtime; effects sit in a modifier layer above them |
| 2026-09-20 | P2.11 | Named acceptance test falsified three ways, each restored and proved byte-identical | `tests/unit/upgrade_effect_check_test.gd` | Changing `effect_per_rank` in `heavy_rounds.tres` from 0.2 to 0.3 gave exit 100 with the exact predicted wrong value; commenting out the `is_maxed()` guard let rank reach 4 and gave exit 100; adding a second per-channel rank dictionary to break the shared-rank rule gave exit 100. Twelve cases at exit 0 in isolation, confirmed before and after the falsification cycles, engine-error guard clean |
| 2026-09-20 | P2.11 | Ten findings recorded rather than resolved in place | `LEDGER.md` F05-01 to F05-10 | Five named interpretations of ambiguous contract or Register wording, four integration seams the assembled scene still owes (including F05-07, which is the same shape as F03-27), and one skill conflict. No number needed a `NO REGISTER ROW` escalation - the Register carried every value this task required |
| 2026-09-20 | Orchestrator | Did **not** treat P2.11's isolated green suite as the task's result | - | The implementer reported the full `res://tests/unit` suite as unstable while four other implementers were mid-flight in shared files (397 then 394 tests, 1 then 14 failures), and correctly declined to fix failures outside its scope. The settled verification pass runs after the last of the five tasks lands, against a still tree - the Phase 03 precedent, where running against a moving tree produced a false reading |
| 2026-09-20 | P2.12 | Level-Up Draft delivered | `src/ui/draft_controller.gd`, `draft_card_view.gd`, `draft_fill_ring.gd`; `scenes/ui/draft.tscn`; six named suites plus three supplementary, 25 cases; `ui_sfx.gd` extended additively (21 insertions, 0 deletions) | The trigger runs at SimLoop step 11 rather than polling, and recovers the true number of levels gained because the run inventory's own flag is a boolean rather than a counter - which is what keeps a second simultaneous level-up from being dropped. Full pause through PauseAuthority only. Cards carry the one-of-each guarantee, the 60% dim, and differentiation by frame shape, glyph and header word, never colour alone. Reroll retries to avoid re-showing the same three. The forced first draft at T4's end grants level 1 at no XP cost and keeps the held XP toward level 2 |
| 2026-09-20 | P2.12 | All six named acceptance tests falsified on real source, each restored and `diff`-confirmed byte-identical | `tests/unit/draft_*_test.gd`, `guaranteed_first_draft_test.gd`, `encounter_deferral_test.gd`, `xp_cap_check_test.gd` | Lockout removed, neutral-return arming removed (caught the exact auto-confirm case), the second queued draft dropped, the one-of-each guarantee removed, the held XP discarded at the forced draft, and the draft RNG seed root hardcoded. **The last one is the finding**: the same-seed test stayed green exactly as predicted, and only the different-seed negative case caught it - the pairing rule this project learned from P2.8's vacuous determinism test, applied in advance and earning its place |
| 2026-09-20 | P2.12 | **A design contradiction raised to the author rather than resolved** | `LEDGER.md` F05-11 | SimClock is `PROCESS_MODE_PAUSABLE`, so it stops for exactly as long as the Draft's pause lasts - meaning "all timing on SimClock" cannot hold for a paused menu's own timers. Resolved locally with a `PROCESS_MODE_ALWAYS` accumulator and no `get_tree()` timer or tween, and named in the source. It will recur in P2.14 for the pause menu, the settings menu and the run-end screens, so the author's answer decides three more surfaces |
| 2026-09-20 | P2.12 | Six findings recorded, two of them Majors | `LEDGER.md` F05-11 to F05-16 | Besides the clock contradiction: **F05-13**, three independent `run_seed` exports that must agree for the Determinism test - a named prototype exit test - to mean anything, with nothing enforcing the agreement. Plus a fifth contract found to under-specify implementation, and the sixth 'correct component, never wired' finding in two phases |
| 2026-09-20 | P2.13 | Tower Console delivered | `src/ui/console.gd`; `scenes/ui/console.tscn`; four named suites, 19 cases | The whole of docs/19's Tower Console UI plus the Register's Console rules, C-REPAIR, C-AUTOFIRE, C-SECTORS and C-FALLBACK-CONSOLE: the 0.3 s dwell, the speed gate, the affordability gate with Repair correctly excluded at zero Scrap, every close condition including the stay-closed-after-Cancel rule, the purchase channel that charges exactly once and nothing at all when cancelled, the movement-only sectors with their disarm and re-arm rule, and the exact placement geometry - 200 px, the 30-degree flip hysteresis, and the 24 px on-screen text floor tracked against the camera's view scale. **It never calls `PauseAuthority.push_reason` anywhere**, which the implementer verified by grep rather than by intention, because not pausing is the entire point of the feature |
| 2026-09-20 | P2.13 | Five falsifications, and the second one found a defect in the test rather than the Console | `LEDGER.md` F05-17 | Making the Console pause went red; removing the 0.3 s dwell **stayed green**, because the suite's constants were declared as `const X = Console.X` and followed the mutation. Rewritten as independent literals citing the Register, after which it went red. Same shape as the Phase 01 schema check that asked the script under test what fields to expect - the fourth instance of this defect class in this project |
| 2026-09-20 | P2.13 | Six findings recorded, one a Major that fails with no symptom | `LEDGER.md` F05-17 to F05-22 | **F05-20**: the Console needs `set_run_inventory()` called in code, because `RunInventory` is a `RefCounted` with no NodePath. Unwired, every entry reads zero Scrap and the Console dwell-detects, builds and simply never opens, with no error anywhere - the seventh 'correct component, never wired' finding and the first with no symptom at all. **F05-19**: no EventBus signals for Console open, close or purchase, which C-TELEMETRY names and the Run Recorder needs |
| 2026-09-20 | P2.14 | Run flow delivered | `src/run/run_flow_controller.gd`, `paused_choice_bar.gd`; `src/ui/pause_menu.gd`, `settings_menu.gd`, `run_end.gd` with their three scenes; seven named suites | Run state and the death of either pool ending it; the pause menu, the settings menu carrying the Movement-only toggle, and the run-end screens, all through PauseAuthority only and all laying their choices out horizontally per the platform input floor; focus-loss and controller-disconnect pause with the harness's `--no-focus-pause` flag. Paused-menu timing follows decision D104's carve-out, taken today. Six falsifications, all red, all restored - including one the implementer added because the existing suite never drove a real focus-regained event |
| 2026-09-20 | P2.14 | **The T4 measurement produced a result that reads as a defect rather than a tuning gap** | `LEDGER.md` F05-23, F05-24 | Over five seeds with no player, the Tower ended **every** seed at exactly 500.0 of 500.0 - never hit once - against a target of losing it in at least three of five. And to measure at all, the implementer had to locally patch a real production gap it then reported rather than hid: **wave-spawned enemies are never given a Tower reference**, so in the assembled game no Seeker the waves produce can find the Tower. Twelve melee Seekers arriving over a 22-second window against a single-target Tower, with the Tower taking literally zero damage, is more consistent with a disconnected damage path than with perfect interception. No number was changed: the cause is diagnosed by experiment first |
| 2026-09-20 | Orchestrator | Raised F05-23 to **Blocker** | `LEDGER.md` | The Tower Seeker's whole purpose is to attack the Tower, and the dual-entity tension the prototype exists to prove cannot occur while the enemies that attack the Tower cannot find it. Eight findings in this project now share the shape 'correct component, never wired'; this is the first that disables a design pillar rather than a feature |
| 2026-09-20 | P2.14 | The suite is **not** clean, and the task said so | `LEDGER.md` F05-25 | 584 cases with one failure (the T4 aggregate above), plus 5 engine ERROR lines and 200 orphans traced to the tuning suite's own residual pool cleanup - reduced from roughly 160 per seed to 20 per seed, root cause not chased. The engine-error guard exists because a green gdUnit4 summary once hid a real bug for a whole phase, so a run with five engine errors is not a clean run |
| 2026-09-20 | Integration | **The zero-damage finding diagnosed by experiment, and it was a prototype-breaking defect** | `tests/unit/tower_damage_path_test.gd`; `src/enemy/enemy_controller.gd`; `LEDGER.md` F05-30 | The smallest scenario that could answer it - one Seeker parked in melee range of a real Tower with the Tower's own weapon disabled - landed **zero hits over 240 physics ticks** while its wind-up fired on schedule. Cause: the attack cycle is gated on the Register's `reach_or_range_px` (20 px), letting a Seeker stop out to 140 px from the Tower centre, while the hitbox was hard-coded to `body_radius + 6 px` and could only overlap a 106 px Tower hurtbox within 126 px. A 14 px band where the AI believes it is attacking and the shapes never touch. Fixed by sizing the hitbox to the attack's own reach. **P2.14's plausible hypothesis - that the Tower intercepted all twelve Seekers - was wrong**, which is exactly why the rule is to measure rather than reason |
| 2026-09-20 | Integration | The Blocker fixed at the class rather than the instance | `src/enemy/enemy_controller.gd`; `LEDGER.md` F05-31 | An enemy with no explicit Tower reference resolves the Tower through the EntityRegistry's `&"tower"` tag, the same query interface everything else uses - available because the debt pass registered the Tower there this morning. Hand-wiring each spawned instance would have fixed today's spawner and left the next one broken |
| 2026-09-20 | Integration | The assembled scene wired | `scenes/prototype.tscn`; `src/integration/prototype_integration.gd` (+278 lines); `LEDGER.md` F05-32 | One `run_seed` as the single source the Wave Director and the Draft both derive from; the pickup system, Console, run-flow controller, Draft and upgrade system instanced and connected, including the two seams that fail silently - `set_run_inventory()` on both the Console and the run-flow controller; the capacity providers and debug-overlay reference on the director; the HUD driven from the real inventory; and wave-spawned enemies registered into SimLoop step 3 |
| 2026-09-20 | Orchestrator | **The integration agent was terminated mid-verification by an API session limit** | `LEDGER.md` F05-33 | Its artifacts were salvaged from disk rather than the task re-run, per the Phase 00 lesson about interrupted agents. Completed work verified by reading the diff; three briefed items were not reached and are carried as named work: the assembled-scene assertion suite, the T4 re-measurement after the Blocker fix, and the headless run |
| 2026-09-20 | Orchestrator | Wrote the assembled-scene suite the interrupted agent did not reach, rather than re-running it | `tests/unit/prototype_integration_test.gd` | Seven cases in two deliberate kinds. **Identity assertions** on the seams that fail with no symptom - the same `RunInventory` object must actually reach the Console, the run-flow controller and the HUD, and one run seed must actually reach both consumers - because "the node exists" and "it is connected to the right instance" are different claims and only the second one is the bug. **Behavioural assertions** driven through the real scene - an enemy added without a Tower reference resolves one through the registry, and crediting the real inventory moves the HUD's own numbers. The NodePath check walks the tree rather than reading a list, so a node wired later is covered without anyone remembering to extend it |
| 2026-09-20 | Orchestrator | **T4 re-measured against a working damage path, and the answer changed meaning rather than changing** | `tests/unit/teaching_siege_tuning_test.gd`, 5 seeds; `tests/unit/tower_damage_path_test.gd` | Still 0 of 5 destroyed, still exactly 500.0 of 500.0 in every seed. But the two diagnosis scenarios now separate the explanations that were tangled together: with the Tower's weapon **disabled**, a Seeker does damage the Tower (the F05-30 fix works); with it **enabled**, a single Seeker at a realistic 1400 px **dies at 7.08 s before landing a hit**. So the earlier zero was two defects stacked - a broken damage path AND an undertuned wave - and only the first is fixed |
| 2026-09-20 | Orchestrator | Worked out why, from the authored numbers rather than by trying values | `data/tower/base_weapon.tres`, `data/enemies/tower_seeker.tres`, `data/encounters/t4_siege.tres` | The Tower fires 20 damage at 1.25 shots per second - 25 DPS - and engages only inside 480 px. A Seeker has 60 health, so it dies in 2.4 s, and at 0.55x speed it crosses the 480 px engagement band in about 2.7 s. Single-file arrivals every 2.0 s never overlap enough for a second Seeker to reach the Tower while the first is still being shot. The lever is therefore arrival **overlap**, not enemy count: the candidate measured first is the Seeker spawn interval at 1.0 s, unchanged in every other respect, so one variable moves |
| 2026-09-20 | Orchestrator | **The tuning instrument itself was broken, and three measurements taken through it were meaningless** | `LEDGER.md` F05-34; `tests/unit/teaching_siege_tuning_test.gd`; `tests/unit/tower_damage_path_test.gd` scenario C | Halving T4's Seeker interval moved the result by nothing at all - not a smaller effect, zero, across five seeds. Following this project's own rule that a change with no effect means the mechanism is disconnected, the next scenario isolated the single remaining variable: how the enemy is created. A pooled Seeker came back with a **null hitbox**, because the harness built an `EntitySpawner` with no container paths and `Pool` only parents an instance when it has a container. Every enemy that harness ever spawned was outside the scene tree and outside the physics world. **The real game wires all four containers in `main.tscn`**, so the game is unaffected and the instrument was the defect |
| 2026-09-20 | Orchestrator | Reverted the tuning change made on the invalid measurement | `data/encounters/t4_siege.tres` back to the authored 2.0 s interval | A number tuned against a broken instrument is worse than an untuned one, because it looks deliberate. The Register's T4 row is unchanged and T4 remains explicitly untuned |
| 2026-09-20 | Orchestrator | A second, independent benefit measured rather than assumed | `tower_damage_path_test.gd`: 10 orphans before the container fix, **0** after | An unparented pooled instance is detached and alive, which is Godot's own definition of an orphan, and no teardown could reach it. This is part of F05-25's 200 - the integration task's named cause, the Tower weapon's own separate pool, is a real second contributor. Neither was the whole of it, which is why the reported 200-to-0 did not reproduce |
| 2026-09-20 | P2.14 (T4 tuning) | **The first honest T4 measurement**, on the repaired harness, at the authored numbers | 5 seeds, no player: final Tower health 482, 500, 467, 475, 475 of 500 | Real variance at last - enemies are damaging the Tower. The authored numbers take about 5% off it and destroy it in **0 of 5** against a target of at least 3 of 5, so T4 is genuinely far undertuned. Every earlier measurement read a flat 500.0 in every seed, which was the instrument, not the wave |
| 2026-09-20 | P2.14 (T4 tuning) | Candidate 2 - Seekers 24 at 1.5x density (interval 1.0 s, same 24 s span) - **overshoots badly** | 5 seeds each half | No player: destroyed in **5 of 5** at about 27-28 s, which clears the first half of the target. Responding bot: the Tower also ends at **0% in 5 of 5**, where the target is above 50% in 5 of 5. A teaching Siege a responding player cannot survive teaches nothing. Rejected |
| 2026-09-20 | P2.14 (T4 tuning) | The measurement series exposed a **saturation threshold**, which is what makes this wave hard to tune | 5% damage at 12 @ 2.0 s; 100% at 24 @ 1.0 s | Damage is not proportional to density. Below the threshold the Tower kills each Seeker inside its 480 px band and takes almost nothing; above it, Seekers accumulate at the wall and their damage compounds. The tuning target straddles that threshold deliberately - the Tower alone must fall, and a player who responds must hold it - so the number has to sit between the Tower's own kill rate and the kill rate a player adds |
| 2026-09-20 | P2.14 (T4 tuning) | Candidate 3 derived from the rates rather than guessed | Seekers 16 at 1.5 s | The Tower kills 25 DPS over 60 HP = 0.417 Seekers per second. Arrival at 1/1.5 = 0.67 per second leaves a surplus of 0.25 per second accumulating at the wall with no player. The handgun's 20 DPS lifts the combined kill rate to roughly 0.75 per second, which **exceeds** arrival, so a responding player stops the accumulation entirely. That is precisely the teaching dynamic the Register's own target describes |
| 2026-09-20 | P2.14 (T4 tuning) | **Candidate 3 meets both halves of the Register's own target, first time, from the derivation** | 5 seeds per half | No player: the Tower is destroyed in **5 of 5** at 36-38 s of a 40 s wave - late enough to read as a wave the Tower nearly survived. Responding bot: **100% health in 5 of 5**, well above the 50% floor. 11 cases, 0 errors, 0 failures, **0 orphans**, exit 0 |
| 2026-09-20 | P2.14 (T4 tuning) | The tuned number written to the one place numbers live, and to every place restating it | `MASTER_SDLC.md` Register T4 row + Change Log 0.8.10; `docs/11_Wave_Director.md`; `data/encounters/t4_siege.tres` and its citation comment | Seekers 16 at 3 s with a 1.5 s interval, replacing the starting estimate of 12 at 2.0 s. No Review Decision Log row: the tuning target is unchanged and was already the Register's own, so this is a number change under the ordinary rule, not an intent change |
| 2026-09-20 | Orchestrator | **The assembled-scene suite caught a Blocker on its first run** | `LEDGER.md` F05-37; `scenes/prototype.tscn` | Six NodePath exports on the Console and the run-flow controller were authored child-relative - `Main/Tower` - from nodes that sit BESIDE `Main`. Every one resolved to nothing. The Console would never have had a Tower, a player, an upgrade catalogue or a camera, and the run would never have ended, with no error printed anywhere and every component suite passing. The scene's own comment beside those nodes says "A SIBLING of Main, not nested inside it" |
| 2026-09-20 | Orchestrator | Falsified the new suite four ways before trusting it | `LEDGER.md` F05-38 | One NodePath broken back to its sibling-relative form; `Console.set_run_inventory()` removed; the per-frame HUD sync removed; the Tower-resolution fix reverted. All four red, every mutated file restored and `diff`-confirmed byte-identical |
| 2026-09-20 | Orchestrator | Three defects in my own new suite, each fixed at the cause | `LEDGER.md` F05-39 | It guessed the HUD's path rather than resolving it once; it asserted a lazily-populated cache rather than the seam; and it awaited **physics** frames for a sync that runs in `_process`. The last one made it pass in one position in the file and fail in another, which looked like test-order dependence and cost two wrong hypotheses - a leaked pause reason and a dangling registry entry - before the frame-type mismatch was found |
| 2026-09-20 | Orchestrator | The assembled scene run headless, and it is cleaner than it has ever been | `Godot ... res://scenes/prototype.tscn --quit-after 600` | 600 frames, exit 0, **zero errors, zero warnings, no leak lines**. F03-30's standing 4-ObjectDB-and-2-resource leak, present on every headless run since Phase 03, is gone (F05-40) |
| 2026-09-20 | Orchestrator | **Settled verification, run against a still tree after the last change rather than during** | `.	estsun_tests.ps1 -TestPath res://tests/unit`; `tools/checks/banned_api_check.sh`; `tests/settings_check.gd`; a 600-frame headless run | **594 test cases across 89 suites, 0 errors, 0 failures, 0 flaky, 0 skipped, 0 orphans, exit 0**, with no engine-error line under the F02-14 guard. Banned-API check exit 0. Settings check PASS on 16 layers, 24 actions, 8 settings, 6 autoloads and templates 4.7.1. The headless prototype run exits 0 with no errors, no warnings and no leak lines. Nothing here says the phase is passed or ready - that is the reviewers' and the author's |
