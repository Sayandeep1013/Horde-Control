# Phase 00 - Execution Log

Purpose: a dated record of every action, command, file change, test run, and result for this phase.

| Date | Task | Action | Command or file | Result |
| --- | --- | --- | --- | --- |
| 2026-09-17 | Phase setup | Author approved the 20-phase execution split, one execution phase per checkable gate | `phases/README.md`, `phases/LESSONS.md`, 20 phase folders | Tree created: README, LESSONS, and five documents per phase. All 90 plan task IDs placed in exactly one phase |
| 2026-09-17 | E0.1, E0.2 | Added both tasks to the master's Development Phase Map with the full ten fields, plus dependency updates on P0.2 (now depends on E0.1) and P0.5 (now depends on E0.1 and E0.2) | `MASTER_SDLC.md` | Master at 0.8.1 with a Change Log row; recorded as decision D77 naming the alternative considered |
| 2026-09-17 | E0.1 | Swept superseded wording after the master amendment | `CLAUDE.md`, `NEXT_SESSION.md`, `phases/README.md` | CLAUDE.md now cites 0.8.1 and points at `phases/README.md` first; NEXT_SESSION.md carries a 2026-09-17 state block. Remaining `0.8.0` strings are historical (the 0.8.0 Change Log row and decision D56) and were left alone |
| 2026-09-17 | E0.1 | MCP smoke test, both configured servers | `get_godot_version` on `godot-comprehensive` and `godot-coding-solo` | Both returned `4.7.1.stable.official.a13da4feb`, matching the executable at `D:\godot`. `list_projects` on `D:/Gamedev` returned `[]` from both, consistent with no project existing yet |
| 2026-09-17 | E0.1 | Version discovery for the P0.5 pin | `npm view @coding-solo/godot-mcp versions`; `git ls-remote https://github.com/tugcantopaloglu/godot-mcp HEAD` | `@coding-solo/godot-mcp` has exactly one published version, 0.1.1. The GitHub server has no released version, so its only stable pin is commit `fcbc29e03297900b9a1fcc6c7cbae116fe9d532a` |
| 2026-09-17 | E0.1 | Source-level security and capability audit of both configured servers, read-only, nothing installed or run | Report: `scratchpad/e01_installed_servers_audit.md` | `godot-coding-solo` 0.1.1 is a CLI wrapper (spawn with argument arrays, no shell interpolation, no listener, no addon); its gap is an untimed `execFileAsync`, so a hang blocks the call. `godot-comprehensive` injects a GDScript autoload into `project.godot` on every `run_project` call, not opt-in, opening a 127.0.0.1:9090 listener exposing `game_eval` (arbitrary GDScript at runtime); `game_multiplayer` `create_server` binds all interfaces; path validation rejects only `..`. Neither package makes hidden network calls (both declare `axios` but never import it) |
| 2026-09-17 | E0.1 | Checked git identity and line-ending config before P0.1 | `git config --get user.name` / `user.email` / `--list --global` | Identity present (`Sayandeep1013`, `saaiyaan1013@gmail.com`); `core.autocrlf=input`, `init.defaultBranch=main`. P0.1's commit step will not stall on a missing identity |
| 2026-09-17 | P0.2 (prerequisite) | Found the 4.7.1 export templates missing: `%APPDATA%\Godot\export_templates` existed but was empty. Asked the author, since installing them writes outside the project under E0.1's stop rule | `%APPDATA%\Godot\export_templates` | Author authorised downloading and installing the official `.tpz`. Verified the asset first via the GitHub release API: `Godot_v4.7.1-stable_export_templates.tpz`, 1,280,486,955 bytes, from the `4.7.1-stable` release. C: has 52 GB free |
| 2026-09-17 | P0.2 (prerequisite) | Downloaded and installed the export templates to `%APPDATA%\Godot\export_templates\4.7.1.stable` | PowerShell: `Invoke-WebRequest` then `ZipFile::ExtractToDirectory` and a rename of the archive's `templates` folder | First attempt was blocked by a sandbox guard on a wildcard `Copy-Item -Force`; re-ran using a rename of the archive's `templates` folder instead, which succeeded (exit 0). Installed and verified: `version.txt` reads `4.7.1.stable` and 35 files are present, including `windows_release_x86_64.exe` and its console variant. P0.2's export step now has the templates the Settings check requires |
| 2026-09-17 | E0.2 | Candidate comparison and security read of Godot skill packages for Claude Code, read-only; nothing installed, no `/plugin` command run | Report: `scratchpad/e02_skills_candidates.md` | `Randroids-Dojo/Godot-Claude-Skills` (the candidate NEXT_SESSION.md names) is deprecated by its own README in favour of a successor; its GdUnit4 and CI content is still pinned to Godot 4.3, and its PlayGodot feature needs a custom engine fork, which conflicts with docs/20's pin of the stock 4.7.1 executable. `alexmeckes/godot-claude-skills` is clean but 4 of its 5 SKILL.md files lack the frontmatter Claude Code needs to register a skill, so it would fail E0.2's own verification bar, and it assumes a third MCP server this project does not have. Two candidates not named in the plan were found: `gamedev-skills/awesome-gamedev-agent-skills` (clean, actively maintained, but no test-harness content for P0.7) and `jame581/GodotPrompter` (MIT, Godot-4.7-era content, covers gdUnit4 for P0.7, discloses its SessionStart hook; the hook's 296 lines were read in full - no network calls, no credential access). Author decision on which to install is pending |
| 2026-09-17 | P0.1 | Created the Godot `.gitignore` and initialised the repository | `D:\Gamedev\.gitignore`; `git init` | `.gitignore` covers `.godot/`, `.import/`, `export_presets.cfg`, `*.translation`, build output, and `sandbox/` so the E0.1 scaffolding can never enter the repository. Repository initialised on branch `main`; 7 untracked paths reported, of which `.claude/` is deliberately left unstaged per this plan's step 5 pending the author's confirmation |
| 2026-09-17 | P0.1 | First commit, after reading the master's version header at commit time rather than assuming it | `git add .gitignore MASTER_SDLC.md CLAUDE.md NEXT_SESSION.md docs phases` then `git commit` | Version read at commit time: 0.8.1, matching the subject line. Commit `a2db130` on `main`: 111 files, 8096 insertions, covering MASTER_SDLC.md, the five drafted documents in `docs/`, CLAUDE.md, NEXT_SESSION.md, `.gitignore`, and all 102 files under `phases/`. `git log` shows the commit, which is what P0.1's exit criterion asks for; whether that criterion is met is for the review gate to record. `.claude/` is the only remaining untracked path, awaiting the author's decision |
| 2026-09-17 | P0.2 (prerequisite) | Debug-toggle bindings the Settings check requires were undefined in docs/19 | `docs/19_UI_UX.md` > Input Map and Pseudo-Localization; `MASTER_SDLC.md` Review Decision Log | Author bound F1 to the debug overlay and F2 to pseudo-localization, keyboard only, no gamepad binding. Recorded in docs/19 and as decision D78; P0.2 binds exactly these two |
| 2026-09-17 | Cross-document fix | docs/29's P3.18 row omitted three VS-tagged tests the master's Acceptance Test Matrix first-tasks at P3.18 (Failed-run progress, Upgrade dominance perception, Encounter fairness), so three mandatory slice tests were scheduled nowhere | `docs/29_Milestones_and_Roadmap.md` | Added all three to P3.18's acceptance test list, applying Document Control's rule that the master wins on intent. No value or design changed |
| 2026-09-17 | Cross-document fix | The 40% biome hook degrade floor was stated in the master's Biomes section and in the Hook degrade test but had no Provisional Values Register row, contradicting the Register's claim to be the only complete list of gameplay numbers | `MASTER_SDLC.md` > Provisional Values Register > Progression & Upgrades | Added the missing row citing owner document 15. The value itself is unchanged |
| 2026-09-17 | P0.3 | Created the documentation skeleton: 26 new stubs, plus remit and Owns fields added to the five existing drafts | `docs/00..30.md` | 26 stubs created, each carrying title, version 0.1.0, a status line, the remit copied from the master's Documentation Structure, and an Owns list copied verbatim (or "none listed in Documentation Structure" for 00, 01, 21, 22, 23, 25, 30). Docs 09, 11, 19, 20, 29 gained the same fields after their Authority line with existing content and version numbers preserved; the D78 Input Map bindings and the corrected P3.18 test list both survived unchanged. Owner-file verification: every document number cited as an owner in the Documentation Structure, the Provisional Values Register's Owner column, and the edge-case register now has a file; none missing. Noted: doc 02's Owns list keeps the master's non-ASCII superset notation because it was copied verbatim from the source of truth |
| 2026-09-17 | P0.1 | Author decided `.claude/` is versioned | `.claude/settings.json` | To be staged in the next commit, so the MCP tool policy travels with the repository and later changes to it are reviewable in the diff |
| 2026-09-17 | E0.2 | Author chose the skill to install | `jame581/GodotPrompter` | Chosen over the deprecated candidate NEXT_SESSION.md names (4.3-era GdUnit4 content, PlayGodot needs a custom engine fork conflicting with docs/20's stock-binary pin) and over `alexmeckes/godot-claude-skills` (4 of 5 SKILL.md files lack registration frontmatter). The author types the two marketplace commands; verification against the sandbox follows a restart |
| 2026-09-17 | E0.1 | Author set the policy for `godot-comprehensive`'s runtime bridge | `docs/28` (to be written at P0.5) | Sandbox only: that server's `run_project` and `game_*` tools are used only against `sandbox/connection_test`, so the autoload injection the audit found can never mutate the real project's pinned `project.godot`. Its authoring tools (scenes, nodes, scripts, project settings, input map, layers) do not inject and remain available for P0.2; running the real project and reading its output go through `godot-coding-solo` and Godot's own headless CLI |
| 2026-09-17 | E0.1 | Control-path research completed (step 1 of E0.1) | Report: `scratchpad/e01_control_paths.md` | Compared the command-line MCP servers, community editor/game bridges, Godot's built-in LSP (6005), DAP (6006) and remote debugger (6007) ports, and headless `--headless --script` runs. Findings that shape the choice: neither configured server exposes a headless flag on `run_project`, so all CI and exit-code work must go through Godot's own headless path regardless; both servers have patched RCE histories, and `godot-coding-solo`'s npm release predates a later GitHub-only fix, which is a pinning trap for P0.5; the built-in ports are loopback-only with no authentication. The report's recommended primary (`godot-comprehensive`) rests on the live-bridge tools the author has now restricted to the sandbox, so the recommendation is adopted split by tool class rather than wholesale, as recorded in the row above |
