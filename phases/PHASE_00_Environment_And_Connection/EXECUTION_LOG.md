# Phase 00 - Execution Log

Purpose: a dated record of every action, command, file change, test run, and result for this phase.

| Date | Task | Action | Command or file | Result |
| --- | --- | --- | --- | --- |
| 2026-09-17 | Phase setup | Author approved the 20-phase execution split, one execution phase per checkable gate | `phases/README.md`, `phases/LESSONS.md`, 20 phase folders | Tree created: README, LESSONS, and five documents per phase. All 90 plan task IDs placed in exactly one phase |
| 2026-09-17 | E0.1, E0.2 | Added both tasks to the master's Development Phase Map with the full ten fields, plus dependency updates on P0.2 (now depends on E0.1) and P0.5 (now depends on E0.1 and E0.2) | `MASTER_SDLC.md` | Master at 0.8.1 with a Change Log row; recorded as decision D77 naming the alternative considered |
| 2026-09-17 | E0.1 | Swept superseded wording after the master amendment | `CLAUDE.md`, `NEXT_SESSION.md`, `phases/README.md` | CLAUDE.md now cites 0.8.1 and points at `phases/README.md` first; NEXT_SESSION.md carries a 2026-09-17 state block. Remaining `0.8.0` strings are historical (the 0.8.0 Change Log row and decision D56) and were left alone |
| 2026-09-17 | E0.1 | MCP smoke test, both configured servers | `get_godot_version` on `godot-comprehensive` and `godot-coding-solo` | Both returned `4.7.1.stable.official.a13da4feb`, matching the executable at `D:\godot`. `list_projects` on `D:/Gamedev` returned `[]` from both, consistent with no project existing yet |
| 2026-09-17 | E0.1 | Version discovery for the P0.5 pin | `npm view @coding-solo/godot-mcp versions`; `git ls-remote https://github.com/tugcantopaloglu/godot-mcp HEAD` | `@coding-solo/godot-mcp` has exactly one published version, 0.1.1. The GitHub server has no released version, so its only stable pin is commit `fcbc29e03297900b9a1fcc6c7cbae116fe9d532a` |
| 2026-09-17 | E0.1 | Source-level security and capability audit of both configured servers, read-only, nothing installed or run | Report: `scratchpad/e01_installed_servers_audit.md` | `godot-coding-solo` 0.1.1 is a CLI wrapper (spawn with argument arrays, no shell interpolation, no listener, no addon); its gap is an untimed `execFileAsync`, so a hang blocks the call. `godot-comprehensive` injects a GDScript autoload into `project.godot` on every `run_project` call, not opt-in, opening a 127.0.0.1:9090 listener exposing `game_eval` (arbitrary GDScript at runtime); `game_multiplayer` `create_server` binds all interfaces; path validation rejects only `..`. Neither package makes hidden network calls (both declare `axios` but never import it) |
| 2026-09-17 | E0.1 | Checked git identity and line-ending config before P0.1 | `git config --get user.name` / `user.email` / `--list --global` | Identity present (`Sayandeep1013`, `saaiyaan1013@gmail.com`); `core.autocrlf=input`, `init.defaultBranch=main`. P0.1's commit step will not stall on a missing identity |
| 2026-09-17 | P0.2 (prerequisite) | Found the 4.7.1 export templates missing: `%APPDATA%\Godot\export_templates` existed but was empty. Asked the author, since installing them writes outside the project under E0.1's stop rule | `%APPDATA%\Godot\export_templates` | Author authorised downloading and installing the official `.tpz`. Verified the asset first via the GitHub release API: `Godot_v4.7.1-stable_export_templates.tpz`, 1,280,486,955 bytes, from the `4.7.1-stable` release. C: has 52 GB free |
| 2026-09-17 | P0.2 (prerequisite) | Downloaded and installed the export templates to `%APPDATA%\Godot\export_templates\4.7.1.stable` | PowerShell: `Invoke-WebRequest` then `ZipFile::ExtractToDirectory` and a rename of the archive's `templates` folder | First attempt was blocked by a sandbox guard on a wildcard `Copy-Item -Force`; re-ran using a rename of the archive's `templates` folder instead, which succeeded (exit 0). Installed and verified: `version.txt` reads `4.7.1.stable` and 35 files are present, including `windows_release_x86_64.exe` and its console variant. P0.2's export step now has the templates the Settings check requires |
| 2026-09-17 | E0.2 | Candidate comparison and security read of Godot skill packages for Claude Code, read-only; nothing installed, no `/plugin` command run | Report: `scratchpad/e02_skills_candidates.md` | `Randroids-Dojo/Godot-Claude-Skills` (the candidate NEXT_SESSION.md names) is deprecated by its own README in favour of a successor; its GdUnit4 and CI content is still pinned to Godot 4.3, and its PlayGodot feature needs a custom engine fork, which conflicts with docs/20's pin of the stock 4.7.1 executable. `alexmeckes/godot-claude-skills` is clean but 4 of its 5 SKILL.md files lack the frontmatter Claude Code needs to register a skill, so it would fail E0.2's own verification bar, and it assumes a third MCP server this project does not have. Two candidates not named in the plan were found: `gamedev-skills/awesome-gamedev-agent-skills` (clean, actively maintained, but no test-harness content for P0.7) and `jame581/GodotPrompter` (MIT, Godot-4.7-era content, covers gdUnit4 for P0.7, discloses its SessionStart hook; the hook's 296 lines were read in full - no network calls, no credential access). Author decision on which to install is pending |
| 2026-09-17 | P0.1 | Created the Godot `.gitignore` and initialised the repository | `D:\Gamedev\.gitignore`; `git init` | `.gitignore` covers `.godot/`, `.import/`, `export_presets.cfg`, `*.translation`, build output, and `sandbox/` so the E0.1 scaffolding can never enter the repository. Repository initialised on branch `main`; 7 untracked paths reported, of which `.claude/` is deliberately left unstaged per this plan's step 5 pending the author's confirmation |
| 2026-09-17 | P0.1 | First commit, after reading the master's version header at commit time rather than assuming it | `git add .gitignore MASTER_SDLC.md CLAUDE.md NEXT_SESSION.md docs phases` then `git commit` | Version read at commit time: 0.8.1, matching the subject line. Commit `a2db130` on `main`: 111 files, 8096 insertions, covering MASTER_SDLC.md, the five drafted documents in `docs/`, CLAUDE.md, NEXT_SESSION.md, `.gitignore`, and all 102 files under `phases/`. `git log` shows the commit, which is what P0.1's exit criterion asks for; whether that criterion is met is for the review gate to record. `.claude/` is the only remaining untracked path, awaiting the author's decision |
| 2026-09-17 | P0.2 (prerequisite) | Debug-toggle bindings the Settings check requires were undefined in docs/19 | `docs/19_UI_UX.md` > Input Map and Pseudo-Localization; `MASTER_SDLC.md` Review Decision Log | Author bound F1 to the debug overlay and F2 to pseudo-localization, keyboard only, no gamepad binding. Recorded in docs/19 and as decision D78; P0.2 binds exactly these two |
| 2026-09-17 | Cross-document fix | docs/29's P3.18 row omitted three VS-tagged tests the master's Acceptance Test Matrix first-tasks at P3.18 (Failed-run progress, Upgrade dominance perception, Encounter fairness), so three mandatory slice tests were scheduled nowhere | `docs/29_Milestones_and_Roadmap.md` | Added all three to P3.18's acceptance test list, applying Document Control's rule that the master wins on intent. No value or design changed |
| 2026-09-17 | Cross-document fix | The 40% biome hook degrade floor was stated in the master's Biomes section and in the Hook degrade test but had no Provisional Values Register row, contradicting the Register's claim to be the only complete list of gameplay numbers | `MASTER_SDLC.md` > Provisional Values Register > Progression & Upgrades | Added the missing row citing owner document 15. The value itself is unchanged |
| 2026-09-17 | P0.3 | Created the documentation skeleton: 26 new stubs, plus remit and Owns fields added to the five existing drafts | `docs/00..30.md` | 26 stubs created, each carrying title, version 0.1.0, a status line, the remit copied from the master's Documentation Structure, and an Owns list copied verbatim (or "none listed in Documentation Structure" for 00, 01, 21, 22, 23, 25, 30). Docs 09, 11, 19, 20, 29 gained the same fields after their Authority line with existing content and version numbers preserved; the D78 Input Map bindings and the corrected P3.18 test list both survived unchanged. Owner-file verification: every document number cited as an owner in the Documentation Structure, the Provisional Values Register's Owner column, and the edge-case register now has a file; none missing. Noted: doc 02's Owns list keeps the master's non-ASCII superset notation because it was copied verbatim from the source of truth |
| 2026-09-17 | P0.1 | Author decided `.claude/` is versioned | `.claude/settings.json` | To be staged in the next commit, so the MCP tool policy travels with the repository and later changes to it are reviewable in the diff |
| 2026-09-17 | E0.2 | Author chose the skill to install | `jame581/GodotPrompter` | Chosen over the deprecated candidate NEXT_SESSION.md names (4.3-era GdUnit4 content, PlayGodot needs a custom engine fork conflicting with docs/20's stock-binary pin) and over `alexmeckes/godot-claude-skills` (4 of 5 SKILL.md files lack registration frontmatter). The author types the two marketplace commands; verification against the sandbox follows a restart |
| 2026-09-17 | E0.1 | Author set the policy for `godot-comprehensive`'s runtime bridge | `docs/28` (to be written at P0.5) | Sandbox only: that server's `run_project` and `game_*` tools are used only against `sandbox/connection_test`, so the autoload injection the audit found can never mutate the real project's pinned `project.godot`. Its authoring tools (scenes, nodes, scripts, project settings, input map, layers) do not inject and remain available for P0.2; running the real project and reading its output go through `godot-coding-solo` and Godot's own headless CLI |
| 2026-09-17 | E0.1 | Control-path research completed (step 1 of E0.1) | Report: `scratchpad/e01_control_paths.md` | Compared the command-line MCP servers, community editor/game bridges, Godot's built-in LSP (6005), DAP (6006) and remote debugger (6007) ports, and headless `--headless --script` runs. Findings that shape the choice: neither configured server exposes a headless flag on `run_project`, so all CI and exit-code work must go through Godot's own headless path regardless; both servers have patched RCE histories, and `godot-coding-solo`'s npm release predates a later GitHub-only fix, which is a pinning trap for P0.5; the built-in ports are loopback-only with no authentication. The report's recommended primary (`godot-comprehensive`) rests on the live-bridge tools the author has now restricted to the sandbox, so the recommendation is adopted split by tool class rather than wholesale, as recorded in the row above |
| 2026-09-17 | P0.3 | Spot-checked P0.3's output rather than accepting the subagent's report unread, then fixed what the check found | `docs/*.md`; Phase 00 ledger F-01 | Read docs 07 (long Owns list), 23 (no Owns list, correctly recorded as such rather than invented) and 28 (the file E0.1 writes into): all three copy the master's Documentation Structure faithfully. The check also found that all 26 new stubs omitted the trailing two-space break after `**Version:**`, collapsing the Version and Status lines on render, while the five pre-existing drafts carry it. Fixed with a scripted pass over only the affected files and verified: 31 of 31 now carry the break, 0 lack it, diff confined to 26 files at one line each. Raised and closed as ledger finding F-01 |
| 2026-09-17 | P0.1, E0.1 | Verified the sandbox-commit failure point against the real directory instead of leaving it asserted | `git check-ignore -v sandbox`; `git status --porcelain` | Commit `68ef501` landed with an empty working tree afterwards. E0.1's agent has now created `sandbox/` on disk, and `git check-ignore -v` reports `.gitignore:25:sandbox/` matching it, so the rule is firing against a real directory rather than a hypothetical one. The predetermined risk "the sandbox project is committed by accident" is mitigated in practice; `git status` before each commit remains the standing check |
| 2026-09-17 | E0.1, P0.5 | Checked `godot-coding-solo`'s provenance before pinning it, following the research report's warning of a pinning trap | npm registry metadata for `@coding-solo/godot-mcp`; GitHub API commits for `Coding-Solo/godot-mcp` | Confirmed and worse than a version nit. npm's only published version, 0.1.1, was published 2026-02-03T17:23:27Z. Six commits have landed since, including `d4cc0f92` "fix: prevent arbitrary GDScript instantiation via add_node/create_scene", merged as PR #99 - an RCE fix that exists only in the repository and was never published to npm. The project's MCP config uses `@latest`, which resolves to that unpatched 0.1.1. Repository HEAD `1209744fad78f3998f98c7394fd0f6ef50da5281` contains the fix. This lands directly in P0.2's path, since `add_node` and `create_scene` are the calls that would author the project skeleton. Raised as ledger finding F-02 and put to the author |
| 2026-09-17 | E0.1, P0.5 | Applied the author's decision on F-02 and pinned both MCP servers, editing the machine-managed config through a parser rather than by hand | `C:\Users\sayan\.claude.json` (outside the project, changed under the author's explicit authorisation) | Backed up the original first (126,816 bytes, backup path recorded in the scratchpad), then rewrote only the two `args` arrays. `godot-coding-solo`: `["--yes","@coding-solo/godot-mcp@latest"]` became `["--yes","github:Coding-Solo/godot-mcp#1209744fad78f3998f98c7394fd0f6ef50da5281"]`, the commit carrying the RCE fix. `godot-comprehensive`: `["--yes","github:tugcantopaloglu/godot-mcp"]` became the same spec pinned at `#fcbc29e03297900b9a1fcc6c7cbae116fe9d532a`, the commit the audit confirmed was already in use, so that pin changes no behaviour. Verified after writing: the file re-parses, both `GODOT_PATH` values are intact, and the other configured servers are untouched. The pins take effect only on restart, so E0.1's `godot-coding-solo` matrix rows were exercised against the unpinned 0.1.1 build and need re-checking afterwards |
| 2026-09-18 | E0.1, P0.5 | Session restarted, which was meant to activate the pins. Both Godot MCP servers instead failed to connect (`CONNECTION_CLOSED`) | `C:\Users\sayan\.claude.json`; `npx` probe of both pinned specs | Regression caused by the pin. `npx` cannot install any commit-pinned git spec on this machine: both specs fail with `npm error GitFetcher requires an Arborist constructor to pack a tarball` (npm 10.9.3). The `Coding-Solo` repo gitignores `build/` and ships `bin -> ./build/index.js` only inside the npm tarball, so a git install must compile through its `prepare` script; the previously working unpinned spec only worked because a warm npx cache entry already existed, and changing the spec changed the cache key. Raised as F-04 and being fixed by cloning and building both servers locally at their pinned commits, which removes npx from the path while keeping the pin |
| 2026-09-18 | E0.1 | Salvaged the stopped matrix agent's partial evidence rather than re-running it, since the agent ended without writing its report | `sandbox/connection_test/*.log`, `project.godot`, `scenes/`, `scripts/` | Recovered: Godot 4.7.1 confirmed running a real project (`Godot Engine v4.7.1.stable.official.a13da4feb`, Vulkan, GTX 1650 Ti); a CLI run captured game output (`TestSprite ready, speed=42.0`); a headless heartbeat ran past 2,040 frames with no stall, so the 4.7.1 headless stall bug the research flagged did not reproduce here; a port-in-use scenario was staged (`LISTENING_ON_9090`). Not recovered: the matrix table itself, the MCP-method rows, latency figures, and the subagent tool-access row. Those need re-running once the servers are back |
| 2026-09-18 | Repository | Pushed the verified history to the author's GitHub repository | `git push -u origin main` -> https://github.com/Sayandeep1013/Horde-Control.git | Remote was empty; pushed all four commits up to `43f56d7`. 138 tracked files: the master, `docs/`, `phases/`, `CLAUDE.md`, `NEXT_SESSION.md`, `.gitignore`, `.claude/settings.json`. `sandbox/` is excluded by `.gitignore`, and `tools/` was added to `.gitignore` in the same pass so the locally built MCP servers can never be committed |
| 2026-09-18 | E0.1, P0.5 | Fixed F-04 by removing npx from the MCP startup path: cloned both servers at their pinned commits into `tools/mcp/` and built them locally | `git clone` + `git checkout <sha>` + `npm install` (which runs each package's `prepare` build) | `coding-solo` at `1209744fad78f3998f98c7394fd0f6ef50da5281` built `build/index.js` (79,278 bytes); `comprehensive` at `fcbc29e03297900b9a1fcc6c7cbae116fe9d532a` built `build/index.js` (345,711 bytes). Both checked-out commits confirmed with `git rev-parse`. `tools/` is gitignored, so the builds never enter the repository |
| 2026-09-18 | E0.1, P0.5 | Proved the new server spec works before editing the config, rather than repeating the mistake that caused F-04 | Piped a real MCP `initialize` request into each built server over stdio | Both returned a valid response: `{"result":{"protocolVersion":"2024-11-05","capabilities":{"tools":{}},"serverInfo":{"name":"godot-mcp",...}}}`. Only then was the config changed, after a second backup, to `command: node` with the absolute path to each built entry point; `GODOT_PATH` and the other configured servers were verified intact afterwards. A restart is still required before Claude Code reconnects |
| 2026-09-18 | E0.2 | Checked whether the chosen Godot skill was installed | `~/.claude/plugins/installed_plugins.json`, `known_marketplaces.json` | Not installed: the plugins file is empty (`{"version":2,"plugins":{}}`) and the only known marketplace is `claude-plugins-official`. The two `/plugin` commands for `jame581/GodotPrompter` have not been run, so E0.2 remains open and CLAUDE.md's "No Godot skills are installed yet" line is still accurate |
| 2026-09-18 | E0.1, P0.5 | Verified the rebuilt servers after the restart, closing F-02 and F-04 on evidence rather than on the config file alone | Live `get_godot_version` on both servers; `Win32_Process` command-line inspection | Both returned `4.7.1.stable.official.a13da4feb`. The running processes are exactly `node D:/Gamedev/tools/mcp/coding-solo/build/index.js` and `node D:/Gamedev/tools/mcp/comprehensive/build/index.js`, proving both run the locally built pinned commits with npx removed from the startup path. F-02 and F-04 are both recorded fixed |
| 2026-09-18 | E0.2 | Re-checked the skill install after the author added the marketplace and reloaded plugins | `~/.claude/plugins/known_marketplaces.json`, `installed_plugins.json`, marketplace manifest | The marketplace `godot-prompter-marketplace` is registered and cloned, and its manifest offers exactly one plugin, `godot-prompter` version 1.13.3. `installed_plugins.json` is still `{"version":2,"plugins":{}}`, and `/reload-plugins` reported 0 plugins and 0 skills, because reloading re-reads what is installed rather than installing anything. The install step has not been run yet, so E0.2 stays open |
| 2026-09-18 | E0.2 | Author ran the install command; the plugin registered | `/plugin install godot-prompter@godot-prompter-marketplace`; `installed_plugins.json` | Installed `godot-prompter` 1.13.3 (git sha `ff3b514ffa08804b88d3d9d2465843734a4022d9`), scoped to `D:Gamedev`. 55 `godot-prompter:*` skills and 8 specialist agents are now listed, so the "appears in the skill list after a restart" half of E0.2's exit criterion is satisfied. The remaining half, completing one task against the E0.1 sandbox, is still outstanding |
| 2026-09-18 | E0.2, P0.2 | Found that F-03 had a wider blast radius than P0.2 and corrected it | `sandbox/connection_test/project.godot` | The plugin's SessionStart card announced "This project targets Godot 4.4" and told every agent to prefer 4.4 APIs. Traced to the only `project.godot` under `D:Gamedev`: the sandbox file the MCP server generated with `config/features=PackedStringArray("4.4")`. A throwaway test artifact was mis-instructing the whole toolchain on a project pinned to 4.7.1. Corrected the sandbox file to `PackedStringArray("4.7")` so it matches the engine running it; F-03 stays open because P0.2 still has to write the real `project.godot` itself rather than accept the server's default |
| 2026-09-18 | E0.2 | Completed the verification half of E0.2: one real task against the E0.1 sandbox, using the installed skill | `godot-prompter:resource-pattern`; `sandbox/connection_test/scripts/e02_verify_resource.gd`; MCP `validate_script` | Invoked the skill, then wrote a typed Resource following its checklist (`class_name`, typed `@export`, `@export_range`, no logic inside the Resource) and validated it through the Godot MCP server against the sandbox project: `{"valid": true, "errorCount": 0}`. To prove the check can actually fail rather than always reporting success, the same tool was run against the deliberately broken script the earlier agent left behind: `{"valid": false, "errorCount": 1}` with the message "Expected end of statement after expression" at line 4. Skill, MCP connection and sandbox therefore work together. Whether E0.2 meets its exit criterion is for the review gate to record |
| 2026-09-18 | E0.2 | Added the GodotPrompter routing section to CLAUDE.md, with the author's agreement, and swept the stale tooling wording | `CLAUDE.md` | The plugin asks once before writing to an agent instructions file and does not add it silently. Subagents never see its session card, and this project does most implementation through Sonnet subagents, so without the section they would not know the 55 skills exist. The section is explicitly subordinate: where a skill conflicts with docs/20, the Provisional Values Register, or an Author decision, this project wins and the conflict goes to the phase LEDGER. It also states the 4.7.1 target so no agent inherits the plugin's mistaken 4.4 reading. The "No Godot skills are installed yet" line was replaced with the installed pack and version |
