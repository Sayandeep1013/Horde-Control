# Phase 00 - Ledger

Every finding raised against this phase, carried across review iterations until closed.

Severities: Blocker, Major, Minor.
Statuses: open; fixed; deferred (with owner); withdrawn.

| ID | Finding | Severity | Raised by | Status | Evidence | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| F-01 | The 26 stubs P0.3 created omitted the trailing two-space line break after `**Version:**`, so the Version and Status lines collapse onto one line when rendered. The five pre-existing drafts (09, 11, 19, 20, 29) carry that break, so the new stubs were inconsistent with the convention already in the repository | Minor | Orchestrator, spot-check of P0.3's output before the review gate | fixed | Before: 26 of 31 files in `docs/` lacked the break, and the 5 that carried it were exactly the pre-existing drafts. After: 31 of 31 carry it and 0 lack it; `git diff --stat` shows 26 files changed with 26 insertions and 26 deletions, one line each, so nothing else was touched; `cat -A` confirms the line now ends `**Version:** 0.1.0  $` | Scripted `sed` pass over only the files missing the break, appending two spaces to the Version line. The Status line needs no break because a blank line already follows it |
| F-02 | `godot-coding-solo` is configured as `npx @coding-solo/godot-mcp@latest`, which resolves to npm 0.1.1 (published 2026-02-03). The RCE fix `d4cc0f92` "prevent arbitrary GDScript instantiation via add_node/create_scene" (PR #99) landed after that publish and exists only in the GitHub repository, so the installed server is unpatched in precisely the calls P0.2 would use to author the project skeleton. Pinning to the npm version at P0.5 would make the unpatched build permanent | Major | Orchestrator, provenance check before P0.5's pin | fixed | npm metadata: only published version 0.1.1, published 2026-02-03T17:23:27.236Z. GitHub API: 6 commits since that timestamp, including `d4cc0f92` and its merge `1209744f`. Repository HEAD is `1209744fad78f3998f98c7394fd0f6ef50da5281`. After the config change, re-reading and re-parsing the config shows `godot-coding-solo` args as `["--yes","github:Coding-Solo/godot-mcp#1209744fad78f3998f98c7394fd0f6ef50da5281"]` | Author chose the fixed GitHub commit. The MCP config now pins `godot-coding-solo` to commit `1209744f`, which carries the RCE fix, replacing `@latest`. `godot-comprehensive` was pinned in the same pass to `fcbc29e03297900b9a1fcc6c7cbae116fe9d532a`, the commit already in use, which satisfies P0.5's requirement to pin both servers. Verified after the restart on 2026-09-18: `godot-coding-solo` answers a live `get_godot_version` call with `4.7.1.stable.official.a13da4feb`, and its running process is `node D:/Gamedev/tools/mcp/coding-solo/build/index.js` - the local build of commit `1209744f`, which carries the RCE fix. The unpatched npm 0.1.1 is no longer in the startup path |
| F-03 | A `project.godot` generated through the Godot MCP server declares `config/features=PackedStringArray("4.4")`, not 4.7. P0.2 requires `project.godot` to record engine version 4.7 with a boot check confirming 4.7.1 (docs/20 > Godot 4.x Implementation Standards), so creating the real project through that tool unmodified would contradict the engine pin in the very first file it writes | Major | Orchestrator, salvaging E0.1's sandbox artifacts | fixed | `sandbox/connection_test/project.godot`, written by the MCP server, contains `config/features=PackedStringArray("4.4")` while the running engine reports `4.7.1.stable.official.a13da4feb` | Blast radius found on 2026-09-18, wider than P0.2: the GodotPrompter plugin scans the project for a `project.godot`, and the sandbox file was the only one on disk, so its SessionStart card announced "This project targets Godot 4.4" to every agent and subagent in the project - on a project pinned to 4.7.1. Mitigated immediately by correcting the sandbox file to `PackedStringArray("4.7")`, which also matches the engine actually running it. Closed at P0.2 on 2026-09-18, both halves done. `project.godot` was authored by hand rather than generated, recording config/features 4.7 plus Forward Plus, and Godot left that intact across ProjectSettings.save(). The Settings check now asserts the RECORDED features value, not just the running engine, and that assertion was proven to bite: setting the array back to 4.4 produced a named failure and exit code 1, after which the file was restored and the check passed again |
| F-04 | Pinning both MCP servers to commit-pinned git specs broke both: `npx` on this machine cannot install any git spec, failing with `GitFetcher requires an Arborist constructor to pack a tarball`. The servers were unavailable for a whole session as a result | Major | Orchestrator, after the restart that was meant to activate the pins | fixed | Both servers reported `CONNECTION_CLOSED`; direct `npx` probes of each pinned spec reproduce the same npm error. `Coding-Solo/godot-mcp` gitignores `build/` and exposes its bin only through the npm tarball, so a git install must run its `prepare` build | Cloning both repositories at their pinned commits into `tools/mcp/` and building them locally, then pointing the MCP config at `node <path>/build/index.js`. This keeps the author's decision to run the patched commit while removing npx from the startup path. Done: both repositories are cloned and built at their pinned commits (`1209744f` and `fcbc29e0`, both confirmed by `git rev-parse`), and the MCP config now runs `node <path>/build/index.js` for each, with `GODOT_PATH` preserved. Before changing the config this time, each built server was probed with a real MCP `initialize` request and both returned a valid `protocolVersion` response, so the spec was proven working rather than assumed. Verified after the restart on 2026-09-18: both servers reconnected and answered live `get_godot_version` calls with `4.7.1.stable.official.a13da4feb`. Process inspection confirms the running command lines are exactly `node D:/Gamedev/tools/mcp/coding-solo/build/index.js` and `node D:/Gamedev/tools/mcp/comprehensive/build/index.js`, so npx is gone from the startup path and both servers run their pinned commits |
| F-05 | Godot treats everything under the project root as a resource, so the first Windows export packed tools/mcp/ - the locally built MCP servers, including their GDScript - into the shipped pck. An empty scene produced an 11,229,568 byte pack. sandbox/ would have been swept in the same way, being a nested Godot project inside the real one | Major | Orchestrator, inspecting the first P0.2 export output | fixed | Export log showed savepack storing res://tools/mcp/comprehensive/build/scripts/mcp_interaction_server.gd.remap and similar. Pack size before: 11,229,568 bytes. After adding .gdignore to both directories and re-exporting: 22 KB, and a byte search of the pack for mcp_interaction_server finds nothing | Added an empty .gdignore to tools/ and sandbox/, which makes the engine skip those subtrees entirely - no scan, no import, no export - rather than relying on export filters. Note for anyone rebuilding the servers from docs/28: both .gdignore files sit inside gitignored directories, so they are not in the repository and must be recreated alongside the server builds |
| F-06 | Subagents are not gated by the permissions.ask list in .claude/settings.json. A Sonnet subagent called all six tools that list named - delete_file, export_project, manage_docker_export, manage_ci_pipeline, game_http_request, game_websocket - with no prompt and no denial, each failing only for ordinary reasons such as no export preset configured. The sandbox-only rule for godot-comprehensive is therefore enforced by instruction, not by the tool layer, and this project delegates implementation to subagents by design | Major | E0.1 matrix subagent, reporting its own tool access | fixed | The subagent quoted verbatim errors from each of the six calls showing execution rather than denial. It never pushed a call to a real effect, and stated the restraint came from its task constraints, not the tool layer | Author chose to deny the four tools no phase in scope needs: manage_docker_export, manage_ci_pipeline, game_http_request and game_websocket are now in permissions.deny, confirmed by those tools disappearing from the session. delete_file and export_project stay on ask because later tasks need them, so they remain reachable by a subagent without a prompt. That residual exposure is deliberate, documented in docs/28 Known Limitations, and mitigated only instructionally through CLAUDE.md and explicit constraints in every delegation prompt. Completed on 2026-09-18 after the blind P0.1 review found the account incomplete: a machine-local override file, .claude/settings.local.json, carries an allow list for all six tools including the four now denied. Two consequences. First, precedence: the four denied tools disappeared from the session while that allow list was already present, so deny beats a local allow - observed behaviour, not documented guarantee. Second, exposure: that file was excluded only by a machine-global ignore file outside the repository, which does not travel with a clone, and this repository now has a remote. It is now excluded by the repository's own .gitignore, confirmed with git check-ignore against the real path, and it has never been committed in any of the repository's commits |
| F-07 | The add_node tool of godot-coding-solo reports success and logs a property assignment followed by Save result 0 OK, but the property is never written to the saved scene. Node and scene creation work; the properties argument is silently dropped | Major | E0.1 matrix subagent, verifying by independent read-back rather than trusting the return value | fixed | An independent read of the saved tscn after the call shows no position property present, despite the success log | This is why the matrix required independent verification rather than non-error returns. godot-coding-solo is demoted to version and run operations only and its property writes are not trusted. Authoring goes through godot-comprehensive, whose scene create, save and read-back passed. Recorded in docs/28 under known failure modes |
| F-08 | The game_key_press tool of godot-comprehensive returned success for a held key-down, but Input.is_key_pressed queried independently through game_eval, immediately and again after a real one-second gap, returned false both times | Minor | E0.1 matrix subagent | fixed | Two independent game_eval queries returned false after a reported-successful key press | Deliberately not called a confirmed bug: the probe scene had no _input handler to cross-check against, so the discrepancy may be the probe rather than the tool. Input injection is marked unverified in the matrix rather than passed. Reopened and re-resolved on 2026-09-18 after iteration 2 falsified the first root cause. The first closure asserted that Input.parse_input_event never updates the key-state array; that was reasoned, not tested, and a reviewer disproved it on the pinned engine. The tested mechanism, from reading the handlers in tools/mcp/comprehensive/src/scripts/mcp_interaction_server.gd and probing the running game: game_key_press with a key parameter calls parse_input_event and then awaits one process frame and sends a release, so any poll after the call reads false; game_key_hold with a key parameter sends no release, yet the key state still did not stick, and the probe showed DisplayServer.window_is_focused() false, consistent with Godot clearing pressed input on focus loss in an MCP-launched window; game_key_hold with an action parameter calls Input.action_press and DOES work - Input.is_action_pressed returned true with strength 1.0 and was still true 5829 frames later. Standing rule for P2.15 scripted bots, corrected: drive bots through INPUT ACTIONS, using game_key_hold with the action parameter and polling Input.is_action_pressed. Do not poll is_key_pressed after a key-parameter call. Raw key events do reach _input handlers - a runtime probe counted 2 InputEventKey events, the second being the auto-release - so _input-based verification also works || F-06b | Residual half of F-06, split out so it is owned rather than sitting indefinitely at partially fixed: delete_file and export_project remain on ask, and the ask gate does not intercept a subagent. Separately, run_project and every game_* tool sit under the server-wide allow entry and were never ask-gated at all, so the sandbox-only rule for them has never been tool-enforced | Major | Phase 00 reviewer, who declined to let a Major sit at partially fixed across a gate | deferred (owner: Phase 01 entry, re-test trigger: any Claude Code release that changes how subagent permissions resolve) | .claude/settings.json: allow holds the two bare server namespaces; ask holds delete_file and export_project; deny holds the five denied tools | Accepted deliberately rather than closed. export_project stays reachable because P0.2 and P4.5.3 need an export path, and delete_file because later phases need it; denying them would block work in scope. Enforcement until the tool layer changes: every delegation prompt carries the sandbox-only constraint and the no-delete, no-export prohibition explicitly, and Phase 01 re-tests the boundary at entry. Recorded in docs/28 Known Limitations |
| F-09 | PLAN P0.2 step 6 required the folder-layout choice to be recorded as a gap for document 23 to describe once it has content. No row recorded it, and docs/23_Folder_Structure.md is still a bare stub while the delivered tree is wider than the plan sketched: 14 subfolders under src/, 7 under data/, plus scenes/, addons/ and tests/ | Minor | Phase 00 reviewer | open (owner: P3.2b, which brings document 23 toward working) | docs/23_Folder_Structure.md is the P0.3 stub; the layout exists on disk and in commit bd65fd8 | Recorded here so document 23 inherits the gap rather than reverse-engineering the layout. The layout itself is not in question; only its ownership and description are |
| F-10 | The Settings check asserted existence rather than values and passed on a materially broken project. A reviewer deleted the entire display block from project.godot and rebound move_up from W to Z, and the check still printed PASS with exit 0. Related: five of the seven settings docs/20 pins are not recorded in project.godot at all, because Godot 4.7.1 strips default-valued keys on ProjectSettings.save(), so the execution log claim that they were pinned was wrong | Major | P0.2 blind critical review | fixed | Reviewer reproduction: display keys deleted and move_up rebound, delivered check printed PASS exit 0. Godot fallback proven: with those keys deleted the engine reports 1152x648 and stretch disabled, not the pinned 1920x1080 canvas_items | Rewritten to assert effective values through ProjectSettings.get_setting rather than file text, since a text pin of a default-valued key cannot survive an editor save: all eight pinned settings by value, every action compared as an exact event set by physical keycode, joypad button, joypad axis and mouse button, and the autoload/BootCheck target. Falsified three times by the implementer and once independently by the orchestrator: rebinding move_up to Z produced the exact expected-versus-actual event diff and exit 1, then passed again after git restore. The real project reports PASS with 16 layers, 24 actions, 8 settings |
| F-11 | The godot-prompter skill pack's gdUnit4 CLI guidance is stale and actively misleading for the shipped version, on precisely the capability E0.2 chose the pack for. It documents `addons/gdUnit4/GdUnitRunner.gd` with a `--testsuites` flag; in gdUnit4 v6.2.1 that file does not exist, the entry point is `addons/gdUnit4/bin/GdUnitCmdTool.gd` and the flag is `-a`. The skill never mentions `--ignoreHeadlessMode` anywhere, which is the one flag that makes headless CI work at all | Major | E0.2 re-verification through the skill, run against the sandbox | open (owner: P0.7, which must not follow the skill's CLI guidance) | Following the skill's documented command headless aborts with `Headless mode is not supported!` and process exit code 103. Source confirms it: `GdUnitTestCIRunner.gd` checks `DisplayServer.get_name() == "headless"` and quits with `RETURN_ERROR_HEADLESS_NOT_SUPPORTED` (103) unless the flag sets `_headless_mode_ignore`. The working invocation had to be derived by reading the installed source, not the skill | Does not invalidate the install; the pack's generic testing guidance (framework selection, assertion tables, what not to test) was not contradicted. But P0.7 must take its command line from this ledger and from docs/28, never from the skill. Recorded against E0.2's own justification, since gdUnit4 coverage for P0.7 was a stated reason for choosing this pack over the alternatives |
| F-12 | gdUnit4's upstream repository has moved from `MikeSchulze/gdUnit4` to `godot-gdunit-labs/gdUnit4`. The old URL currently 301-redirects, which is not guaranteed to persist | Minor | E0.2 re-verification | open (owner: P0.7, before it pins a source URL) | The GitHub API redirects `MikeSchulze/gdUnit4` to the new org; release author and licence metadata are unchanged (MIT, Mike Schulze), which reads as an org migration rather than a fork | P0.7 pins the new org's URL and the exact version. A human should eyeball the ownership change before the pin is treated as trusted, since org ownership cannot be verified from the redirect alone |
