# Phase 01 - Execution Log

Dated record of every action, command, file change, test run, and result for this phase.

| Date | Task | Action | Command or file | Result |
| --- | --- | --- | --- | --- |
| 2026-09-18 | Entry | Read the loop-rule (a) inputs before any implementation: CLAUDE.md, phases/README.md, phases/LESSONS.md, NEXT_SESSION.md, this phase's PLAN.md, and Phase 00's EXECUTION_LOG.md, FAILURE_POINTS.md, LEDGER.md and REVIEW.md | - | Done. PLAN.md's "Carried lessons" placeholder and its stale entry-condition and open-question wording were rewritten from what Phase 00 actually recorded (see the same-day PLAN.md rows below) |
| 2026-09-18 | Entry | Pin verification, step 1 of the phase-entry sequence in NEXT_SESSION.md | `git -C tools/mcp/coding-solo rev-parse HEAD`; `git -C tools/mcp/comprehensive rev-parse HEAD` | Both matched the recorded pins exactly: coding-solo `1209744fad78f3998f98c7394fd0f6ef50da5281`, comprehensive `fcbc29e03297900b9a1fcc6c7cbae116fe9d532a`. `tools/.gdignore` and `sandbox/.gdignore` both present (0 bytes each, as intended) |
| 2026-09-18 | Entry | Settings check re-run, step 3 of the phase-entry sequence: the regression guard for everything Phase 00 pinned | `Godot_v4.7.1-stable_win64_console.exe --headless --path . --script res://tests/settings_check.gd` | Exit 0. `Settings check: PASS (16 layers, 24 actions asserted of 115 input entries present, 8 settings, 1 autoload(s), templates 4.7.1.stable)`, and the BootCheck autoload confirmed 4.7.1 |
| 2026-09-18 | Entry | Read `.claude/settings.json` and `.claude/settings.local.json` while preparing the F-06b re-test | `.claude/settings.local.json` | Found the finding recorded below as F01-01: settings.local.json carried `allow` entries for all six tools settings.json gates or denies |
| 2026-09-18 | Entry | Established settings precedence for F01-01 by delegated research plus direct observation of this session's own tool surface | - | `deny` in settings.json beats `allow` in settings.local.json: `manage_docker_export`, `manage_ci_pipeline`, `game_http_request`, `game_websocket` and `game_multiplayer` are absent from this session's tool surface. The two `ask`-listed tools, `delete_file` and `export_project`, are present, so for them the local `allow` did override the `ask` gate |
| 2026-09-18 | Entry | F-06b re-test, step 2 of the phase-entry sequence, run from a Sonnet subagent because that is the path the ask gate is known not to cover. Probe file authored first so nothing of value was at risk | `sandbox/connection_test/f06b_probe.txt` | Probe 1, `run_project` against `D:/Gamedev`: **REFUSED** - `Project path is outside the allowed roots (GODOT_MCP_ALLOWED_DIRS): D:/Gamedev`. Probe 2, `run_project` against `D:/Gamedev/sandbox/connection_test`: **ALLOWED**, launched on 4.7.1 and was stopped immediately. Probe 3, `delete_file` on the probe file: **executed with no prompt and no denial**; the subagent verified by independent read-back (`ls`, `stat`) that the file was actually gone, not merely reported gone |
| 2026-09-18 | Entry | Author consulted on four items that needed a decision before implementation could start | - | Four decisions taken, recorded as D84-D87 in MASTER_SDLC.md > Review Decision Log |
| 2026-09-18 | Entry | Applied decision D84: stripped the six `allow` entries from settings.local.json, restoring settings.json's intent | `.claude/settings.local.json` | File now carries an empty allow list. A copy of the previous contents was kept in the session scratchpad. Not yet re-verified: the tool surface reflects the settings read at session start, so whether `delete_file` and `export_project` return to prompting is confirmed only after a restart |
| 2026-09-18 | Entry | Invoked the GodotPrompter skills required before implementation, per CLAUDE.md > GodotPrompter | `godot-prompter:resource-pattern`; `godot-prompter:godot-testing` | resource-pattern applied to P0.6 with no conflict. godot-testing conflicts with this project on two points, recorded as F01-03 in LEDGER.md |
| 2026-09-18 | P0.4 | Delegated documents 00, 01 and 02 to a Sonnet implementer, carrying the no-delete, no-export, no-MCP constraints explicitly | `docs/00_Vision_and_Design_Philosophy.md`; `docs/01_Design_Pillars.md`; `docs/02_Gameplay_Loop.md` | Delivered, all three at 1.0.0 with doc 28's header shape. Doc 01 states a ranked precedence order for the six pillars, each rank justified from the master's own wording - rank 1 takes the master's only "does not belong in the game" language, rank 2 its only literal "wins" resolution - and names one conflict the master does not settle (Systems isolate, effects combine vs Failure must teach, on emergent cross-system failures) as an open question rather than resolving it. Doc 02 resolves all six of its Owns entries under their own headings, with A1 and A2 carried as labelled Author Decision subsections |
| 2026-09-18 | P0.4 | Implementer's own self-review caught and corrected a mis-cited Register section: doc 01 had attributed the upgrade-pool dominance measure to Progression & Upgrades when it sits under Economy & Pickups | `docs/01_Design_Pillars.md` | Corrected before hand-back, verified against MASTER_SDLC.md lines 3318 and 3340 |
| 2026-09-18 | P0.4 | Three items the implementer flagged rather than resolved silently, each checked by the orchestrator | - | (1) A genuine gap in the master - the Moment-to-Moment Loop's stated duration has no Register row, unlike the other three scales; the bare figure was omitted rather than given an invented citation. Recorded as LEDGER F01-06, owner the author. (2) Doc 02's A2 section wrote "a comfortable margin" where the master reads "comfortably above half health". The implementer's Register citation was checked and is correct - the "T4 Siege, teaching (max 40 s)" row carries both tuning targets - so only the wording needed fixing. Recorded and fixed as LEDGER F01-07. (3) Several structural-versus-tuned numeral judgment calls, flagged individually in the evidence report; the orchestrator's own audit found only "2D" and doc 01's precedence ranks surviving, both structural |
| 2026-09-18 | P0.4 | Change Log row for MASTER_SDLC.md **proposed, not written**, per Document Control > Gate Approval, which reserves writing an accepted or stable row for the human designer | - | Proposed text recorded in `evidence/p04_report.md`. The implementer proposed both PLAN.md's literal "00/01/02 stable" and a fuller version stating what was actually done, and was explicit that the fuller version proposes recording the self-review, not asserting that the independent review Document Control additionally requires has happened. No row was written into the master |
| 2026-09-18 | P0.6 | Delegated the eleven contract schemas, their samples and the scripted Schema check to a Sonnet implementer, with the conventions fixed in advance (shared structs typed once, exact enum member lists, the `has_<field>` nullable convention, the D87 BandedValue decision) and falsification of the check required | `src/data/*.gd` (54 scripts); `src/data/samples/*.tres` (11) | Delivered. 11 contract resources, 42 struct resources (12 shared per docs/20's shared table, 7 list-of-pairs resources, 23 contract-specific), and `contract_enums.gd` carrying all 28 enums. `TowerUpgradeDefinition extends UpgradeDefinition` rather than copying fields, per the convention. Samples were generated through Godot's own `ResourceSaver` so the `.tres` syntax is exactly what 4.7.1 emits |
| 2026-09-18 | P0.6 | Schema check (Acceptance Test Matrix > Build Checks), re-run by the orchestrator rather than taken from the implementer's report | `Godot_v4.7.1-stable_win64_console.exe --headless --path . --script res://tests/schema_check.gd` | Exit 0. `Pass 1 (script load): PASS (54 scripts under src/data/ loaded and instantiated with zero errors)`, then all 11 contracts PASS individually, `Schema check: PASS (11 contracts validated, 0 problems)` |
| 2026-09-18 | P0.6 | Falsification of the Schema check, four ways, per the carried lesson that a passing check proves nothing until it fails against its own mechanism | `tests/schema_check.gd` | (1) An **inherited** export reset to default (`TowerUpgradeDefinition.effect_per_rank`, chosen as the assertion the implementer was least confident in, since it tests whether the reflection walk reaches ancestor-declared exports) → exit 1, failure named the field. (2) A field pointed at the wrong resource class → exit 1. (3) A sample moved aside with `mv`, not deleted → exit 1, failure named the missing sample by path. (4) A rogue undeclared field added to a sample → exit 0, unchanged, with the implementer's reasoning recorded for why the check should not catch it. Every file restored, confirmed by `diff -rq` against a pristine backup |
| 2026-09-18 | P0.6 | Engine behaviour worth carrying forward, surfaced by falsification 2 | - | Godot 4.7.1 silently nulls a type-mismatched resource property on load rather than raising an error. No engine diagnostic backstops a wrong-class reference in a `.tres`; only the schema check's own null check catches it. Recorded as LEDGER F01-08 |
| 2026-09-18 | P0.6 | Nine contradictions or ambiguities in MASTER or docs/20 flagged rather than silently resolved | `evidence/p06_report.md` | Carried to the review gate for the P0.6 critical agent to weigh in on. Summarised as LEDGER F01-09 |
| 2026-09-18 | P0.7 | Verification export for the export-hygiene fix, which LEDGER F01-05 had left outstanding | `Godot_v4.7.1-stable_win64_console.exe --headless --path . --export-release "Windows Desktop" builds/windows/HordeControl.exe` | Export exit 0. Pack is 96 KB. Searching the pack for `gdUnit4`, `GdUnitCmdTool`, `settings_check`, `test_trivial`, `run_tests` and `reports/` returns **zero matches each**; searching for `boot_check`, `enemy_definition` and `main.tscn` returns 5, 11 and 2 matches, confirming the filter excluded the test surface without over-excluding real project content. F01-05 closed |
| 2026-09-18 | P0.7 | Delegated the gdUnit4 harness to a Sonnet implementer, with the verified command supplied rather than re-derived, and all four exit codes required | - | Delivered. gdUnit4 v6.2.1 copied from the sandbox to `addons/gdUnit4`; `plugin.cfg` read back from the real project confirms 6.2.1. Enabling the plugin proved not to be required and was tested rather than assumed: `GdUnitCmdTool.gd` is a standalone `SceneTree` main-loop script that bypasses the `EditorPlugin`. **`project.godot` was not modified** - confirmed independently by `git diff --stat project.godot` returning empty |
| 2026-09-18 | P0.7 | Test layout: two sibling directories so a run can target either in isolation, the failing suite being a permanent fixture that must never be swept into a default run | `tests/harness/pass/test_trivial_pass.gd`; `tests/harness/fail/test_trivial_fail.gd` | Authored |
| 2026-09-18 | P0.7 | Harness check (Acceptance Test Matrix > Build Checks). Exit codes re-run by the orchestrator rather than taken from the implementer's report | `Godot_v4.7.1-stable_win64_console.exe --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a res://tests/harness/pass (or .../fail) [--ignoreHeadlessMode]` | Three of four reproduced independently and matching the implementer's report exactly: **0** on the passing suite, **100** on the failing suite, **103** on the passing suite with `--ignoreHeadlessMode` omitted. Exit **1** (import pass not run) was **not reproduced** at the time: the `.godot` cache in this repository is already populated, and producing a genuinely un-imported state would need that cache deleted or a second project stood up, both outside the implementer's constraints. **[Corrected 2026-09-18, LEDGER F01-19: the second half of that sentence was false. A reviewer reproduced exit 1 in about two minutes by copying the project to a scratch directory, deleting nothing. The row is left as written with this marker rather than rewritten, so the mistake stays visible.]** Reported as not reproduced rather than filled in - see LEDGER F01-04 |
| 2026-09-18 | P0.7 | Runner script mapping each exit code to a distinct message and script exit status, per NEXT_SESSION.md's requirement that CI distinguish all four | `tests/run_tests.ps1` | Authored; runs the import pass then the suite, and propagates 0/100/103 with an unknown-code fallback to 1 |
| 2026-09-18 | P0.7 | Settings check re-run after the addon was added, to confirm the harness did not disturb the Phase 00 baseline | `tests/settings_check.gd` via `--script` | Still PASS at exit 0, unmodified |
| 2026-09-18 | P0.7 | Export hygiene. The implementer investigated it, correctly declined to put a `.gdignore` inside `addons/gdUnit4/` (it would hide the addon from the engine's own resource scanner and break the harness, unlike `tools/` and `sandbox/` which nothing loads at runtime), and correctly declined to edit `export_presets.cfg` because that file was outside its permitted write scope. The orchestrator applied the fix | `export_presets.cfg`; `.gitignore` | `exclude_filter` set to `addons/gdUnit4/*, tests/*, reports/*`. Also found by the implementer: gdUnit4 writes HTML/XML reports to `res://reports/`, which was neither excluded from exports nor gitignored - `reports/` added to `.gitignore`, and the four report directories generated by the demonstration runs removed with `rm -rf` (generated artefacts, 550 KB, referenced by nothing in the project). Verification export still to run |
| 2026-09-18 | P0.4 | Orchestrator spot-check of the three documents, ahead of the P0.4 review spot-check required by D83 | `docs/00`, `docs/01`, `docs/02` | Numbers audit: the only numerals surviving in all three documents are "2D" in doc 00 and the precedence ranks 1-6 in doc 01's ordering table; both are structural, not gameplay values. **Corrected 2026-09-18 (LEDGER F01-26)**: this row originally said doc 02 "carries none at all", which the phase reviewer falsified - doc 02 quotes the Register row title "T4 Siege, teaching (max 40 s)" and uses T1-T4 throughout. The substance holds, because a figure inside a quoted row title is a citation rather than a restatement, but the original sentence was stated more strongly than the artefact supports. Placeholder lint clean. No banned gate wording ("passed", "satisfied", "ready", "is stable") in any of the three |
| 2026-09-18 | Gate | Convened the Phase 01 review gate at the pace decision D83 fixed in advance: one critical agent for P0.6, one for P0.7, a spot-check for P0.4 (documentation), and one phase reviewer. Each received PLAN.md, LEDGER.md and the artifacts, was told to treat the implementers' evidence reports as claims to verify rather than as evidence, and was instructed to mark every ledger item closed, not closed, or regressed before raising anything new | - | Running |
| 2026-09-18 | P0.7 | Applied decision D86, which the P0.7 delegation could not carry out because its permitted write scope excluded `docs/` - an orchestrator error recorded as LEDGER F01-10 | `docs/28_AI_Development_Workflow.md` | gdUnit4 pinned under Pinned Tool Versions: version 6.2.1, upstream `godot-gdunit-labs/gdUnit4`, 516 files, 2.0 MB, SHA-256 over the sorted file list and contents `5ae43377c9501520d2a6191c59e4e156aa3525ad4d9c69716db62e1d78ab8b9a`. No commit sha exists to record - the addon ships as a release distribution with no `.git` directory - and the document says so rather than leaving the gap implicit. Pin verification commands added, and the export-exclusion measure recorded with an explicit warning that a `.gdignore` must not be used inside the addon. Document raised to 1.1.0 |
| 2026-09-18 | P0.7 | Confirmed the recorded content hash is stable under ordinary use, before relying on it as a pin | a SHA-256 over the sorted file list and contents of `addons/gdUnit4` (command recorded in docs/28) | Identical before and after a full `--import` pass, at 516 files both times. A hash that an import pass mutated would have been useless as a pin, so this was checked rather than assumed |
| 2026-09-18 | P0.7 | **[Corrected 2026-09-18, LEDGER F01-19 and F01-56: the downgrade this row records still rested on an untested reason, and the finding is now simply fixed - exit 1 was reproduced. This row is left as written with this marker, like its sibling, rather than rewritten.]** Re-read docs/28 while writing the pin and found it already answers LEDGER F01-04 | `docs/28_AI_Development_Workflow.md` | Phase 00 had already reproduced exit code 1 directly, on a fresh copy with `.godot/` removed, where the invocation fails with `Parse Error: Could not find type "GdUnitTestCIRunner"`. F01-04 downgraded accordingly: the behaviour is evidenced in the project's own records and the implementer's reasoning for why it cannot recur in this repository is confirmed correct |
| 2026-09-18 | Gate | Review gate iteration 1 returned. Four reviewers, none of whom saw an implementer's reasoning | `evidence/review_p04.md`, `review_p06.md`, `review_p07.md`, `review_phase.md` | P0.4 **7/10** (2 Major), P0.6 **8/10** (2 Major), P0.7 **6/10** (2 Major), phase execution **8/10** (2 Major, no Blocker). Bar not met: it allows no open Major. Eighteen further findings recorded as F01-11 to F01-28 |
| 2026-09-18 | Gate | Reviewer quality, recorded because it is what the gate exists for. The P0.4 reviewer resolved all 20 Register citations rather than the 6-8 asked, confirming every one resolves to a real row with its title matching character for character, and tested both of doc 01's uniqueness claims against the whole master. The P0.6 reviewer checked all eleven contracts field by field and found **zero missing and zero unrequired fields**, all 28 enums exact, no shared struct typed twice. The P0.7 reviewer reproduced all four exit codes, including the one this project had twice failed to. The phase reviewer tested 14 factual claims from the record; 13 reproduced exactly | - | Two of the Major findings are against the orchestrator rather than any implementer (F01-10, F01-11), and a third (F01-19) falsifies reasoning the orchestrator had written into the ledger |
| 2026-09-18 | Iteration 2 | F01-19/F01-04: reproduced gdUnit4 exit code **1**, which two previous attempts had recorded as unreproducible, by the method the ledger itself named and declined to use | `tar` copy of the project into the session scratchpad, then `--path` at the copy | Exit **1**, `SCRIPT ERROR: Parse Error: Could not find type "GdUnitTestCIRunner"`. Nothing deleted, nothing written in the repository, about two minutes. The import pass on the same copy then cures it and the suite exits 0 - which answers the more useful question nobody had asked: a fresh CI checkout does recover |
| 2026-09-18 | Iteration 2 | F01-18: rewrote the runner script, which had been reporting PASS after executing zero tests on two separate paths | `tests/run_tests.ps1` | Three guards added - executable existence, `project.godot` existence, and a zero-test check placed **before** the exit-code switch, because gdUnit4 exits 0 on `No test cases found` - plus `$LASTEXITCODE` cleared before each invocation and null-checked after. Falsified four ways: bad path to exit 1 naming the cause; zero-test path to exit 1; passing suite to 0; failing suite to 100 |
| 2026-09-18 | Iteration 2 | F01-20: closed the two export-hygiene gaps the reviewers found - the `reports/*` filter arm had been verified against an already-deleted directory, and eleven placeholder fixtures were shipping in the pack | `export_presets.cfg` | `src/data/samples/*` added to `exclude_filter`, and the export re-run with `reports/` genuinely populated by the reviewers' own runs, so both arms are now proven rather than vacuous. Pack fell from 96 KB to 72 KB; zero matches for `gdUnit4`, `settings_check`, `test_trivial`, `run_tests`, `reports`, `PLACEHOLDER` and `samples`; `boot_check`, `enemy_definition` and `main.tscn` still present |
| 2026-09-18 | Iteration 2 | F01-25: rebuilt LEDGER.md, whose Status column was missing from every data row - a 7-column header over 6-column rows, so Resolution rendered empty and the column loop rules (c) and (d) operate on did not exist | `LEDGER.md` | All 28 findings now carry a Status, verified mechanically at 7 columns per row. A first attempt put Status and Evidence in each other's positions and was caught by the same check |
| 2026-09-18 | Iteration 2 | F01-15, F01-16 and five reviewer rulings: delegated the P0.6 fix pass to a Sonnet implementer | `tests/schema_check.gd`, `src/data/merge_rule.gd`, `src/data/upgrade_definition.gd`, three samples | The Schema check now carries a hand-written `REQUIRED_FIELD_MANIFEST`, transcribed from MASTER's contract bullets and independent of the scripts, so a schema and its manifest can disagree. `MergeRule.match_radius_px` removed in favour of `get_match_radius_px(economy)`, making the 71-versus-64 drift structurally impossible. Array validation now recurses into every element, exceptions are keyed by class and field so a populated band label can never fail, and domain assertions were added for the six fields docs/20 actually bounds |
| 2026-09-18 | Iteration 2 | Verified the manifest fix independently, on a field the fix implementer had not used | Deleted `magnet_radius_px` from `src/data/player_definition.gd`, re-ran the check, restored from backup, confirmed with `diff` | Exit **1**: `Player Definition: MASTER field "Magnet radius." has no matching @export "magnet_radius_px" on the schema (REQUIRED_FIELD_MANIFEST)`. Before this fix the same deletion left the check green, because it derived its field list from the very script it was checking |
| 2026-09-18 | Iteration 2 | Applied four author decisions taken on the iteration 1 findings | `CLAUDE.md`, `MASTER_SDLC.md`, `docs/20`, `docs/01`, `docs/02`, `src/data/telegraph_data.gd` | D88 records doc 01's pillar precedence order with its alternatives; D89 keeps the XP level-cost coefficients as authored data; D90 adds a lead-time field to the shared Telegraph data struct, distinct from wind-up duration, so the Encounter contract's "with lead time" has somewhere to live; D91 adds a single structural-count carve-out to CLAUDE.md's numbers rule, with a tie-break clause so it cannot be stretched. docs/20's Contract Field Semantics updated for D89 and D90; schema check re-run after the new field and its samples, exit 0 |
| 2026-09-18 | Iteration 2 | F01-13 and F01-14: fixed the circularity in doc 01's central deliverable, and the five fidelity losses | `docs/01`, `docs/02` | Doc 01's precedence preamble now separates the Central Tension as a binary **gate** from the table as a **comparative** ranking, so rank 1 decides which of two acceptable features sharpens the tension more, instead of being excluded by the table's own entry condition. Doc 02's biome count now cites the Register instead of stating it bare; the four-scales section no longer claims all four durations are reference frames, since the master exempts only Session Shape's ranges and states wave length as a Provisional Default; the onboarding window and the A1 compression magnitude now name what they defer; doc 01's safe corner restores "intent mix", the mechanism the master singles out |
| 2026-09-18 | Iteration 2 | F01-24: the master's "Phase 0 accepted" Change Log row is **deliberately not proposed**, and this row records why - which the gate requires and iteration 1 omitted | - | Phase 01 executes only P0.4, P0.6 and P0.7. Phase 0 in the master's Development Phase Map also contains P0.1, P0.2, P0.3, P0.5, E0.1 and E0.2, all executed by Phase 00 - whose own review scored it 6/10 against a bar of 8 with four of six tasks below the task bar, and whose closure is an open decision reserved to the designer under the Gate Approval rule. Proposing a "Phase 0 accepted" row now would presume that decision. The row an agent may legitimately propose is P0.4's document-stability row, whose text is in `evidence/p04_report.md`, proposed and not written |
| 2026-09-18 | Iteration 2 | Review gate iteration 2 returned for P0.4, P0.6 and P0.7 | `evidence/review_p04_iter2.md`, `review_p06_iter2.md`, `review_p07_iter2.md` | P0.4 **7/10** (1 Major carried, 1 Major newly introduced by a fix), P0.6 **9/10** (both Majors closed, 4 Minor), P0.7 **7/10** (1 new Major). Every Major raised at iteration 2 was against the orchestrator's own work, not an implementer's |
| 2026-09-18 | Iteration 2 | F01-36: the zero-test guard added earlier this iteration was written as a **negative** match on captured stdout, and a reviewer showed it fails open - with stdout lost, the marker is absent, the exit code is 0, and the script reported PASS for a run that executed nothing. Iteration 1 had prescribed the positive form and the negative form was implemented instead | `tests/run_tests.ps1` | Rewritten as a positive assertion on gdUnit4's own `Executed test cases : (n/n)` summary, so a missing or unreadable output is never trusted. Falsified against both of the reviewer's reproductions - a silent executable that exits 0, and an unwritable TEMP with a typo'd test path - both of which previously printed PASS and now exit 1 naming the cause. Regression: pass 0 (and now reports the executed count), fail 100, zero-test path 1, empty test path 1, empty executable path 1 |
| 2026-09-18 | Iteration 2 | F01-23: the `runtest.cmd` warning in docs/28 was proven backwards by running the wrapper rather than reading it | `docs/28_AI_Development_Workflow.md` | The wrapper passes neither `--headless` nor `--ignoreHeadlessMode`, so it never reaches exit 103 - it opens a window, initialises Vulkan, runs the suite and exits 0. Exit 103 arises only when `--headless` is passed *without* the flag. The advice not to use it for CI is unchanged; its stated cause is corrected, and the document now says the original reason was wrong rather than quietly replacing it |
| 2026-09-18 | Iteration 2 | F01-38: docs/28 handed later phases the raw gdUnit4 command line and mentioned the guarded runner nowhere | `docs/28_AI_Development_Workflow.md` | docs/28 now directs later phases to `tests/run_tests.ps1`, and says why the raw command is not sufficient on its own: a bare exit 0 from it does not mean any test executed |
| 2026-09-18 | Iteration 2 | F01-31: extended the Schema check's manifest one level down, to the interiors of the struct resources, where the re-reviewer had demonstrated the closed Major still surviving | `tests/schema_check.gd` | A `STRUCT_REQUIRED_FIELD_MANIFEST` keyed by struct class name, transcribed from docs/20's field semantics rather than from the scripts, hooked into the existing validation recursion so it is exercised at every depth and array element. 41 of 42 struct classes mapped; `BandedValue` deliberately left unmapped because docs/20 never states a combined struct shape for it, and mapping it would mean deriving the manifest from the script - the exact defect being fixed. Pure addition: +330 lines, 0 removed |
| 2026-09-18 | Iteration 2 | Verified the struct manifest independently, on an array-nested field the fix implementer had not used | Deleted `SpawnGroup.spawn_interval_seconds`, re-ran, restored from backup, confirmed with `diff` | Exit **1**: `Encounter Definition.spawn_groups[0] (SpawnGroup): docs/20 field "Spawn group struct (Shared fields and struct types): spawn interval." has no matching @export "spawn_interval_seconds"`. It names the docs/20 wording, the array index and the struct class, and reaches through an array into a nested struct |
| 2026-09-18 | Iteration 2 | Applied the phase reviewer's remediation list in full | `MASTER_SDLC.md`, `CLAUDE.md`, `NEXT_SESSION.md`, `phases/README.md`, `phases/LESSONS.md`, `LEDGER.md` | D92 and D93 recorded with their alternatives and the master raised to 0.8.5; the five author-owned ledger rows carried into `NEXT_SESSION.md` where the author actually looks; F01-02's absolute negative claim replaced with what is established plus the untested PreToolUse-hook option it had dismissed; README row 01 stopped assessing the phase's own gate; the LESSONS trailer corrected and the structural-check lesson widened from column count to row position, column count and column identity |
| 2026-09-18 | Iteration 2 | F01-47, raised by the orchestrator against itself: the ledger-integrity check it had run and twice reported to the author as evidence the bar was clear was reading the wrong column, and could not have failed | - | Rewritten to derive every column index from the header row by name, then **falsified against a planted open Major** before being believed. The corrected check reports 47 findings, 16 Major, 31 Minor, no row above the header, no wrong column count, no invalid status cell, and no open Blocker or Major. A related overstatement was corrected the same way: "ten of sixteen Majors are the orchestrator's" came from a keyword search that confused rows it fixed with rows it caused; the true figure is nine, now listed individually |
| 2026-09-18 | Close | Full regression on the frozen tree, with no agent running | `settings_check.gd`, `schema_check.gd`, both gdUnit4 suites | Settings check 0, Schema check 0, harness pass suite 0, harness fail suite 100. Both named exit tests in PLAN.md > Exit criteria reproduce |
| 2026-09-18 | Iteration 3 | **Froze the tree before opening the gate**, which is the one condition no previous iteration met and the substance of F01-41. The working tree is committed and clean; reviewers are pointed at the tagged commit and told to report any dirty state as a finding rather than review around it. The orchestrator makes no edit of any kind until all four reviewers have reported | git tag `phase01-review-iter3` | Iteration 3 convened at the same four-reviewer pace D83 fixes. Each reviewer receives its iteration 2 predecessor's report as the standard to check against |
| 2026-09-18 | Iteration 3 | Review gate iteration 3 returned, on the frozen tree | `evidence/review_p04_iter3.md`, `review_p06_iter3.md`, `review_p07_iter3.md`, `review_phase_iter3.md` | P0.4 **8/10**, P0.6 **9/10**, P0.7 **8/10**, phase execution **8/10**. Every reviewer independently confirmed the freeze held: HEAD unmoved at `8145333` across seventeen sampled checks by the phase reviewer alone, and no tracked file changed for any of them |
| 2026-09-18 | Iteration 3 | Author consulted on decision D92, whose label the phase reviewer found unsupported | - | **D92 had been labelled "Author decision" and was never put to the author** - the orchestrator wrote it in response to a reviewer finding and applied the label reflexively. The author has now chosen to keep the substance with the label corrected to an orchestrator decision open to review. Recorded as LEDGER F01-51 |
| 2026-09-18 | Iteration 3 | Reconstructed the consultation record the phase reviewer found missing | - | **D93 was genuinely taken by the author**, in the same question batch that produced D88-D91: the question asked whether to propose the "Phase 0 accepted" row or record why it is withheld, and the author chose to record the reason. **D92 had no consultation at all** and the log now says so rather than leaving a reader unable to tell which decisions were consulted. D84-D87 and D88-D91 were already logged |
| 2026-09-18 | Iteration 3 | F01-52: scoped the runner's executed-count guard to the exit-0 case, restoring the four-way distinction CI depends on | `tests/run_tests.ps1` | The guard had sat before the exit-code switch, and neither a real 103 nor a real exit 1 prints the summary it required, so both were relayed as 1 with the wrong cause. Falsified with shims that pass the import stage then return a chosen suite code: 103 relays 103, 1 relays 1, real pass 0, real fail 100. F01-36's fail-closed behaviour verified intact |
| 2026-09-18 | Iteration 3 | F01-53: removed the system temp file from the runner's output path | `tests/run_tests.ps1` | Suite output is captured in memory. An unwritable TEMP had turned a genuinely passing suite into a reported failure and discarded every diagnostic line with it; that case now exits 0, and a typo'd test path names the typo instead of reciting three causes that did not occur |
| 2026-09-18 | Iteration 3 | F01-48: mapped `BandedValue` in the struct manifest, from decision D87 rather than from the script | `tests/schema_check.gd` | The exemption had rested on the premise that no document of record writes the struct's shape; D87 writes it in full. Falsified on both cases the reviewer had shown passing at exit 0 - deleting the value field, and deleting the band-label pair from the script alone - each now fails naming D87 and every contract carrying the struct. The failure-message template changed from "docs/20 field" to "required field", since the manifest's sources are not all docs/20 |
| 2026-09-18 | Iteration 3 | F01-50, F01-54, F01-56: corrected the inferred-leg count in three places, repaired the LESSONS table, and cleared four smaller record defects | `docs/01`, `MASTER_SDLC.md`, `phases/LESSONS.md`, `LEDGER.md` | Document 01, D92 and the 0.8.5 Change Log row now all say three legs go beyond the master's wording rather than two. LESSONS.md's table had been split by blank lines so nine rows rendered detached from the header - the third recurrence of that defect class, in the file holding the lesson about it. The 0.8.5 row now names D93, and D93's reason no longer uses the banned word |
