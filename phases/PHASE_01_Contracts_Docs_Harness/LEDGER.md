# Phase 01 - Ledger

Every finding raised against this phase, carried across review iterations.

Severities: Blocker, Major, Minor. Statuses: open, fixed, closed, deferred with owner, recorded, withdrawn. "Recorded" is used for observations that are neither defects to fix nor claims to close - a tested fact about the engine, or a process failure whose remedy is a rule rather than an edit.

Iteration 1 of the review gate raised findings from four reviewers: a P0.6 critical agent, a P0.7 critical agent, a P0.4 spot-check, and a phase reviewer. Each reviewer's own finding ID is kept in the Finding column so its report can be traced.

| ID | Finding | Severity | Raised by | Status | Evidence | Resolution |
| --- | --- | --- | --- | --- | --- | --- |
| F01-01 | `.claude/settings.local.json` carried `allow` entries for all six tools that `.claude/settings.json` gates or denies, contradicting CLAUDE.md and decision D81. Four were inert - `deny` wins even from the lower-precedence file - but `delete_file` and `export_project` sit on `ask`, and a higher-precedence local `allow` overrode that gate, so both ran unprompted in the main session too, not only in subagents. Strictly wider than F-06b | Major | Orchestrator, at phase entry | fixed, verification outstanding | The four denied tools are absent from this session's tool surface while `delete_file` and `export_project` are present; the F-06b probe then executed `delete_file` with no prompt, verified by read-back | Six `allow` entries stripped under decision D84; the file now carries an empty allow list, previous contents kept in the session scratchpad. The running session's tool surface was fixed at startup, so that the two `ask` tools prompt again is **not yet demonstrated**. Re-check at the next session start before closing |
| F01-02 | F-06b, carried from Phase 00, is half closed and half confirmed open. Closed: `GODOT_MCP_ALLOWED_DIRS` does confine `run_project`, and through it the whole `game_*` surface, to the sandbox. Open: the `ask` gate still does not intercept a subagent | Major | Phase 00 (F-06b), re-tested at entry | deferred, owner the author | Probe 1 refused `D:/Gamedev` with `Project path is outside the allowed roots`; probe 2 allowed the sandbox; probe 3 deleted the probe file unprompted, confirmed gone by `ls` and `stat` rather than by the tool's success message | The closed half settles NEXT_SESSION.md's "verify it actually refuses D:/Gamedev" in the affirmative. The open half is mitigated only by instruction: every delegation prompt this phase carried the no-delete, no-export constraint explicitly. Not this phase's to close **Deferred rather than left open.** The wording here previously read "there is no fix available from this project's side", which is an absolute negative claim with no experiment behind it - the same defect this phase recorded three times against others, written into the row that argues the phase meets its bar. Corrected: what is *established* is that `deny` is the only setting in the permission system's own vocabulary that intercepts a subagent, which Phase 00's F-06 and this phase's probe both show. What was never tested is whether a **PreToolUse hook** could gate these two tools for subagents, since a hook runs in the harness rather than in the permission layer. That is a real candidate and it is untested, so it is named here as work rather than dismissed. The delegated research and Phase 00's own F-06 both found `deny` is the only control that reliably intercepts a subagent, and the author has twice decided against promoting these two tools to `deny` - at D81, to keep the export path P0.2 and P4.5.3 depend on, and again at D84. The residual exposure is therefore an accepted risk with a named owner, not an unresolved defect this phase can work on. Re-test at each phase entry, as this phase did.  |
| F01-03 | The `godot-prompter:godot-testing` skill conflicts with this project twice: it documents gdUnit4's CLI as `--add-gdunit-test-runner` or the editor dock, neither of which exists in v6.2.1 and neither of which mentions `--ignoreHeadlessMode`; and it recommends GUT for GDScript-only projects where P0.7 specifies gdUnit4 | Minor | Orchestrator, on invoking the skill before P0.7 | closed as a record | The P0.7 critical agent verified against the installed addon: no `GdUnitRunner.gd`, no `--testsuites`, no `--add-gdunit-test-runner` anywhere; `--ignoreHeadlessMode` is real in `GdUnitTestCIRunner.gd` | Not fixed - the skill is third-party and is not edited. Per CLAUDE.md the project wins and the conflict is recorded here. Same defect as Phase 00's F-11, confirmed still present in pack v1.13.3. The P0.7 implementer was handed the verified command and told not to re-derive it |
| F01-04 | gdUnit4 exit code **1** ("import pass not run") was not reproduced, while 0, 100 and 103 were reproduced twice over. NEXT_SESSION.md requires CI to distinguish all four | Minor | P0.7 implementer, self-reported | fixed | Reproduced 2026-09-18 by `tar`-copying the project to the session scratchpad and pointing `--path` at the copy: exit **1**, `SCRIPT ERROR: Parse Error: Could not find type "GdUnitTestCIRunner"`. The import pass then cures it - the suite exits 0 on the same copy | **The ledger's own earlier reasoning was wrong and is corrected here.** It claimed exit 1 "cannot occur here without deleting that cache or standing up a second project", then compounded it with "would require breaking the very cache the project needs". The P0.7 critical agent falsified both in under two minutes, deleting nothing. See F01-19 |
| F01-05 | gdUnit4 writes reports to `res://reports/` on every headless run; that directory was neither export-excluded nor gitignored. `addons/gdUnit4` (~2 MB) and `tests/` were also unexcluded against a preset whose `export_filter` is `all_resources` | Major | P0.7 implementer, beyond its brief | closed | Phase 00's F-05 precedent: the first Windows export packed 11.2 MB of MCP server into a build of an empty scene | Fixed by the orchestrator (outside the implementer's write scope). `exclude_filter` now covers `addons/gdUnit4/*, tests/*, reports/*, src/data/samples/*`; `reports/` gitignored. A `.gdignore` was deliberately not used - inside the addon it would hide it from the resource scanner and break the harness. Verified by export: 72 KB pack, zero matches for every excluded path, `boot_check`/`enemy_definition`/`main.tscn` still present |
| F01-06 | A gap in the master: the Moment-to-Moment Loop is the only one of the four loop scales whose stated duration has no Provisional Values Register row | Minor | P0.4 implementer | open, owner the author | MASTER_SDLC.md § Core Gameplay Loop against the Register | Handled correctly for this phase - the bare figure was omitted from doc 02 rather than given an invented citation. The choice between adding a Register row and declaring it a reference frame is the author's |
| F01-07 | Doc 02's Author Decision A2 wrote "a comfortable margin" where the master reads "comfortably above half health", dropping which quantity the margin is in | Minor | P0.4 implementer, self-flagged | closed | MASTER_SDLC.md line 1055 against doc 02 | Fixed: now reads "a comfortable health margin" and names both deferred tuning targets. The P0.4 spot-check reviewer independently judged the fix adequate and confirmed the Register citation resolves - that row carries both targets |
| F01-08 | Godot 4.7.1 silently nulls a type-mismatched resource property when loading a `.tres`. No engine error, no warning, no diagnostic on stdout or stderr | Minor | P0.6 implementer, by experiment | closed as a standing fact | Falsification run 2; independently reproduced by the P0.6 critical agent, which confirmed zero engine diagnostics | Not a defect to fix. It raises the Schema check's value considerably: it is the only thing between a mistyped `.tres` reference and a silently null field at runtime. Phases 02+ must assume no engine help here. Belongs in LESSONS.md at phase close |
| F01-09 | Nine contradictions or ambiguities between MASTER's contracts and docs/20's field semantics, flagged by the implementer rather than silently resolved | Minor | P0.6 implementer | closed | `evidence/p06_report.md` | The P0.6 critical agent settled six as reviewer (#2, #3, #5, #6, #7, #8). #1 became F01-17's R7 ruling. #4 (XP cost-formula coefficients) and #9's rogue-field half are carried below, together with a tenth the implementer missed (R12: docs/20's "Telegraph requirements ... with lead time" has nowhere to live, since `TelegraphData` has no lead-time field) Settled by decisions D89 and D90: the XP level-cost coefficients stay authored as data, and the shared Telegraph data struct gains a lead-time field so the Encounter contract's requirement has somewhere to live. docs/20's Contract Field Semantics updated for both. |
| F01-10 | Orchestrator error, not an implementer defect: decision D86 requires the gdUnit4 pin in docs/28, but the P0.7 delegation's write scope excluded `docs/`, so the implementer was structurally unable to satisfy a phase exit condition, and nothing in its brief would have revealed that | Major | Orchestrator, on writing the review-gate prompts | closed on substance | The P0.7 delegation's constraint list against decision D86 | docs/28 now pins gdUnit4 (v6.2.1, upstream org, 516 files, 2.0 MB, SHA-256 `5ae43377...8b9a`), with verification commands and the export-exclusion note, raised to 1.1.0. No commit sha exists - the addon ships as a release distribution - and the document says so rather than leaving the gap implicit. The hash was confirmed stable across an `--import` pass before being recorded. The P0.7 reviewer verified the final state independently and called the content-hash choice better than D86 asked for. One element remains: see F01-21. **Preventive rule: when a decision lands mid-phase, re-check already-written delegation scopes against it before dispatch** |
| F01-11 | A rule that does not exist warranted four exemptions from the task's top-risk rule. `evidence/p04_report.md` cites CLAUDE.md three times for a "structural count" carve-out from the numbers rule, quoting "three biomes" and "four scales" as CLAUDE.md's own worked examples. CLAUDE.md contains no such examples, no carve-out, and no worked example of anything | Major | P0.4 spot-check | fixed | Confirmed by grep: zero occurrences of "three biomes", "four scales" or "structural" in CLAUDE.md | **Root cause is the orchestrator's.** Those examples came from the P0.4 delegation prompt, which offered them as acceptable non-gameplay numbers; the implementer adopted the carve-out reasonably and then mis-attributed it to CLAUDE.md, which is its own error. It did not reach the artifacts - the reviewer confirmed three of the four exemptions stand on the master's verbatim wording. Iteration 2 narrowed it further: at least one count first listed as exempted, document 01's own pillar count, was never a gameplay number and needed no carve-out, so both this row's original framing and D91's reason overstated how many cases the rule has to cover. Corrected in both. Whether a structural-count carve-out should exist at all governs every later document Settled by decision D91: CLAUDE.md's numbers rule gains one carve-out for counts the master itself states as structure, with a tie-break clause sending arguable counts to the Register. The three weaker instances in doc 02 were rewritten to cite the Register. |
| F01-12 | Doc 01 asserts binding new design with no Review Decision Log row: the six-rank pillar precedence order, which the document itself calls "binding", and the anti-pattern-to-pillar enforcement mapping are both absent from the master and from the Decision Log. CLAUDE.md requires a row naming the alternative | Major | P0.4 spot-check | fixed | docs/01 against MASTER_SDLC.md § Review Decision Log | The counter-argument is real and the reviewer raised it: Documentation Structure assigns doc 01 exactly this question, so producing an answer may be remit rather than change. As the record stands the author cannot make that call, which is itself the defect Settled by decision D88, which records the order, names the alternatives considered, and cites the master wording each rank rests on, so the author can reverse it deliberately. Doc 01 now cites D88 at the table. **Iteration 2 found this only half closed**: D88 covered the precedence order but said nothing about the second half of the finding, the anti-pattern-to-pillar enforcement mapping, which document 01 still asserted declaratively with no row behind it - and at least one leg of which the master does not support. Closed by decision D92, which records the mapping and names its alternatives, and by document 01 now stating that the mapping is its own work and flagging the two legs that are inferred rather than quoted.  |
| F01-13 | Doc 01's precedence table is circular at rank 1, in the document's central deliverable: the preamble scopes the table to conflicts where "the Central Tension test above does not by itself settle the question", yet rank 1 **is** the Central Tension, so rank 1 can never decide a conflict the table is consulted for | Minor | P0.4 spot-check | fixed | docs/01 § "Precedence: Which Pillar Wins" | Fixable without changing the order: the Central Tension is a binary gate (a violating feature does not ship) while the table is comparative (between two features that both pass the gate, which better serves each pillar). Separating gate from ranking removes the circularity and leaves the ordering intact Doc 01's preamble now separates the Central Tension as a binary gate, applied first, from the table as a comparative ranking applied second between two features that both pass that gate. Rank 1 decides which of two acceptable features sharpens the tension more. The order itself is unchanged. |
| F01-14 | Five smaller P0.4 fidelity losses: "three biomes" stated bare in doc 02 where the master tags it a Provisional Default; doc 02 calls all four loop-scale durations reference frames, though the Wave Loop's range mirrors a figure the master explicitly tags "(Provisional Default)"; "first ninety seconds" softened to "the opening seconds" with nothing named as deferred; doc 01's safe corner omits "intent mix", the mechanism the master singles out; and A1 renders "halves T1 and T2" as "compresses" without signalling a deferred magnitude | Minor | P0.4 spot-check | fixed | docs/01, docs/02 against MASTER_SDLC.md and the Register | All five are wording fixes that name what is being deferred instead of silently dropping it. The last is F01-07's class one notch milder All five rewritten to name what they defer: the biome count and the onboarding window now cite their Register rows, the four-scales section states which figures are reference frames and which are Provisional Defaults instead of flattening all four, A1 records that a fixed proportion is deferred, and doc 01's safe corner restores 'intent mix'. |
| F01-15 | The Schema check cannot detect a required field the schema never declared. It derives its field list from the script's own `get_property_list()`, so deleting a field removes it from script and sample simultaneously and the check stays green. It enforces "no declared field is left at its default"; MASTER's criterion is "no missing required field". It also performs no type comparison against docs/20, though the phase's own top-risk row lists it as a type-mismatch detector | Major | P0.6 critical agent | fixed in part; see F01-31 and F01-32 | grep evidence in `evidence/review_p06.md`: the only field-name literals in the check are three exception tables plus three contract-specific assertions | The schemas are complete today, but that was established by the reviewer's manual pass, not by the instrument. Fix delegated: an explicit hand-written manifest of MASTER's required field names per contract, independent of the scripts so the two can disagree, plus falsification proving a deleted `@export` now fails the check `tests/schema_check.gd` now carries a hand-written `REQUIRED_FIELD_MANIFEST` transcribed from MASTER's contract bullets and independent of the scripts, checked before the value walk. Falsified by the implementer on two contracts including an inherited field, and independently by the orchestrator on a third field the implementer had not used: deleting `magnet_radius_px` now fails with the MASTER field name and the missing export named, where before it left the check green. **Corrected at iteration 2.** This row previously read simply 'fixed', which overstated it in two ways the re-reviewer demonstrated. The finding had two sentences, and only the first was addressed: the manifest closes the missing-field half at the contract level, but the second sentence - that the check performs no type comparison against docs/20 - is untouched, and retyping a field from int to float against docs/20's stated integer still leaves the check green (F01-32). Separately, the manifest asserts only the wrapper of MASTER's compound bullets, so the same defect survives inside the 42 struct resources and was demonstrated twice (F01-31). What is genuinely closed is real and was verified by execution on four contract fields nobody had used, plus the one derived field: the reviewer confirmed the manifest is a perfect one-to-one against MASTER's bullets for all eleven contracts, omitting nothing. |
| F01-16 | `MergeRule.match_radius_px` duplicates a Register-owned number that docs/20 types as a **reference** to Economy Configuration's Merge radius field, and the shipped samples already disagree: `merge_radius_px = 71` against `match_radius_px = 64`. A per-pickup copy also puts the value beyond the documented Fallback-Ladder escalation that raises it at runtime | Major | P0.6 critical agent | fixed | docs/20's Pickup field table; the two sample files; MASTER line 1401 | Independently confirmed by the orchestrator. Fix delegated: remove the authorable export and resolve the value from the Economy Configuration through a method, following the `WaveDefinition.get_spawn_budget()` precedent the reviewer approved for derived values The authorable export is gone, replaced by `get_match_radius_px(economy)` on the `WaveDefinition.get_spawn_budget()` precedent. Only one `merge_radius_px` now exists in the whole sample set - Economy's own - so the 71-versus-64 drift is structurally impossible rather than merely corrected. |
| F01-17 | Seven smaller P0.6 findings and rulings: `MergeRule` drops two of docs/20's four sub-fields into a comment while the identical `HopperConversionRule.trigger` is exported; only array element `[0]` is validated (falsified - a blanked second intent-mix entry leaves exit 0); a rogue `.tres` key is accepted silently by both engine and check; `hurtbox_definition` is absent from the player sample and exempted by name; `recursive_interaction_guard` uses an empty-string sentinel that collides with its own default, so "no guard deliberately" and "never filled in" are the same bytes; exception tables keyed by bare field name will start **failing correct content** when document 12 populates band labels at the vertical slice; and there is no value-domain enforcement (`entity_cap_weight` defaults to 0 where docs/20 says "≥ 1") | Minor | P0.6 critical agent | fixed, two deliberately not | `evidence/review_p06.md`, with four independent falsifications | Five delegated as code fixes per the reviewer's explicit rulings. Two are deliberately **not** fixed: the reviewer ruled the rogue-field case a known limitation to own with a named owner rather than a resolved question, and a single-member enum has no possible non-default value to set Five fixed per the reviewer's rulings: the two comment-only sub-fields exported as Strings, array validation recursing into every element, `has_recursive_interaction_guard` added so a deliberate absence is distinguishable from an unfilled field, exceptions re-keyed by class and field so a populated band label can never fail, and domain assertions for the six fields docs/20 actually bounds. Two deliberately unfixed, as the reviewer ruled: the rogue-extra-field case is a known limitation to own, and a single-member enum has no non-default value to set. |
| F01-18 | `tests/run_tests.ps1` reported **PASS after executing zero tests**, on two reproduced paths: a missing or wrong `-GodotPath` raises a non-terminating `CommandNotFoundException` in PowerShell, leaving a stale `$LASTEXITCODE` of 0 that both guards misread as success; and gdUnit4 itself exits **0** on `No test cases found, abort test run!`, which the script mapped to PASS - while its own comment tells later phases to widen `-TestPath`, so a typo would yield a permanent green | Major | P0.7 critical agent | fixed | Both paths reproduced verbatim by the reviewer; the zero-test exit 0 independently reproduced by the orchestrator against `res://src/core` | Carried Lesson 1 applied thoroughly to P0.6's Schema check and not at all to P0.7's runner: the engine exit codes were falsified, the script consuming them never was. Rewritten with three guards - executable existence, `project.godot` existence, and a zero-test check that runs **before** the exit-code switch - plus `$LASTEXITCODE` cleared before each invocation and a null check after. Falsified: bad path → exit 1 with a named cause; zero-test path → exit 1; pass → 0; fail → 100 |
| F01-19 | F01-04's stated ground was false. The ledger claimed exit 1 "cannot occur here without deleting that cache or standing up a second project"; the reviewer reproduced it in under two minutes with a scratchpad copy, deleting nothing and writing nothing in the repository. The ledger itself named the right method and declined to use it | Major | P0.7 critical agent | fixed | Reproduced again by the orchestrator: fresh copy → exit 1 with the expected parse error; import pass → suite exits 0 | Carried Lesson 7 - never write a cause without an experiment - recurring inside the phase that carried it forward. The fault is the orchestrator's reasoning, not the implementer's report, which correctly said "not reproduced" rather than fabricating. F01-04 corrected. The more useful question the reviewer asked and answered: a fresh CI checkout does recover, because the import pass cures it |
| F01-20 | The `reports/*` filter arm was verified against a directory that had already been deleted, so it proved nothing; and eleven `PLACEHOLDER_` sample fixtures were shipping in the release pack | Minor | P0.7 and P0.6 critical agents | closed | The P0.7 reviewer read the pack's own file index rather than the orchestrator's log; the orchestrator confirmed 43 `PLACEHOLDER` matches in the pack | `src/data/samples/*` added to `exclude_filter`, and the export re-run with `reports/` genuinely populated by the reviewers' own runs - so both arms are now proven rather than vacuous. Pack fell from 96 KB to 72 KB; zero matches for `gdUnit4`, `settings_check`, `test_trivial`, `run_tests`, `reports`, `PLACEHOLDER` and `samples`, with real content still present |
| F01-21 | D86 and versioning: docs/28 was raised to 1.1.0 but **has no Change Log section at all**, and no `docs/` file does; the master's Change Log gained no row either, so half of D86 is undischarged. Separately, D84-D87 were added to the master with no version bump and no Change Log row - the one Phase 00 pattern that recurred. Also unrecorded: gdUnit4's licence, though the addon is now vendored into the repository | Minor | P0.7 critical agent; phase reviewer | fixed in part | `evidence/review_p07.md`, `evidence/review_phase.md` | The master's Change Log row may legitimately defer to phase close under loop rule (e), which folds it into the close step - but that has to be said rather than assumed, which is what this row does **Updated at iteration 2**: this row's text is now out of date and is corrected here. The master's Change Log row exists (0.8.4, naming document 28's 1.1.0 bump), the master's version was bumped, and gdUnit4's MIT licence is recorded in document 28 against the vendored `addons/gdUnit4/LICENSE`. What remains open is narrower and is a real question rather than an omission: no document under `docs/` has a Change Log section at all, so D86's "a Change Log row" had nowhere of its own to go and was satisfied in the master instead. Whether working documents should carry their own Change Logs is the author's call.  |
| F01-22 | The deliberately-failing test fixture is guarded from a default or CI run only by one script default and a comment, not structurally | Minor | P0.7 critical agent | open | `tests/harness/fail/`, `tests/run_tests.ps1` | Lower risk now that F01-18's zero-test guard makes widening `-TestPath` safe, but the fixture still fails any run that sweeps all of `res://tests` |
| F01-23 | docs/28's `runtest.cmd` warning states an untested and probably-backwards cause: its test-run line passes no `--headless` at all, so it would run windowed rather than exit 103. The reviewer did not test it and said so | Minor | P0.7 critical agent | fixed | docs/28 against `addons/gdUnit4/runtest.cmd` | Another cause asserted without an experiment, this one inherited from Phase 00's document. Either test it or reword it to say only what is known **Closed at iteration 2, and the claim was worse than suspected**: the reviewer ran the wrapper instead of reading it and found the stated cause is the opposite of the truth. It omits `--headless`, so it never reaches exit 103 - it runs windowed and exits 0. Exit 103 comes only from `--headless` without `--ignoreHeadlessMode`. docs/28 now records that its original reason was wrong, rather than quietly substituting a new one. Third recurrence in this phase of the rule about untested causes.  |
| F01-24 | The master's "Phase 0 accepted" row was never **proposed** to the author, and no reason for withholding it is recorded. The row that was proposed, `00/01/02 stable`, is P0.4's document-stability row, not the Phase 0 gate row. The prohibition half holds - no gate row was written - but the gate requires the proposal | Major | Phase reviewer | recorded | Repo-wide grep finds "Phase 0 accepted" only in statements of the requirement, never as a proposal | Phase 01 covers P0.4, P0.6 and P0.7; Phase 0 in the master's Development Phase Map also contains P0.1, P0.2, P0.3, P0.5, E0.1 and E0.2, which Phase 00 executed and whose own review scored 6/10 against a bar of 8. Whether a "Phase 0 accepted" row can be proposed at all while Phase 00's closure is itself undecided is the question that needs recording The row is deliberately not proposed, and EXECUTION_LOG.md now records why: Phase 01 executes only P0.4, P0.6 and P0.7, while Phase 0 also contains the six tasks Phase 00 executed, and Phase 00's own closure is an open decision reserved to the designer. Proposing Phase 0 acceptance now would presume that decision. |
| F01-25 | LEDGER.md's **Status column was missing from all ten data rows**: a 7-column header over 6-column rows. Rendered, Evidence landed under Status, Resolution under Evidence, and Resolution rendered empty. Loop rules (c) and (d) both operate on that column. Same defect class as Phase 00's F-01 | Major | Phase reviewer | fixed | Verified mechanically with awk: header 7 columns, every data row 6 | This file rebuilt with the Status column present and populated on every row. `EXECUTION_LOG.md` and `FAILURE_POINTS.md` were checked the same way by the reviewer and are well-formed |
| F01-26 | Overclaim in the record: EXECUTION_LOG's P0.4 spot-check row said doc 02 carries numerals "none at all". Doc 02 carries `"T4 Siege, teaching (max 40 s)"` and uses T1-T4 throughout | Minor | Phase reviewer | fixed | docs/02 line 107 | The substance survives - the figure sits inside a quoted Register row title, which is a citation rather than a restatement - but the sentence was stated more strongly than the artifact supports. Corrected in EXECUTION_LOG.md. This was the only overclaim the phase reviewer found among 14 tested factual claims |
| F01-27 | Implementation continued after the review gate was convened: three further P0.7 rows follow the gate-convened row, and the reviewer watched docs/28 and four sample files change on disk while its review was open. Reviewers briefed at gate-open were briefed on a different tree | Minor | Phase reviewer | recorded | EXECUTION_LOG.md ordering; the reviewer's own observed mtimes | Logged in the open, unlike Phase 00's unrecorded deviation, and the P0.7 reviewer independently re-verified the changed artefact and reported the timing itself. The rule it argues for - freeze the tree at gate-open, or re-run the sweep and tell reviewers it moved - applies to iteration 2 |
| F01-28 | PLAN.md steps P0.4/10 and P0.6/10 require their output "in EXECUTION_LOG.md"; both were written to `evidence/*.md` instead. An unrecorded narrowing | Minor | Phase reviewer | recorded | PLAN.md against the artefacts | Deliberate - three implementers writing concurrently to one log file would have raced, and the evidence files are referenced from the log. But the plan said one thing and the phase did another without saying so, which is the finding |
| F01-29 | A fix introduced a broken citation, in the one place the phase could least afford it. Doc 02's rewritten four-scales paragraph cited the wave-length figures to the Provisional Values Register › Spawning & Waves; that section has 23 rows and none of them states a combat wave's target or maximum duration. The figures live in › Encounter Budgets, row by row. The same sentence also over-reached in claiming every figure is left to the Register row that owns it, when the moment-to-moment frame has no row at all (F01-06) | Major | P0.4 spot-check, iteration 2 | fixed | Confirmed by the orchestrator: a search of Spawning & Waves returns no wave target or maximum duration row, while master line 226 states the figures in prose and the Encounter Budgets rows carry the per-wave maxima | The substantive claim was right and only the address was wrong, but iteration 1's strongest single result was that all 20 citations resolved, and a fix aimed at numbers-rule fidelity broke that - which is why it is Major rather than Minor. Now cites Encounter Budgets for the per-wave maxima, Spawning & Waves for the inter-wave gap, and Onboarding & Session for the derived arithmetic, and names the moment-to-moment gap explicitly instead of implying a citation exists for it |
| F01-30 | Three smaller iteration 2 findings on document 01: the restored "intent mix" clause was self-undercutting, reading "the wave's intent mix rather than by any single wave" where the master's point is that intent mix rather than any single wave delivers the guarantee; the new precedence preamble scoped the table to conflicts between two features, where the document elsewhere scopes it pillar against pillar, leaving a single proposal that pulls two pillars apart outside the stated scope; and the phrase "passed that gate", introduced by the F01-13 fix, would trip any mechanical sweep for the banned gate vocabulary even though it describes a design test on a game feature rather than a project review gate | Minor | P0.4 spot-check, iteration 2 | fixed | docs/01 against MASTER_SDLC.md > Explicit Anti-Patterns and against document 01's own scope statements | All three reworded. The banned-word collision is worth noting for later phases: the vocabulary the project forbids in one sense is ordinary English in another, so a mechanical sweep will produce false positives and the fix is to avoid the word rather than to argue the sense |
| F01-31 | The required-field manifest that closed F01-15 stops at the contract level, while roughly a dozen of MASTER's bullets are compound and name sub-quantities the schema puts inside struct resources. The manifest asserts only the wrapper, so the same failure mode iteration 1 graded Major survives inside the interiors of the 42 struct types | Minor | P0.6 critical agent, iteration 2 | fixed | Demonstrated twice by deletion: removing `XpLevelCost.shard_value` (MASTER bullet "XP shard value and level cost formula") from struct script and sample leaves exit 0, as does removing `TowerFootprint.interaction_radius_px` (MASTER bullet "Footprint radius and Interaction Radius") | Delegated: extend the manifest one level, keyed by struct class name and transcribed from docs/20's Contract Field Semantics struct wording rather than from the scripts, since deriving it from the scripts would reproduce the exact defect being fixed. Falsification required on the reviewer's two demonstrations plus three further struct fields it did not use **Closed.** A `STRUCT_REQUIRED_FIELD_MANIFEST` keyed by struct class name was added, transcribed from docs/20's field semantics rather than from the scripts, and hooked into the existing validation recursion so it is exercised at every depth and every array element without a second driver list. 41 of the 42 struct classes are mapped; `BandedValue` is deliberately left unmapped and the reason is the same principle - docs/20 types its two sub-quantities as separate shared-table rows and never writes a combined struct shape, so mapping it would mean deriving the manifest from the script. Falsified on both of the reviewer's demonstrations, on three further sub-fields including a doubly-nested one, and independently by the orchestrator on an array-nested field the fix implementer had not used: deleting `SpawnGroup.spawn_interval_seconds` now fails naming the docs/20 wording, the array index and the struct class.  |
| F01-32 | F01-15's second sentence - that the Schema check performs no type comparison against docs/20 - was never addressed, while the ledger row read `fixed` without distinguishing which half. The phase's own top predetermined risk lists the Schema check as a detector of type mismatches | Minor | P0.6 critical agent, iteration 2 | open, needs an owner | Proved by retyping `PickupDefinition.value` from `int` to `float` against docs/20's `Value` typed integer: the check still exits 0 | Recorded rather than quietly fixed, because the honest resolution may not be code. The sample-side case the risk row actually describes **is** covered - a wrong-class reference in a `.tres` is caught by the null assertion that F01-08 documents - so what is missing is script-side type conformance against docs/20, which is a different and larger instrument. The choice between building it, narrowing the risk row's wording, or deferring it with an owner belongs to the author. F01-15's status is corrected above to stop claiming this half |
| F01-33 | `UpgradeDefinition.console_price_per_rank` is the nearest surviving analogue of F01-16: docs/20's Economy row says the Console price formula is what "every prototype upgrade's Console price per rank resolves from", yet the field is an independently authored integer with no cross-check against the Economy Configuration | Minor | P0.6 critical agent, iteration 2 | open, needs an owner | docs/20 Economy Configuration field table against `src/data/upgrade_definition.gd` | Genuinely weaker than F01-16 and explicitly not a repeat of it: docs/20 types this field `integer Scrap` rather than "reference to", the price is a product of the formula rather than a copy of a value, and the samples are unambiguous placeholders so no drift is demonstrated. The reviewer also confirmed that nothing else in `src/data/` duplicates a Register-owned number the way F01-16 did, having grepped docs/20 for every field typed as a reference or derived value |
| F01-34 | Two ledger rows added during iteration 2, F01-29 and F01-30, were inserted **above the table header** and with their Status and Evidence columns in each other's positions. They rendered as a detached headerless table, and a column-count check could not catch it because all rows were still 7 columns | Minor | P0.6 critical agent, iteration 2 | fixed | Confirmed by the orchestrator: both rows sat at lines 4-5 with the header at line 11 | **Same defect class as F01-25, one iteration after it was raised against this very file, and by the same hand.** The cause is mechanical: when the ledger was rebuilt, the 'Severities/Statuses' legend moved from the bottom of the file to the top, and the insertion script still anchored new rows to that legend. The lesson is that the structural check has to be run after every edit to the record, not once at the gate - a column count alone is insufficient, so the check now also confirms rows sit below the header and that the Status column holds a status word |
| F01-35 | Three observations from the P0.6 re-review that are not defects but should not be lost. (a) `enemy_intent_mix` is the only Resource array in the entire fixture set with more than one element; the other eight hold exactly one, so the fix for per-element array validation is correct but exercised in a single place. (b) Iteration 1's conformance table miscounts two contracts - Director Configuration is 15 fields, not 16, and Economy Configuration is 11, not 12 - though its conclusion is unaffected; those two numbers should not be reused as a baseline. (c) The working tree moved under a reviewer again mid-review, which F01-27 recorded in iteration 1 and said would apply to iteration 2 | Minor | P0.6 critical agent, iteration 2 | recorded | `evidence/review_p06_iter2.md` | (a) argues for richer fixtures when Phase 02 authors real content. (b) is corrected here rather than in the iteration 1 report, which is left as written. (c) is F01-27 recurring after being recorded, which is worse than it occurring once: the rule was written down and then not applied |
| F01-36 | The zero-test guard added earlier in iteration 2 fails **open**. It is a negative match on captured stdout (`No test cases found`), so whenever stdout is not captured the marker is simply absent, the exit code is still 0, and the script reports PASS for a run that executed nothing. Iteration 1 prescribed the positive form - assert gdUnit4's own executed-count summary - and the negative form was implemented instead | Major | P0.7 critical agent, iteration 2 | fixed | Reproduced twice: a `-GodotPath` pointing at any real executable that exits 0 silently, and an unwritable TEMP directory combined with a typo'd `-TestPath`, which printed `PASS (exit 0): the suite ran and every test under res://tests/harness/typo_not_real passed.` The second is an environment condition, not operator error | Rewritten as the positive assertion originally prescribed: the run is trusted only if `Executed test cases : (n/n)` is present with n at least 1, so a missing or unreadable output fails closed for any reason. Both of the reviewer's reproductions now exit 1 naming the cause, and the pass line reports the executed count so a silently shrinking suite is visible. **This is the orchestrator's error, not an implementer's** - the weaker form was chosen when the stronger one had already been specified |
| F01-37 | Two smaller runner defects: an empty `-TestPath` made gdUnit4 exit 100, which the script reported as "at least one assertion failed" - the wrong cause entirely; and guard 1 itself threw on an empty `-GodotPath`, leaving the downstream null check to catch it by accident | Minor | P0.7 critical agent, iteration 2 | fixed | Both reproduced by the reviewer | Guard 1 now tests for null-or-whitespace before touching the filesystem, and a new guard rejects any `-TestPath` that is not a `res://` path before the suite runs. Both verified: each now exits 1 with its own message |
| F01-38 | `tests/run_tests.ps1` was referenced in no permanent document - docs/28 still handed later phases the raw, unguarded command line - and the script's own usage examples invoked `pwsh`, which is not installed on this machine | Minor | P0.7 critical agent, iteration 2 | fixed | docs/28's gdUnit4 section; the script's `.EXAMPLE` blocks | docs/28 now directs later phases to the runner and states why the raw command is insufficient on its own: a bare exit 0 from it does not mean any test executed. Examples switched to `powershell -NoProfile`, which runs here |
| F01-39 | Two stale rows in the phase's own record: the EXECUTION_LOG rows carrying the falsified exit-1 reasoning had no inline correction marker, although a comparable row had received one for F01-26; and F01-21's ledger text still said the Change Log row did not exist after it had been written | Minor | P0.7 critical agent, iteration 2 | fixed | EXECUTION_LOG rows for the harness check and the docs/28 re-read; LEDGER F01-21 | Both corrected in place with dated markers rather than rewritten, so the original mistakes stay visible. F01-21 is narrowed to what is genuinely still open: no `docs/` document has a Change Log section at all, which is a question for the author rather than an omission |
| F01-40 | Verification worth recording rather than a defect: the `.gitattributes` protection for the gdUnit4 hash was tested rather than reasoned about. The reviewer cloned the repository four times - with `core.autocrlf` set to true, input and false, plus a negative control with `.gitattributes` removed from both worktree and index at `autocrlf=true` | Minor | P0.7 critical agent, iteration 2 | recorded | The three protected clones each produced 516 files and the recorded hash `5ae43377...8b9a`; the unprotected control produced `31b80546...` with CRLF terminators | The pin holds for a clone on any line-ending configuration, and the protection is load-bearing rather than decorative. The export filter arms were falsified the same way: dropping `reports/*` and `src/data/samples/*` in a scratch copy grows the pack from 70,756 to 182,244 bytes and packs the report assets and all eleven sample remaps |
| F01-41 | Loop rule (c) was broken continuously through review iteration 2. The phase reviewer watched five separate edits land in the live tree while it was reviewing, was handed a **false failure** from the Schema check because a parallel reviewer had renamed a method in that tree, and had to reconstruct the committed tree in a scratch directory to review at all. No reviewer was told the tree had moved. This is F01-27 recurring inside the very iteration whose LESSONS row forbids it, which is why it is Major here and was Minor there | Major | Phase reviewer, iteration 2 | fixed as a rule, recorded as a failure | Five timestamped observations in `evidence/review_phase_iter2.md`: a method renamed at 07:30:32, four record and document files edited 07:37-41, two ledger rows written above the table header at 07:39:48, the ledger growing 28 to 35 findings at 07:44:46, and the runner script rewritten around 07:46 | **This is the orchestrator's failure and the most legitimate finding of the iteration.** Writing the lesson down after iteration 1 changed nothing, because nothing enforced it. The rule is corrected in LESSONS.md to name a mechanism rather than an intention: reviewers read a frozen tree - a commit, a tag, or an exported copy - and any mid-gate change is either deferred until the gate closes or announced to every open reviewer. The reviewer's own caveat is the proof it matters: it warned that any statement it made about the record had a shelf life of minutes |
| F01-42 | Three ledger rows owned by the author were never carried into `NEXT_SESSION.md`, which is where the author actually looks for what needs them, and the decision to withhold the "Phase 0 accepted" row had no Review Decision Log row of its own | Minor | Phase reviewer, iteration 2 | fixed | `NEXT_SESSION.md` > "What needs the author, and when" against the ledger's author-owned rows | `NEXT_SESSION.md` now carries a table of every author-owned Phase 01 row - F01-02, F01-06, F01-32, F01-33 and F01-21 - each with the question and why it is the author's rather than an agent's, and states plainly that none of them blocks Phase 02. The withholding decision is recorded as D93 with its alternatives |
| F01-43 | `phases/README.md`'s row 01 asserted "no open Blocker or Major remains", which is the phase assessing its own gate - and it rested on the status `recorded`, which the ledger's own header does not declare as a status | Minor | Phase reviewer, iteration 2 | fixed | The README row against LEDGER.md's declared status vocabulary | The row now states the four reviewers' scores as facts and says explicitly that whether the phase meets its bar is not asserted there, pointing at the reviewers' reports and the ledger instead. `recorded` is also added to the ledger's declared statuses, since five rows legitimately use it for observations that are neither defects nor fixes |
| F01-44 | F01-02's deferral rested on an absolute negative claim with no experiment behind it - "there is no fix available from this project's side" - written into the row that argues the phase clears its bar, during the same iteration that added a LESSONS row saying negative claims need experiments | Minor | Phase reviewer, iteration 2 | fixed | LEDGER F01-02 as written at iteration 2 | Corrected to separate what is established from what was never tested. Established: `deny` is the only setting in the permission system's vocabulary that intercepts a subagent. Untested, and now named as work rather than dismissed: a **PreToolUse hook** runs in the harness rather than the permission layer and might gate the two tools for subagents. Nobody has tried it, and the row now says so. Carried into the author queue in `NEXT_SESSION.md` |
| F01-45 | Two smaller record defects: LESSONS.md's trailer still read "Phase 00 is in progress", and the structural-check lesson the phase had just adopted was too narrow - a column count cannot detect a row inserted above the table header, nor a row whose Status and Evidence cells are in each other's positions, and both of those happened after the lesson was written | Minor | Phase reviewer, iteration 2 | fixed | LESSONS.md trailer; the ledger's two subsequent structural breaks, both of which passed a column count | The trailer is rewritten and now carries the sharper observation the phase actually earned: a lesson here is reliably applied to the artifact it was learned on and reliably not generalised one artifact over, so the useful question at phase entry is where else it applies. The structural lesson is widened to row position, column count and column identity |
| F01-46 | The doc-lint claim in EXECUTION_LOG was briefly stale: iteration 2's own fix for the precedence circularity introduced the phrase "passed that gate" into document 01, contradicting the recorded lint result while the gate was open | Minor | Phase reviewer, iteration 2 | fixed | Observed at 07:33 by the phase reviewer; independently caught by the P0.4 reviewer as part of F01-30 and reworded at 07:39 | Already repaired before either reviewer reported, by the P0.4 finding. Recorded because two independent reviewers found the same thing within six minutes, which is what a moving tree costs: both spent effort on a defect that was being fixed underneath them |
| F01-47 | The orchestrator's own ledger-integrity check was reading the wrong column. It tested the "Raised by" cell for the values `Major` and `Blocker`, which no Raised-by cell can ever contain, so its "open Blocker/Major: none" result was structurally incapable of returning anything else. That result had already been reported to the author twice as evidence the phase cleared its bar | Major | Orchestrator, self-caught while re-running the check | fixed | The check used split index 4 for severity where the header puts it at index 3; rewritten to derive every index from the header row by name, it reports 46 findings, 15 Major, 31 Minor and - now genuinely tested - no open Blocker or Major | **The phase's own defining defect, committed by the orchestrator in the act of verifying the ledger against it.** It is the same shape as F01-15 and F01-36: a check trusted because it was green, which could not have gone red. The conclusion happens to survive re-testing, which is luck rather than diligence and does not make the earlier report sound. The fix is the rule the phase already wrote down and did not apply here - a check is not believed until it has been made to fail - and the corrected version derives its column positions from the header instead of hardcoding them, so a future column reorder cannot silently disarm it |
